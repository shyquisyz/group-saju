# 관계도 (Relation Saju) — 프로젝트 인계 문서 v2

> 새 대화 시작 시 이 문서 + `index.html` + `GALAXY-LAYOUT-SPEC.md`를 함께 업로드할 것.
> v1(HANDOFF.md) 대비 **§2가 대폭 개정**되었다. v1의 §2는 "변경 금지"였으나 이번 세션에서 근본 구조가 바뀌었으므로 **v2가 우선한다.**

---

## 0. v1 → v2 개정 요약 (반드시 먼저 읽을 것)

> **표기**: 특별한 언급이 없으면 "결정 = 구현 완료"다. **[미구현]**/**[부분구현]**이 붙은 항목은
> 방향은 확정됐지만 코드에는 아직(또는 일부만) 반영되지 않았다는 뜻 — 이 표만 보고 구현됐다고
> 착각하지 말 것. 아래 세 항목은 2026-08-23 §5.3 작업 중 감사(audit)로 확인했다.
>
> **이 표는 요약이다. 각 항목의 실제 구현 상태는 본문 절이 우선한다. 본문을 고칠 때는 이 표도
> 함께 갱신할 것.** (본문만 "구현 완료"로 고치고 이 표의 [미구현]을 남겨둔 사고가 세 번 반복됐다
> — 2026-08-24 실사에서 "다중소속", "고리 가상 성계 예외" 두 행이 그렇게 발견됐다.)

| 항목 | v1 | v2 | 사유 |
|---|---|---|---|
| 항성 판정 | 그룹마다 다르게 산출, 핵심 차별점 | **나(관측자)가 은하의 유일한 항성.** ~~그룹 항성계 중심엔 나의 분신~~ → **[F-1, 2026-08-27] 분신 폐기.** 그룹 항성계의 궤도 중심은 **비어 있고**, 그 자리에 아주 옅은 **반지 표식** 하나만 놓인다. 화면에 나는 원점의 본체 하나뿐이다(§2.8) | "내가 주인공" 결정 |
| §2.2 점수 룰 | 항성 결정에 사용 | **폐기하지 않고 "중심인물" 산출로 강등** (텍스트 인사이트로만 노출) | 계산 자산 보존 |
| 궤도 | 항성 중심 동심 궤도 | **모든 그룹 궤도가 나를 통과하는 원** | 관계 = 나를 거쳐 감 |
| 궤도 평면 | 단일 평면 고정 (v0.7) | **0°/45°/90°/135° 결정적 분산** | 통과 구조상 평면 집중 시 중심이 뭉개짐 |
| 미분류 | 없음 | **다른 그룹과 동일한 항성계.** `orderIndex=-1`, `R_g=110` 고정([F-3, 2026-08-27] 22.0→110 = ×5 ← [C-2] 18.0→22.0)으로 원점을 통과하는 궤도를 돎. 각도 계산은 `orderIndex`를 직접 쓰지 않고 §3.2의 `slot`(미분류→0)을 거친다 — JS의 `%`가 음수를 그대로 돌려줘 규격 밖 경사(-45°)가 나오던 버그를 [C-4]에서 잡았다(§2.2 초안은 "은하 중심에 정지"였으나 공전이 구현되지 않았던 게 밝혀져 GALAXY-LAYOUT-SPEC §3.6으로 재개정) | 그룹 없는 사람 = 나와 1:1만 존재. 관계가 안 정해졌으니 궤도도 안 정해졌다는 의미로 최원 궤도 고정 |
| 나 없는 그룹 | 생성 가능 (§7.2) | **폐지 [구현 완료, 2026-08-24]** — 그룹 생성 시트는 "나"를 기본 체크·해제 불가로 열고, 인물 수정 시트는 내가 이미 속한 그룹의 칩을 잠그며, 나를 편집할 때는 삭제 버튼 자체가 사라진다. 데모 데이터도 규칙에 맞췄다(모든 그룹에 나를 포함). 막을 때 이유를 시트 안에 상시 노출. 자세한 내용은 §7.2 | 나=항성이면 모순. 가상 성계가 대체 |
| 그룹 뷰 | 드릴다운 | ~~**모든 그룹 상시 표시**, 선택은 포커스일 뿐~~ → **[F-2, 2026-08-27] 선택 = 장면 전환.** 선택한 항성계가 **멈추고 그 중심이 나에게 맞춰진다.** 멤버는 나를 중심으로 공전하고 다른 항성계는 화면에서 사라진다. 카메라는 이동하지 않는다 — 항상 원점을 보고 거리만 바꾼다 | 8단계에서 드릴다운을 폐기한 근거는 "선택이 숨김이 되면 은하 전체가 안 보인다"였는데, [F-3]에서 거리가 5배가 되며 은하 조망(카메라 889)과 그룹 조망(109)의 축척 차가 8배로 벌어져 **한 화면에 둘 다 담는 것 자체가 무의미**해졌다 |
| 다중소속 | 미정 | **그룹마다 복제 노드로 표시 [구현 완료, 2026-08-24]** — 형제 그룹 다중소속에 한해 3D 추가 인스턴스가 생성된다(`groupInstances` Map, `index.html`). 부모-자식 그룹은 설계상 중첩으로 표현하고 복제하지 않는다. 자세한 내용은 §7.4 | "내가 주인공" 결정 |
| 고리 (§2.4) | 은하 전체 기준, 모든 뷰 동일 | **[2026-08-26 재설계, 9852764] 기준을 "은하 전체 발신량" → "나와의 관계"(§7.2 에고센트릭)로 교체.** 1개=나를 생 / 2개=나를 극 / 3개=소속이 넓다 / 그 외 없음. 구 규칙은 데모 8인에서 8명 전원이 달아(총 22개) 표식 구실을 못 했다 → 7개·4명. ~~가상 성계 예외~~ 소멸(`rebuildVirtualRings()` 삭제) — 예외의 근거가 발신량이었는데 새 규칙이 발신량을 다루지 않는다. 규칙이 하나로 통일됐다. 자세한 내용은 §2.4 | 모두가 달면 표식이 아니다 |

---

## 0.5 이름 [2026-08-27 확정]

| | 이름 | 비고 |
|---|---|---|
| 서비스명 | **그룹사주** | **가칭** — 바뀔 수 있다 |
| 저장소 | **`group-saju`** | 확정 |

### 왜 "그룹사주"인가

**1. 차별점을 정확히 가리킨다.** 기존 사주 앱은 개인 아니면 1:1 궁합에서 멈춘다. 이 서비스는
**여럿의 관계**를 본다(§1). "그룹"이 그 차이를 한 단어로 말한다.

**2. 이름은 화면이 말해주지 않는 것을 말한다.** 우주·행성·은하는 열면 바로 보인다 — 이름이 또
말할 필요가 없다. 반대로 "여러 명을 묶어서 본다"는 화면만 봐서는 바로 안 읽히므로 이름이 진다.

**3. 검색에 유리하고 다른 사주 앱과 안 섞인다.** "사주"로 이미 붐비는 자리에서 "그룹"이 붙으면
찾는 사람이 정확히 찾고, 개인 사주 앱들과 이름이 겹치지 않는다.

### 검토했다가 버린 것

| 후보 계열 | 버린 이유 |
|---|---|
| 영문 조어 | 한국 사용자가 기억하기 어렵고, **서양 점성술로 읽힌다** |
| "사이" · "행성사이" 계열 | **두 사람으로 좁게 읽힌다** — 여럿을 담는다는 강점이 죽는다 |
| 줄임말 | 원말이 알려지기 전에는 **아무 뜻 없는 소리**다 |
| "단체사주" | 사무적이고 낡은 느낌. "단체"는 조직을, "그룹"은 사람 묶음을 가리킨다 |

---

## 1. 한 줄 정의

개인 사주가 아니라 **관계 사주**를 보여주는 서비스. 여러 사람을 등록해 그룹으로 묶으면, 그 그룹이 하나의 **항성계**로 시각화되고 관계 해석이 자동 생성된다.

기존 사주 앱은 개인 풀이 + 1:1 궁합에서 멈춘다. N명을 노드로, 관계를 엣지로 그리는 그래프 뷰는 주류 서비스에 없음. 사람들이 진짜 궁금해하는 건 "내 사주"가 아니라 "우리 사이가 왜 이런가".

---

## 2. 확정된 컨셉 (v2)

### 2.1 은하 구조 — 3구역

```
은하 중심   나 = 유일한 항성 (실체, 꽉 찬 발광). 원점에 고정된 건 이것 하나뿐

은하 궤도   그룹 항성계들 + 미분류 성계. 중심은 비어 있고 옅은 반지 표식만 놓임
              ([F-1, 2026-08-27] 구 "나의 분신(흐린 발광)이 앉음")
              모든 궤도 원이 나를 통과한다(미분류도 예외 없음 — §2.2 초안 "정지" 폐기,
              GALAXY-LAYOUT-SPEC §3.6 참고)
              경사: orderIndex(그룹은 생성 순, 미분류는 항상 -1)로 ψ_i/tilt_i 결정적 배정

바깥        가상 성계 (슬롯 1개)
              복제본을 담는 작업대. 꼬리로 구분. 궤도 없음, 고정
```

**Zone A/B 구분이 여전히 유효한가 (v2 미분류 재분류 이후 판단):** 미분류가 Zone B로 옮겨가면서
Zone A는 코드상 "나를 원점에 고정" 두 줄로 줄었다. 그래도 구분 자체는 유지한다 — 좌표계(§1) 전체와
모든 Zone B 궤도("원점을 통과")의 전제가 "나=유일하게 고정된 기준점"이라는 사실이기 때문에,
코드량과 무관하게 개념적으로는 여전히 구분할 가치가 있다고 판단했다. 다만 문서상 "구역 A"라는
이름에 "은하 중심"처럼 공간적 함의를 싣지 않도록 GALAXY-LAYOUT-SPEC §2 제목을 "나(본체 항성)"로
좁혀뒀다.

### 2.2 항성 판정 룰 → "중심인물" 산출

```
score = 생(生) 발신 수 × 2 + 극(剋) 발신 수 × 1 − 극 피격 수
최고점자 = 그 그룹의 중심인물
```

**계산은 그대로 유지한다.** 다만 중심에 앉히지 않고, 그 사람의 고리를 밝히거나 패널에 "이 그룹의 중심축: 구성원B"로 표기한다. 검증 예시(나+구성원B 부부 → 구성원B가 최고점)는 여전히 유효하나, 이제 **시각 중심이 아니라 텍스트 인사이트**다.

### 2.3 일간별 행성 아이덴티티 (절차적 텍스처) — **[2026-08-26 전면 재설계, dd27c59]**

**[F-5, 2026-08-27] 방향 전환 — "질감"에서 "그림"으로.** 사용자 판단: *"여름 나무 사주면 천체에
나무가 있는 것처럼 보였으면 좋겠다"*, *"은하 화면에서 잘 안 보여도 괜찮다, 어차피 확대하면
보이니까"*. 판단 기준이 **작은 크기(7.5px)의 구분**에서 **확대한 크기(40~60px)의 판독**으로
바뀌었다. 작은 크기는 색만 갈리면 된다(F-5-2에서 달성).

**목 묶음에서 얻은 세 원칙** — 나머지 형상에 계속 적용한다.
1. **같은 색 위에 밝게 얹으면 녹는다.** 어둠으로 형태를 만들고 하이라이트는 위쪽에만 얹는다.
   (수관을 `L+20`으로 밝게 그렸더니 구 음영·환경 반사를 지나며 형태가 사라졌다)
2. **형상은 위도 중간대에만.** 정거원통도법이라 극 쪽은 가로로 늘어나 뭉개진다.
3. **세부보다 실루엣.** 사용자가 보는 크기에서 개별 잎은 어차피 안 보인다.
4. **3D를 넣을 때는 정점 `|r|` 최댓값을 반드시 잰다.** 겉면 반경이 1이므로 그 안에 들어가야
   한다. 눈으로는 안 보이는 이탈이 생긴다(甲에서 가지 끝 1.22 · 이끼 돔 1.087 · 돌 1.05
   — 세 건 다 실측으로 찾았다). 실측값: 甲 0.985 / 丁 0.985.
   **방식 3(표면 위로 솟은 3D)은 규칙이 뒤집힌다** — 1을 넘는 것이 목적이므로 상한을 재서
   지킨다(戊 1.299 / 상한 1.34). 안 넘으면 구 안에 묻혀 아무것도 안 보인다.
6. **[2026-08-27] 레퍼런스 한 장이 열두 번의 시도보다 싸다 — 방식부터 맞춘다.**
   방식이 셋이다(표면 / 구슬 안 / 표면 밖). **방식이 틀리면 그 안에서 아무리 고쳐도 안 된다.**
   甲은 표면 방식으로 세 번, 戊는 표면 밖 방식으로 **열두 번** 실패했고 둘 다 레퍼런스를 받고
   방식을 바꾸자 풀렸다. **세 번 고쳤는데 같은 계열의 실패가 반복되면 방식을 의심한다** —
   증상이 아니라 **증상의 공통점**을 본다. 막히면 레퍼런스를 요청하는 게 추측보다 싸다.
   (전문은 CLAUDE.md "은유를 그림으로 옮길 때 — 방식부터 맞춘다")
5. **[2026-08-27, 己에서] 형태가 맞물려야 하는 것은 난수로 배치하면 안 된다.**
   밭·타일·격자·결정면처럼 **서로 이어지는 것**이 그렇다. 난수로 뿌리면 사이에 빈 데가 생기고,
   맞물림이 깨지는 순간 그건 무늬가 아니라 **낙서**로 읽힌다(己 2차 실패가 정확히 이것이다 —
   뙈기 52개를 난수 좌표에 뿌렸더니 매끈한 공에 빗금 조각이 흩어졌다). 격자로 깔고 위치·크기·
   방향만 흔든다. 난수는 **흩어진 것**(돌·물방울·알갱이)에만 쓴다.
   남은 것에 미리 걸어 보면 — **辛(결정면)은 패싯이 맞물려야 하므로 난수 금지**(다면체
   지오메트리나 보로노이처럼 구조가 맞물림을 보장하는 방법을 쓴다), **癸(물방울)는 흩어진
   것이 맞으므로 난수 가능**. 戊(산맥)도 이 원칙에 걸렸다 — 봉우리를 난수로 뿌린 것이
   10번째 시도에서 드러난 "빈 정면"의 원인이었다.

**[2026-08-27] 戊 — 열두 번 실패하고 방식을 바꿔 해결했다.**
"구 표면 위로 솟은 3D 산맥"으로 열두 번 시도해 전부 스크린샷에서 떨어졌다(성게 / 두리안 /
딱지 / 이빨 / 호박등 / 초콜릿 / 감자 / 납작함 / 얼룩 / 빈 정면 / 호두 / 울퉁불퉁한 소행성).
실패 목록은 `alpsParts` 주석에, 전체 표는 커밋 `31a829c`에 남아 있다.

> ### 열두 번을 관통한 오해
> **없는 문제를 풀고 있었다.** 사용자가 원한 것은 "표면에 산이 솟은 행성"이 아니라
> **甲과 같은 구조 — 유리구슬 안의 산 풍경**이었다. 은유가 "천체의 지형"이 아니라
> "구슬 안에 담긴 큰 산"이었던 것이다. 레퍼런스(2.png)를 받고서야 알았다.
>
> 10번째 시도의 진단("봉우리를 어디에 놓을까가 아니라 지형이 전면에 있어야 한다")은
> 그 방식 **안에서는** 옳았다 — 실측 근거(+Z 정점 최대 반경 1.076, 봉우리 50개가 표면의
> 20~25%만 덮음)도 정확했다. 다만 방식 자체가 틀렸으므로 그 진단으로는 못 풀 문제였다.

**얻은 것 — 레퍼런스는 열두 번보다 싸다.** 甲도 1.png를 받고 풀렸고 戊도 2.png를 받고 풀렸다.
**은유를 그림으로 옮길 때는 방식(표면 / 구슬 안 / 표면 밖)부터 사용자와 맞춘다.** 그게 어긋나면
그 안에서 아무리 잘 만들어도 안 맞는다.

**구현** — 甲과 골격이 같아 `soilBase()`(흙 사발 · 이끼 방석 · 박힌 돌)를 공용 함수로 뽑아 썼다.
甲에서 여섯 번 고쳐 얻은 경계 실측 세 건(돔 1.087 / 돌 1.05 / 가지 끝 1.22)이 그 함수 주석에
그대로 있어 戊가 다시 밟지 않는다. 새로 만든 것은 봉우리·눈·침엽수·안개뿐이다.
戊에서 다시 두 번 틀렸고 둘 다 원칙 1이었다 —
- **[흰 텐트]** 주봉이 작아 눈이 봉우리 전체를 덮었다. 레퍼런스의 층은 **이끼 → 초록 언덕 →
  회색 바위 → 흰 눈**인데 중간의 회색 바위 구간이 통째로 빠져 있었다. 주봉을 크게(밑면이 구
  지름의 80%) 하고 눈은 꼭대기 26%만, 초록은 주봉 밑면 바깥으로 밀어냈다.
- **[흰 산 / 흰 버섯]** 바위가 중간 회색이라 조명을 받아 희끄무레해져 눈이 안 도드라졌고,
  구름 덩이가 부봉만 해서 흰 버섯이 됐다. 바위를 한 단 낮추고 구름을 산기슭으로 내렸다.

**甲과 갈라지는 축**: 甲은 밝은 연두(0xbde05f~)의 넓은 수관, 戊는 **회색 바위와 흰 눈이 주인공**
이고 초록은 아래쪽에만 두되 어둡고 탁한 침엽수 녹색(0x2f5522~)을 쓴다.

**[2026-08-27] 블룸 도입 — 라벨 별도 패스는 안 쓴다(실측으로 불필요 확인).**
r128 기본 번들에 후처리가 없어 jsdelivr에서 6개 파일(23.3KB)을 받는다. 못 받으면 `makeComposer`가
null을 돌려주고 종전대로 `renderer.render`로 그린다.
흰 글자가 같이 번져 뭉개질 것을 걱정해 **라벨 별도 패스** 방안을 만들고 실측한 결과:
- 이름표의 휘도 상한은 텍스처 색(`#eceef2` = 0.933)으로 **고정**이다 — 확대·조명과 무관하다.
- 문턱 0.92에서 이름표는 1~5픽셀, 0.96에서 0픽셀만 넘는다 → 문턱으로 **가를 수는 있다.**
- 그런데 문턱 0.88 이상이면 **블룸 자체가 안 보인다**(항성 둘레 평균 0.682 → 0.697, 2%).
즉 "라벨을 살리면 블룸이 죽고, 블룸을 살리면 글자가 번진다" — **하나의 손잡이다.**
사용자 판단: 번지는 게 오히려 자연스럽다. 별도 패스(+0.041ms, 코드 두 배)를 쓰지 않는다.

**값** `0.55 / 0.55 / 0.78`. 세기별 과노출(0.95 초과) 픽셀 수로 골랐다 —
항성 15/50/**161**/835, 丙 0/1/**41**/2305, 丙 이름표 0/0/**8**/512 (세기 0/0.35/**0.55**/0.8).
0.8부터 丙의 표면 무늬가 통째로 날아간다. 0.55는 후광이 보이면서 텍스처가 남는 마지막 지점이다.
**성계 장면에서 정하고 은하 조망에서 검증했다**(CLAUDE.md "한 장면에서 정한 값은 다른 장면에서
검증한다"). 비용 +0.103ms = 60fps 예산의 2.8%(`gl.finish()` 동기화 후).

**구현 함정 셋** — 다시 손댈 때 그대로 필요하다:
1. `composer.renderToScreen`은 **기본 true**. false면 화면에 안 나온다.
2. `composer.render()` 뒤 **renderer의 렌더 타깃이 null이 아니다.** 그 뒤 `renderer.render`를
   부르면 화면이 아니라 그 타깃에 그려져 **아무것도 안 보인다.**
3. 컴포저 내부 `RenderPass`도 `renderer.render`를 부른다 — 루프를 감싸면 **무한 재귀**.

**[미착수] 辛(다이아몬드) 재작업 — 3.png 기준.**
지금 辛은 "은색 구 안에 육각 결정 다발"인데, 원한 것은 **구 전체가 하나의 다이아몬드**다.
방식은 바뀌지 않는다(구슬 안 → 구 자체가 보석). 레퍼런스에서 읽을 것:
- 구 전체가 **삼각 패싯**으로 덮여 있다. 크고 작은 삼각형이 섞이고 **서로 맞물려 빈틈이 없다**
- 안이 비쳐 **반대편 면들이 겹쳐 보인다** — 깊이가 있다
- **패싯마다 밝기가 제각각**이다(거의 흰색 ~ 짙은 남색). 그 대비가 보석을 만든다
- 몇 군데에서 **별 모양 섬광**이 터진다 — 방금 넣은 블룸이 이걸 번지게 해야 보석다워진다
- 가장자리에 **무지개빛**이 살짝 돈다
**원칙 5가 그대로 적용된다** — 삼각형이 맞물려야 하므로 **지오메트리가 맞물림을 보장**하고
(정이십면체 세분 등) 난수는 **밝기 배정에만** 쓴다.
은유는 유지: "예리하고 완성도 높은 디테일형, 상처도 오래 간다" — 다이아몬드는 가장 단단하면서
각도를 잘못 치면 깨진다.

**[2026-08-27] 원칙 1의 확장 — 밝은 대상에도 그대로 적용된다.**
원칙 1은 목(어두운 나무)에서 얻었지만 **화(태양)에서 똑같이 밟았다.** 丙에서 세 번 갈아엎은
원인이 전부 하나였다 — **알베도를 밝게 그리고 조명으로 보이게 하려 한 것.**
태양은 밝은 게 당연해 보이니 반대로 갔던 것인데, 결과는 매번 원칙 1이 말하는 그대로였다:
연분홍 공(2차), 구멍 뚫린 살색 공(3차). 밝은 albedo는 조명·환경 반사를 지나며 형태를 잃고,
채도가 높으면 L을 올릴수록 흰색이 아니라 **분홍**으로 간다.

> **밝게 보여야 하는 대상일수록 알베도는 어둡게 그리고, 밝기는 다른 채널이 진다.**
> 丙은 알베도 L 68 → **46**으로 내리고 밝기를 **발광맵**에 넘겨서 해결됐다.
> 조명받아 밝은 것과 스스로 밝은 것은 화면에서 **다르게 읽힌다** — 전자는 행성, 후자는 항성이다.

곁따라 나온 규칙 둘:
- **알베도 밝기와 발광 문턱은 한 쌍이다.** 알베도를 어둡게 하면 문턱도 같이 내려야 한다
  (丙: `.45~.85` → `.20~.60`). 한쪽만 고치면 아무것도 발광 범위에 안 들어와 그냥 어두운 공이 된다.
- **채도는 열어도 되지만 색상은 안 된다.** 색상(h)이 곧 오행이다. 채도는 물리적 근거가 있다 —
  백열은 적열 → 황열 → 백열로 가며 채도가 떨어진다(`sCol`의 `S` 인자, `STEM_EMIS_COLOR`).

**[F-5, 2026-08-27] 甲만 예외 — 표면이 아니라 유리구슬 안의 3D 나무.**
세 번 고쳐도 표면에 그린 나무는 "나무처럼" 보이지 않았다(원칙 1·2가 정확히 그 실패 기록이다).
甲은 **여름 나무 색의 반투명 구 + 그 안에 선 3D 나무**로 간다. 회전하면 나무도 같이 도니
어느 각도에서든 나무이고, 늘어남 문제 자체가 없다.
- **성능(실측)**: 나무 하나를 메시 11개로 두면 30명일 때 드로우콜 334 / 프레임 5.94ms.
  지오메트리를 재질별로 병합해 나무당 메시 2개로 줄이니 **드로우콜 64 / 0.20ms**.
  실제 씬에서 30명 전부 甲인 최악 조건도 **드로우콜 124 / 1.06ms**(지오메트리는 33개로 공유).
- **반투명 겹침**: 안의 나무가 불투명이라 먼저 깊이를 쓰고, 그 위에 앞면 유리만 얹힌다.
  `DoubleSide`는 실패했다 — 깊이 기록이 없어 앞·뒷면이 그려진 순서대로 블렌딩되며
  `SphereGeometry`의 위도 링이 **가로 줄무늬**로 드러났다. `FrontSide`면 화소마다 정확히
  한 번만 블렌딩되므로 구조적으로 줄무늬가 생길 수 없다.
- **작은 크기**: 7.5px에서 그냥 초록 점이다(이상 없음). 겉면 불투명도 0.62는 "안이 보이면서도
  작을 때 구가 비어 보이지 않는" 선에서 잡았다.
- **적용 범위는 甲 하나.** 乙(덩굴)은 표면을 감는 게 본래 형태라 표면 방식을 유지한다.

**[F-5, 2026-08-27 2차] 甲을 "유리구슬 안 테라리움"으로.** 나무만 넣으니 허전했다 — 사용자 지시.
- **땅**: 구슬 아래를 흙더미가 채운다. 밑동은 **구의 아랫부분**(반경 0.985의 구면 일부)이라
  유리 안쪽 면에 딱 붙는다 — 떨어뜨려 놓으면 다시 "떠 있는 흙덩이"가 된다. 그 위에 이끼 낀
  둔덕과 낮은 혹 7개(완전한 돔이면 다시 매끈한 판으로 읽힌다).
- **돌**: 흙 표면에 반쯤 박아 유리 너머로 보이게 한다. 안쪽에 두면 아예 안 보인다.
- **뿌리**: 흙더미 표면을 따라 뻗는다(같은 이유).
- **잎**: 5덩이 → **14덩이**, 크기를 다양하게. 색은 연두~짙은 녹 **네 단**을 섞는다 —
  한 색으로 채우면 아무리 크게 그려도 평평한 초록 덩어리다.
- **유리 반사 띠**: 겉면에서 표면 텍스처를 떼고(이끼 결은 이제 구슬 안에 있다) 매끈하게
  (roughness 0.10) + 환경 반사를 세게(3.2) 준다. **그 띠가 없으면 "유리구슬"이 아니라
  그냥 반투명한 초록 막이다.**
- **[좌표를 한 번 다시 잡았다]** 1차는 줄기 높이 0.50에 잎이 y 0.23부터 시작해 **수관이
  줄기를 통째로 덮었고**(줄기가 안 보였다), 흙 윗면이 y=-0.18의 얇은 둔덕이라 **평평한 판**으로
  읽혔다. 흙을 -0.30으로 내리고 두껍게(0.34) 만든 뒤 줄기를 세우고 수관을 그 위로 올렸다.
- **색을 재질이 아니라 정점으로 나눈다.** 잎 네 단·흙·이끼·돌을 재질로 나누면 드로우콜이
  조각 수만큼 늘어난다. `mergeGeos`가 정점 색을 받게 해서 **나무당 메시 3개**(껍질/잎/땅)로
  유지했다. 실측 **30명 전부 甲: 드로우콜 120 / 프레임 0.43ms / 지오메트리 34개(공유)**.

**[F-5, 2026-08-27 3차 — 레퍼런스 이미지 대조 후]** 사용자가 준 참고 이미지(유리구슬 안 테라리움)와
맞춰 보고 고친 것들. **가장 결정적인 차이는 유리 색이었다.**
- **유리를 무색으로.** 초록 유리로 두면 안의 나무·이끼·흙이 전부 초록으로 물들어 "초록 막 안의
  초록"이 된다. 레퍼런스의 유리는 맑고 **초록은 전부 안에서 온다** — 그래야 잎의 연두와 흙의
  갈색이 서로 갈린다. `0x5cb87a` → `0xe6f2ea`, 불투명도 0.62 → **0.30**.
  대가: 7.5px 평균 채도가 **51% → 18%**로 떨어졌다(색상은 h117로 초록 유지). 작은 크기에서
  오행 구분이 약해지는 것은 사실이나, 사용자가 판단 기준을 "확대한 크기의 판독"으로 옮겼으므로
  그 방향을 따랐다. 되돌리려면 `GLASS_TINT`/`GLASS_OPACITY` 두 값이면 된다.
- 잎 색을 **밝은 연두 네 단**으로(레퍼런스의 초록은 전부 라임 쪽이다), 잎 14 → 22덩이.
- **가지를 드러낸다** — 잎 사이로 갈라진 가지가 보여야 나무다. 1차 7갈래 + 2차 14갈래.
- 이끼 위에 **풀 26포기**.
- **되돌아간 것 둘.**
  · 수관이 **납작한 덩어리**로 뭉쳤다 — 가지의 수평 성분이 커서였다. 가지를 세우고, 채움 잎을
    균일 높이가 아니라 **돔 곡면 위**(y = .64 − .9·rr²)에 얹었다.
  · 이끼 결을 납작한 구 14개로 얹었더니 돔과 교차해 **어두운 타원 구멍**처럼 보였다(교차선
    안쪽이 아랫면이라 빛을 못 받는다). 별도 메시를 버리고 **돔 자체의 정점을 밀었다**(`bumpify`) —
    교차가 원리적으로 생기지 않고 메시 수도 안 늘어난다.

~~오행 5종~~ → **일간 10종.** 구 구현은 음양을 버려 갑목=을목, 경금=신금이었다. 실증:
데모의 구성원B(庚)·구성원F(辛)·구성원H(辛)가 같은 금 텍스처를 썼고, 같은 辛인 구성원F과 구성원H가 서로
달라 보인 건 오직 난수 탓이었다 — **차이가 있어야 할 곳(음양)엔 없고, 없어야 할 곳(같은
일간)엔 있었다.**

**색은 오행이, 형태·질감은 음양이 정한다.** hue와 채도는 오행별로 고정하고(`SURF_EL`) 일간은
명도만 ±10~12p 움직인다 — 채도까지 건드리면 원거리에서 다른 오행으로 오인될 수 있다.
명도 **방향은 은유가 정한다**: "양이 무조건 밝다"로 잡으면 甲(짙은 수피)이 乙(연한 새순)보다
밝아져 `STEM_BLURB`와 어긋난다.

**방식**: `표면` = 텍스처만 / `구슬 안` = 겉면을 반투명하게 열고 그 안에 3D
(`hasInner`가 참, `applyShell`이 겉면을 연다) / `표면 밖` = 겉면은 불투명한 채로 두고
3D가 바깥으로 솟는다. 셋 다 `makeShapes(stemIdx)`가 만든다.

| | 천간 | STEM_BLURB 근거 | 방식 | 외관 | 상태 |
|---|---|---|---|---|---|
| 목 | **甲** 양 | 큰 나무 · 곧고 원칙적 · 부러질 때까지 | 구슬 안 | 유리구슬 안 테라리움(나무·이끼·돌·풀) | 완료 |
| | **乙** 음 | 풀과 덩굴 · 유연 · 적응 | 표면 | 얽힌 덩굴, 경계 없는 계조 | 완료 |
| 화 | **丙** 양 | 태양 · 숨기는 게 없음 | 표면 | 어두운 알베도 + 발광맵이 밝기를 짐, 흑점·과립·백반 | 완료 |
| | **丁** 음 | 촛불·달빛 · 섬세 | 구슬 안 | 그을린 등피 안 촛불(초·촛농·심지·불꽃) | 완료 |
| 토 | **戊** 양 | 큰 산 · 묵직 | 구슬 안 | 유리구슬 안 알프스(눈 덮인 바위 봉우리·침엽수·안개) | 완료 |
| | **己** 음 | 논밭의 흙 · 포용 | 표면 | 경작지 격자(뙈기·고랑·두렁·물길) | 완료 |
| 금 | **庚** 양 | 무쇠·바위 · 결단 | 표면 | 주조 얼룩 + 단조 자국 + 가지 친 균열 | 완료 |
| | **辛** 음 | 보석·바늘 · 예리 | 구슬 안 | 모암 위 무채색 육각 결정 11개 | **재작업 예정**(§2.3, 3.png) |
| 수 | **壬** 양 | 큰물·바다 · 계속 흘러야 | 표면 | 심해 + 소용돌이 셋을 감는 해류 | 완료 |
| | **癸** 음 | 빗물·이슬 · 존재감이 과제 | 표면 밖 | 표면에 맺힌 이슬 26개(큰 것 4 + 작은 것 22) | 완료 |

**음양을 실제로 가르는 건 그림이 아니라 재질이다.** `bumpMap`이 없으면 무늬가 조명과
상호작용하지 않아 "구에 붙은 스티커"가 된다. 색 캔버스와 **같은 도형으로 높이 캔버스를 함께**
그린다(따로 만든 노이즈를 쓰면 요철과 무늬가 어긋난다). 양은 `bumpScale`을 크게, 음은 0에 가깝게.
`roughness`도 오행별로 갈랐다 — 금 .10~.42(매끄럽고 반사 강하게), 토 .90~.98(거칠고 무광).

**[2026-08-27] 멤버 궤도 반경 — 비율에서 절대값으로(6~18).**
그룹 장면에서 천체가 40~60px이 되게 하라는 지시로 시작했는데, 실측해 보니 **진짜 문제는 크기가
아니라 편차**였다. `fitGroup`이 최외곽 멤버 궤도로 카메라 거리를 정하는데 그 궤도가 `비율 × R_g`
이고 R_g가 45~110(2.44배)이라, 같은 사람이 그룹에 따라 화면에서 **4.5배** 다르게 보였다.

| | 적용 전(비율 0.16~0.50) | 적용 후(절대 9~18) |
|---|---|---|
| 그룹A(3인) | 19.0~22.0px | 36.9~42.5px |
| 하위그룹A(1인) | 30.4px | 40.5px |
| 그룹C(2인) | 44.1~46.2px | 47.3~50.8px |
| 그룹B(2인) | **10.2~10.5px** | 30.2~30.8px |
| 미분류(1인) | 29.8px | 55.2px |
| **편차** | **4.5배** | **1.83배** |

**은하 조망은 안 변한다** — `fitGalaxy`가 최원점이 아니라 항성 크기(30px)로 거리를 정하기 때문이다.
실측: 카메라 267.4 → 267.4, 항성 30.6 → 30.6px, 행성 5.4~7.0 → 5.3~7.4px. 달라지는 건 배치뿐
(성계가 더 뭉쳐 보인다 — 사용자 판단: "하나의 무리로 읽혀 오히려 낫다").

**함께 바꾼 값 셋** — 안 바꿨으면 조용히 깨진다:
1. `CENTER_RING_RATIO 0.09·R_g` → `CENTER_RING_R 4.2`(절대). 안 바꾸면 R_g 110에서 반지 9.9가
   가장 안쪽 멤버 6을 삼킨다. 4.2는 항성 3.6보다 크고 멤버 하한 6보다 작다.
2. `galaxyMaxD()`의 `2·R_g + R_g·MEMBER_R_MAX` → `2·R_g + MEMBER_R_ABS_MAX`. 안 바꾸면
   R_g 110에서 1980이 나와 `cam.R_MAX`(1100)의 근거가 통째로 틀어진다.
3. `memberRungRatio` → **`memberRungRadius`로 개명.** 반환값의 의미가 바뀌었는데 이름이 그대로면
   호출부가 `× R_g`를 단 채로 남아도 조용히 통과한다.

**바닥 6의 근거(이 밑으로 내리지 말 것 — 실제로 쓰는 값은 9)**: 그룹 궤도는 θ=0에서 원점을 지난다(성계가 나를 통과한다). 그 순간
성계 중심 = 항성 위치이므로 **항성 반경 3.6 + 행성 반경 0.9 = 4.5**보다 커야 관통하지 않는다.
여유 포함 6.

**대가 — 겹침**: 봉 사이 간격은 배율과 무관하게 화면에서 상수다(실측 28.9/28.1/27.7/27.6/28.6px).
카메라가 궤도에 맞춰 따라오기 때문이다. 반면 천체 지름은 1/최외곽궤도로 커진다.
**40~60px + 10칸 + 성계 전체 담기, 셋은 동시에 성립하지 않는다.** 3인 14% / 7인 57%로 측정했고
받아들였다 — 앞뒤로 지나가는 건 깊이 단서이고 레이캐스트가 앞의 것을 집으므로 클릭이 안 막힌다.
(경사를 줘 봤으나 71.2% → 65.3%(14°) → 66.2%(22°)로 해결책이 아니었다.)

**하한은 6이 아니라 9다 — "6"은 바닥이지 값이 아니었다.**
6으로 두니 관통은 안 하는데 **항성 뒤로 숨었다.** 항성 지름이 화면에서 126~152px인데 1칸 멤버의
궤도 반경이 그 절반 남짓이라, 위상이 맞으면 천체가 통째로 가려져 **이름표만 뜬다** — "내 성계를
본다"는 목적을 정면으로 해친다. 실측(3인, 누구든 한 명이라도 가려진 프레임 비율):

| 반경 | 항성 뒤로 숨음 | 멤버 개별 | 천체 지름 | 천체끼리 겹침 |
|---|---|---|---|---|
| 6~18 | 33% | 14~15% | 33/37/48px | 14% |
| **9~18 (채택)** | **26%** | **7~11%** | 31/35/46px | 14% |
| 12~20 | 17% | 7% | 27/31/41px | 12% |

크기 손해는 2px뿐이고 겹침은 그대로다. 대가는 칸 간격 1.33 → 1.0인데, 그건 앞뒤 깊이 단서로
읽히므로 감수했다(사용자 판단). 7인 최악 조건에서도 가림 52% → 44%로 준다.
**바닥 6의 근거는 여전히 유효하다**(항성 3.6 + 행성 0.9 = 4.5, 통과 순간 관통 방지) — 다만 그건
"이 밑으로 내리면 뚫린다"는 하한선이지 쓸 값이 아니다.

**[기록만] `PLANET_SCALE`을 건드리면 조용히 깨지는 값 둘.**
이번 작업에서는 안 건드렸다(은하 조망 거리가 `GALAXY_STAR_PX`로 고정돼 있어 `PLANET_SCALE`을
올리면 **은하 화면의 행성이 같이 커진다** — 6.4~8.9 → 13~18px). 나중에 누가 건드릴 때를 위해:
- `RING_RADIUS 1.28` — 관계 고리의 반경. 행성 반경 0.9의 1.42배로 정해진 값인데 **파생되어 있지
  않다.** `PLANET_SCALE`을 올리면 고리가 행성 **안으로** 들어간다.
- `STAR_SCALE 3.6` — 항성:행성 = 3.6/0.9 = **4:1**(§2.9 "4~8:1"의 하한). `PLANET_SCALE`만 올리면
  이 비가 무너진다.
반면 `COMET_TAIL_LENGTH`·`COMET_TAIL_WIDTH`·`VIRTUAL_MEMBER_SPREAD`·`VIRTUAL_SLOT_RING_*`은
이미 `PLANET_SCALE`에서 파생되므로 따라온다.

**[2026-08-27] 환경 반사 세기는 계열이 아니라 일간별로 정한다.** `STEM_SURF`에 `env`를 두면
`ENV_INTENSITY[kind]` 기본값을 덮어쓴다. 계기는 庚이 **무쇠가 아니라 파란 전기 구슬**로
렌더된 것이다 — 금속도 .80에 반사 4.5를 걸었는데 이 씬에서 반사할 대상이 배경 성운(푸른색)
뿐이라 구 전체가 파란 크롬이 됐다. **오행 색은 멀쩡했다**(금 h210 s6, 거의 무채색). 색이 아니라
반사가 파랗게 만든 것이다. 금속도를 올린 근거였던 "辛이 검게 렌더된다"는 실측은 辛(보석)의
사정이지 庚(무쇠)에는 안 맞았다. 같은 이유로 壬·癸도 내렸다.
현재 값 — 庚 2.0 / 辛 4.5(겉면은 GLASS_ENV 4.6) / 壬 2.0 / 癸 2.4 / 나머지는 계열 기본.

⚠ **`metalness` 상한은 0.40이다.** 처음 庚 .62 / 辛 .92를 줬더니 辛이 거의 검게 렌더됐다 —
이 씬에는 `envMap`이 없어 금속이 반사할 대상이 없기 때문이다(PBR상 정답). 금속감은 metalness가
아니라 **낮은 roughness가 만드는 좁고 밝은 하이라이트**로 낸다. envMap을 도입하면 이 상한을
다시 올릴 수 있다.

**캐시**: `stemIdx`로 캐시하고 난수도 시드를 고정한다 — 같은 일간은 새로고침해도 항상 같은
외관("외관 = 일간"). 개인 구분은 라벨과 위치가 담당한다.
**해상도** `SURFACE_TEX_W` 512 → **1024**. 실측(10종 생성 중앙값 / 텍스처 메모리):
512 = 57ms·10MB / 1024 = 72ms·40MB / 1536 = 62ms·90MB. 생성 시간은 캔버스 연산이 지배해
사실상 평탄하고 **실제 비용은 메모리**다. 육안(실사용 최대 확대의 2배 배율): 512는 균열
가장자리가 계단처럼 끊기고, 1024와 1536은 구분되지 않는다.
**라벨의 천간 표기는 유지**한다(`庚 · 금속의 행성`) — 금·토는 형태만으로 판별이 어렵다.

### 2.4 고리(띠) 의미체계 — **[2026-08-26 전면 재설계, 9852764]**

~~은하 전체 관계 기준 발신량~~ → **나와의 관계(§7.2 에고센트릭).**

**폐기 사유 (실측).** 구 규칙은 데모 8인에서 **8명 전원**이 고리를 달았고 총 22개였다. 우연이
아니라 구조적 결과다 — 오행 5종에 8명을 넣으면 28쌍 중 비화를 뺀 대부분이 생 아니면 극이고,
각 쌍마다 반드시 한 명이 발신자가 되므로 사람이 늘수록 1인당 발신 수도 함께 늘어 상한(2+2)에
포화한다. **모두가 달면 표식이 아니다.**

| 고리 | 조건 | 배치 | 색 |
|---|---|---|---|
| **1개** | 나를 생 (그 사람의 오행이 내 오행을 생) | 수평 | 금색 `#e8c66a` |
| **2개** | 나를 극 | 90° 교차 | 주홍 `#ff7a5a` |
| **3개** | 최상위 항성계 소속 ≥ `RING_GROUP_THRESHOLD`(2) | 120° 분산 | 은백 `#cfd6e4` |
| 없음 | 그 외 | | |

- **내가 주는 것은 표시하지 않는다.** 내가 생·극하는 사람에겐 고리가 없다 — 고리는 내가 받는
  것만 보여준다. 에고센트릭의 당연한 귀결이다.
- **항성(나)은 고리를 달지 않는다.** "나에게 무엇을 하는가"의 표식이라 나 자신에겐 정의되지 않는다.
- **우선순위 3 > 2 > 1.** 한 사람이 "나를 생"이면서 소속이 넓을 수 있다(데모의 구성원B). 더 강한
  표식이 이긴다. 가려진 관계 정보는 **인물 패널의 "나와의 관계" 줄(`.egorel`)이 반드시 말한다** —
  `.rel-list`는 `edges[]`에 등록된 관계만 보여줘 미분류 인물에겐 비어 있지만, 이 줄은
  `relation()`을 직접 계산하므로 누구에게나 항상 나온다.
- **임계값은 최상위 항성계만 세고 미분류는 제외한다.** 중첩 하위 그룹을 세면 안 되는 이유:
  "하위그룹A"는 "그룹A" 안에 **같은 사람들이** 중첩된 것이라, 그걸 세면 사회적 폭이
  넓어지지 않았는데 수만 올라간다. 지시의 문자("소속 그룹 수")보다 의도가 우선한다.
  데모에서는 두 방식이 같은 결과(구성원B 1명)를 내지만 실제 데이터에서 갈린다.
- **세 배치가 같은 반경(1.28)을 쓴다.** 구 구현은 동심원으로 쌓아 반경이 커질수록 "고리가
  행성보다 크다"는 문제가 있었는데, 의미를 **개수와 방향**이 지고 반경이 지지 않게 하니
  그 문제가 구조적으로 사라졌다.
- ~~**예외: 가상 성계의 복제본은 조합 내부 기준으로 재계산한다.**~~ → **소멸.** 그 예외의 근거는
  "이 조합만 놓고 보면 누가 발신하는가"였는데 새 규칙이 발신량을 아예 다루지 않는다.
  `rebuildVirtualRings()`를 삭제하고 `applyEgoRings()`로 규칙을 하나로 통일했다.

**천문학적 은유.** 고리는 그 행성의 **중력이 주변에 미친 흔적**이다. 1개는 흘러드는 물질,
2개 교차는 서로 다른 방향의 조석력, 3개는 **로슈 한계** — 중력이 너무 강해 다가온 위성을 부수고
그 파편이 고리가 되는 한계선이다. "주변에 사람이 많은 사람"이 3개를 다는 이유가 여기 있다.
그 사람의 중력권 안에 이미 여러 천체가 들어와 있는 것이다.

**실측(데모 8인).** 고리 총수 22 → **7개**(−68%), 착용자 8/8 → **4/8명**.
구성원B 3(소속 2계) · 구성원E 2(나를 극) · 구성원F 1 · 구성원H 1 · 구성원C/구성원D/구성원G/나 0.

### 2.5 궤도의 의미

궤도 원이 나를 통과하므로 밀착도는 거리가 아니라 **주기**로 표현된다.

- 나를 **생**하는 그룹 → 작은 원 → 자주 나를 지남 → 늘 가까이
- **비화** → 중간
- 나를 **극**하는 그룹 → 큰 원 → 드물게 옴

### 2.6 자전·공전 속도의 의미

- **공전 = 외부 관계 이동성**, **자전 = 내부 활동량/자기 집중도**
- 축은 반드시 **가치중립**으로 정의한다. "헛짓거리" 같은 부정 판정을 상시 애니메이션으로 표시하면 §7.3(부정 판정 완곡 표현)과 충돌하고, 공유 이미지에 그대로 노출된다
- 현재 근거: L1(일간 오행) 기질 매핑 — 화·목=빠른 공전, 토=느림, 수=빠른 자전, 금=느린 자전
- L3 도달 시 교체: 공전=식상·재성 비중, 자전=비겁·인성 비중
- ~~**속도 편차는 최속:최저 2:1 이내, 주기는 30~90초.**~~ → **[F-3, 2026-08-27 개정] 공전은
  전 성계 동일 주기 50초.** 편차가 0이므로 2:1 규칙은 자동으로 만족된다(구 구현은 `ω=BASE/R_g`라
  편차가 2.44로 **이미 이 규칙을 벗어나 있었다**).
  **[2026-08-27] 주기 50 → 30초.** "전체적으로 속도를 올려라"는 지시. §2.6이 정한 30~90초의
  하한이다. 동시 통과 비율은 주기와 무관하다(아래 표는 **위상차 구조**가 결정한다) — 바뀌는 건
  통과가 얼마나 자주 오느냐뿐이다.
  **행성의 로컬 공전은 별도 규칙**이다(GALAXY-LAYOUT-SPEC §3.4): 일간 기질로 1~10칸, 주기
  45~9초. 그동안 행성이 **로컬 공전을 아예 안 하고 있었다**는 것을 여기서 발견해 함께 고쳤다.
  근거: "여러 항성계가 동시에 중심 부근에 있는 프레임" 비율이 구 11.41% → 동일 주기 **3.00%**,
  깊은 겹침은 2.93% → **0%**. 직관과 달리 주기를 서로소로 벌리면 12.42%로 **더 나빠진다** —
  무리비는 위상차가 모든 값을 훑어 독립 확률의 곱(4성계 기준 12.2%)으로 수렴하기 때문이다.
  같은 주기는 위상차를 영구히 고정하고, 그 위상차를 벌리는 일은 §3.3 황금각이 이미 최적으로
  한다. 자세한 표와 유도는 GALAXY-LAYOUT-SPEC §3.3.
  **[자전은 별개]** 위 L1/L3 매핑은 자전에 대해서는 그대로 유효하다 — 바뀐 것은 공전뿐이다.
  주기 값 자체(50초가 적절한가)는 F-4(공전 속도)에서 다시 본다

### 2.7 통과 이펙트

접근 → 겹침 → 이탈 3단계. 접근 시 중심 표식이 서서히 진해지고, 겹치는 순간 **그 항성계 전체**(멤버·궤도선·고리 포함)가 짧게 정점(0.3초 내외)을 찍고, 이탈 시 감쇠.

**[F-2, 2026-08-27] 선택 중에는 이 효과를 끈다.** 선택한 성계는 중심이 원점에 서 있으므로
통과 위상 k가 1에 영구 고정된다 — "잠깐 스치는 순간"이라는 이펙트의 전제가 무너진다.
구현은 k를 **끌려오지 않은 궤도 위치**로 계산한 뒤 `(1-pull)`을 곱하는 것이다(GALAXY-LAYOUT-SPEC §3.5).

**[F-1, 2026-08-27]** 구 문장은 "분신이 서서히 밝아지고"였다. 분신이 폐기되면서 중심에서
k에 반응하는 것이 **반지의 불투명도 하나**로 줄었다(0.16 → 0.5). 크기·발광·글로우 세 축으로
반응하던 구 분신과 달리 **크기는 건드리지 않는다** — 자리 표식이 자라나면 "천체가 생겨난다"로
읽혀 §2.8이 방금 없앤 오해가 되살아난다. 항성계 전체(멤버·궤도선·고리)의 k 규칙은 불변이다.

- **색을 바꾸지 않는다. 밝기와 스케일만 건드린다** (§2.9 자주색 신비주의 금지)
  → [F-1] 중심 표식에 한해 **스케일도 건드리지 않는다**(위 사유). 멤버·고리는 종전대로
- 그룹이 여럿이면 통과가 잦아 화면이 번쩍인다 → 주기를 길게 잡아 간격을 벌릴 것

### 2.8 중심 표식 규칙 — **[F-1, 2026-08-27] 구 "분신(分身) 규칙"**

- 은하 중심의 **본체**만 실체: 꽉 찬 발광. **화면에 나는 하나뿐이다**
- 각 그룹 항성계의 궤도 중심에는 **아주 옅은 반지** 하나가 놓인다. 천체가 아니라 **자리**다
  - **왜 분신을 없앴나**: 구는 무엇을 그리든 "천체"로 읽힌다. §2.1이 "나는 은하의 유일한
    항성"이라고 정해놓고 화면엔 나를 닮은 반투명 구가 넷 떠 있었다 — 전제가 화면에서
    무너지고 있었다. 위치 계산(`orbitPoint`)은 한 줄도 바뀌지 않았다. **표현만 바뀐 것이고
    멤버는 지금처럼 그 빈 자리를 중심으로 돈다**(GALAXY-LAYOUT-SPEC §3.4)
  - **왜 반지인가 / 왜 궤도 평면에 눕히나**: 반지의 기울기가 곧 그 성계의 공전면이다.
    공간 재조정(C)에서 궤도 장축에 고도를 줘 평면을 갈라놨는데, 중심이 구라서 그 정보가
    화면에 전혀 안 보였다. 반지로 바꾸면서 처음으로 눈에 보인다(실측: 데모 4개 성계의
    평면 법선 고도 +44.7° / −42.3° / +79.2° / +55.6°, 반지 정렬 오차 0.0000°)
  - **어휘**: 색 `#4a5578`, 기본 불투명도 0.16 — **가상 성계 슬롯 표식(0.18)과 같은 급**이다.
    둘 다 "여긴 자리다"만 말하는 것이라 같은 어휘를 쓴다. 오행 색을 쓰지 않는 이유도 같다 —
    중심은 사람이 아니다
  - **크기**: 반경 = 궤도 반경(`R_g`) × 0.09, 두께는 반경의 8%. 고정값이면 거리를 넓힐 때
    점이 된다. 가장 안쪽 멤버 궤도가 `0.25·R_g`([F-3] 구 0.18)이므로 그 36%라 멤버와 겹치지 않는다.
    **[F-3 재확인 완료]** 거리 5배 후 실반경 4.05~9.9, 전체 맞추기에서 화면 지름 9.7~25.0px(캔버스 1116×1018 실측) —
    "자리"로 읽히는 최소 크기다. 더 줄일 여지는 없다
  - **클릭**: 반지는 가운데가 뚫려 있어 레이캐스트가 그냥 통과한다(실측 확인). 안 보이는
    원판을 자식으로 붙여 가운데도 잡히게 한다 — 가상 성계 슬롯에서 이미 밟았던 함정이다
- **~~선택 시 0.7 고정~~ 규칙은 함께 폐기됐다.** 그 규칙의 목적은 "궤도 반대편(k=0)에 있는
  분신도 선택하면 밝아 보이게"였는데(버그 4), **[F-2]에서 그룹 선택이 "선택된 성계가 멈추고
  중심이 원점으로 온다"는 장면 전환이 되면 선택된 성계는 항상 원점에 서 있게 되어 이 규칙이
  존재 이유를 잃는다.** 지금은 표식 불투명도가 k만 따른다
- 중심 표식이 **이름표**를 이어받는다. ~~이름은 본체와 동일, 부제는 그룹명("나" / "그룹A")~~
  → **[2026-08-24 개정] 주 텍스트 = 그룹명, 부제 = 인원수("그룹A" / "4인").**
  화면에 떠 있는 것의 정체는 "나"가 아니라 "그룹A이라는 항성계"이고, 본체 이름은
  원점의 항성이 이미 달고 있어 중복이었다 — 중심이 여럿일 때 같은 이름이 반복돼 어느 성계인지
  안 읽히던 문제도 함께 해소된다. ~~"그 그룹 안에서의 나"라는 의미는 분신의 **생김새**(본체와
  같은 오행 텍스처·글로우)가 이미 지고 있다~~ → [F-1] 그 생김새가 사라졌으므로 **이름표가
  성계를 식별하는 유일한 수단**이 됐다. 부제를 "중심축"이 아니라 인원수로 정한 근거는
  GALAXY-LAYOUT-SPEC §5.4 참고
- 가상 성계의 **복제본**: 혜성 꼬리. 꼬리는 은하 반대 방향으로 뻗음

### 2.9 디자인 톤 — v1과 동일

- 딥스페이스 배경 `#07070e` + 성운, 별밭 2겹(시차). **[2026-08-25]** 성운은 "그라디언트"도
  "블롭"도 아니다 — 방향 기반 3D 노이즈 필드(FBM + 도메인 워프 + ridged 감산)로 생성한다.
  극점 아티팩트와 얼룩 반복을 같이 해소한 결과다(GALAXY-LAYOUT-SPEC §7 목록 10번)
- **[2026-08-27] 별밭이 네모로 보이던 것 — 원인은 크기가 아니라 텍스처 부재.**
  `PointsMaterial`에 `map`이 없으면 GL이 점 스프라이트를 **사각형 그대로** 칠한다. 크기가
  1~2px일 때는 티가 안 났는데, [F-3]에서 점 크기를 5배로 올리자(0.28/0.5 → 1.4/2.5) 사각형이
  눈에 보이게 됐다. **크기를 되돌리면 별이 사라지므로 답이 아니다** — 점에 알파 텍스처를
  물리는 것이 정답이다. 64×64 방사형 그라디언트 한 장을 모든 별밭이 공유한다(makeGlow와 같은
  기법). 정지점을 넷 둔 이유: 선형 두 점(1→0)이면 원반 가장자리가 칼로 자른 듯 끊겨 다시
  도형으로 읽힌다 — 안쪽 35%는 거의 불투명, 바깥은 급격히 떨어뜨려야 "번지는 점"이 된다.
  `alphaTest`는 안 건다(그 문턱에서 또 가장자리가 생긴다). `AdditiveBlending`도 안 쓴다 —
  별이 1200개라 겹치는 자리가 흰 덩어리로 뭉친다.
- **[2026-08-27] 성운 불투명도 1.0 → 0.22.** 사용자 지적: "배경 성운 때문에 천체가 잘 안 보인다.
  절차적으로 그린 구름이 화면을 채우는데 천체와 밝기가 비슷해 구분을 방해한다."
  같은 화면(시작 거리 291, 캔버스 1116×1018)에서 **완전 제거 / 0.18 / 0.22 / 0.30 / 1.0**을
  촬영해 눈으로 비교했다 —
  - **1.0**: 구름 덩어리가 천체와 같은 밝기 대역에 있어 행성이 배경에 묻힌다
  - **0(제거)**: 천체는 또렷해지지만 배경이 평평해져 "우주"가 아니라 "검은 판"이 된다
  - **0.30**: 아직 구름이 "형태"로 읽힌다
  - **0.22 [채택]**: 구조가 있다는 것만 겨우 전해지고 천체와는 밝기가 확실히 갈린다
  텍스처 자체(§7-10 극점 수렴, 노이즈 파라미터)는 한 줄도 안 바꿨다 — 밝기만 낮춘 것이라
  되돌리려면 `V2_TUNABLES.NEBULA_OPACITY` 한 값이면 된다.
- eser 톤 유지: Pretendard + Space Mono, 다크 글래스 UI
- **텍스트 위계 (2026-08-24 재설계, 기준 18px)** — 구 상태는 8.5~26px **15단계**였고 0.5px
  차이가 5쌍 있었다. 그건 위계가 아니라 잡음이라 **6단계 토큰**으로 줄이고 전 요소를 배정했다.

  | 토큰 | px | 배정 |
  |---|---|---|
  | `--fs-xs` | 11 | Space Mono 메타 — eyebrow, tag, field label, .st, .cnt, .k, lockbadge, .sijin small |
  | `--fs-sm` | 13 | 보조 문구 — sheetnote, guardnote, glegend, legendrow, tip, mchip, 생일 줄 |
  | `--fs-md` | 15 | UI 컨트롤 — selchip, memrow, sijin, trow, rel-item, pbtn, danger-full, 태그 패널 |
  | `--fs-base` | **18** | 본문 — `#panel p`, 입력 필드, primary 버튼 |
  | `--fs-lg` | 22 | 제목 — `#panel h2`, `.sheet h3` |
  | `--fs-xl` | 26 | `h1` |

  **예외 3곳**은 토큰에 넣지 않는다 — `#fab`의 "+"(26px), `#zoomctl`의 "⌖"(19px, 2026-08-25
  이전엔 "+/−" 두 칸이었다),
  `.memrow .chk`의 "✓"(12px). 텍스트가 아니라 아이콘 글리프라 크기가 문장 위계가 아니라
  자기 컨트롤의 박스 치수(56/44/18px)에 매여 있다.

  **높이 영향 실측(모바일 430×860)**: 본문이 13.5→18px이 되면서 그룹 패널 콘텐츠가 692px가
  됐는데 `#panel.tall`(82vh=705px)에 그대로 들어간다 — **82vh는 유지**.
  ~~태그 패널은 캡슐 행이 35px가 되어 26vh(224px)에 6.4행뿐이라 패딩·gap·line-height를 줄여
  32px/7행으로 회복했다~~ → **무효(2026-08-25, f2b3706).** 이 계산은 "항성계마다 한 줄"이라는
  전제 위에 있었는데, 그 줄바꿈 자체가 `.tb-sys{display:block}` 때문이었지 넘침이 아니었다.
  세로 스크롤을 **가로 스크롤**로 바꿔 높이를 1행으로 고정했다 — 실측 196px(20.9vh) → 52px(5.6vh).
  항성계가 몇 개든 높이가 변하지 않으므로 행 수 계산 자체가 불필요해졌다. 원래 의도("26vh를
  키우면 3D 은하를 뺏는다 — 태그 패널은 색인이지 주 화면이 아니다")는 그대로 살아 있고,
  오히려 15.3vh를 은하에 되돌려줬다
- **"사주 앱" 특유의 자주색 신비주의 금지.** 데이터 프로덕트처럼 보일 것
- ~~**밀도 기준 (v2 정밀화, 픽셀 휘도 3구간 목표치)**~~ → **[2026-08-27] 목표치 폐기.**
  이 표(암흑 83 / 중간톤 17 / 밝음 0.2)는 **화면의 중간톤을 성운이 만든다는 전제** 위에 있었다.
  C에서 실측으로 확인했듯 중간톤의 95.7%가 성운이고 은하 전체(천체·궤도선·고리·글로우)는
  0.77%뿐이었다. 그 성운을 0.22로 낮추기로 한 이상 목표치 자체가 근거를 잃는다.

  **실측(시작 거리 291, 8시야, 캔버스 1116×1018, Rec.709 / L<25, 25~140, ≥140)**

  | 성운 불투명도 | 암흑 | 중간톤 | 밝음 |
  |---|---|---|---|
  | 1.0 (구) | 83.514 | 16.356 | 0.130 |
  | **0.22 (현재)** | **99.085** | **0.790** | **0.125** |
  | 0 (제거) | 99.166 | 0.710 | 0.124 |

  **이 지표는 이제 성운을 볼 수 없다.** 0.22와 완전 제거의 차이가 중간톤 0.08%p뿐인데
  화면으로는 확실히 구분된다 — 성운 픽셀의 휘도가 하한(L=25) 아래로 내려가 "암흑" 버킷에
  통째로 들어갔기 때문이다. 즉 25/140 3구간 분류가 이 화면에서는 해상력을 잃었다.
  사용자 지시대로 **숫자를 목표로 삼지 않는다** — 판단은 스크린샷으로 한다.
  다시 수치 기준이 필요해지면 임계값부터 새로 잡을 것(예: 성운 대역을 보려면 L 5~25 구간).
  아래는 폐기된 목표치와 그 도달 기록이다.

- **~~밀도 기준 (v2 정밀화, 픽셀 휘도 3구간 목표치)~~ [기록]**

  | 구간 | 목표 | 비고 |
  |---|---|---|
  | 암흑 | 83% | 순수 배경. 이전 "93%가 거의 검정"보다 완화 — 검게 채우는 게 목적이 아니다 |
  | 중간톤 | 17% | **"성기다"는 인상의 실제 원인.** 빈 배경이 아니라 이 구간의 부재. 행성 표면 음영·성운·궤도선 등 은은하게 빛나는 면적이 여기 해당 |
  | 밝음 | 0.2% | 항성의 하이라이트·글로우 코어처럼 시선을 붙잡는 극소수 지점만 ([F-1] 구 "항성·분신의") |

  **[2026-08-25 실측 — 목표에 도달]** 8시야 평균, 카메라를 `camTick`과 같은 식으로 명시 고정하고
  WebGL 프레임버퍼를 직접 읽어 잰 값(Rec.709 휘도, 암흑 L<25 / 중간톤 25≤L<140 / 밝음 L≥140).

  | | 암흑 | 중간톤 | 밝음 |
  |---|---|---|---|
  | 목표 | 83% | 17% | 0.2% |
  | 구 블롭 성운 + 항성 1.6 | 55.81% | 44.12% | 0.063% |
  | 신규 성운 + 항성 3.6 | **82.13%** | **17.70%** | **0.181%** |

  구 성운은 "안 보인다"가 아니라 **너무 넓고 균질했다**(중간톤이 목표의 2.6배). 밝음이 목표의
  1/3이던 것은 항성이 과소했기 때문이고, §7-9에서 3.6으로 올리며 함께 맞았다.
  ⚠ **측정 시 주의**(자세한 규칙은 CLAUDE.md "3D 화면 측정 함정"):
  (1) 창이 백그라운드면 `visibilityState=hidden`으로 rAF가 완전히 멈춘다(실측 2.3초간
  0프레임). 그 상태에서 재면 카메라가 `fitAll`에 수렴하기 전 위치라 값이 전부 틀어진다 —
  실제로 이 함정에 한 번 빠졌다. 반드시 카메라를 명시적으로 세우고 잴 것.
  (2) **위 표의 절대치는 캔버스 크기에 의존한다.** 같은 텍스처·같은 카메라라도 캔버스 폭이
  2560→2180이면 중간톤이 14.87%→15.14%로 움직인다. 데스크톱 패널 개폐만으로도 폭이 달라지니,
  히스토그램을 비교할 때는 캔버스 크기를 함께 고정하고 기록할 것.

  **[2026-08-26 A 이후]** 조명(ambient .55:key .5 → .30:1.15)과 발광(멤버 .22 → .07,
  분신 .18 → .10)을 함께 바꿨는데 밀도는 사실상 불변이다 — 8시야 평균 85.0 / 14.9 / 0.12
  (A 이전 84.7 / 15.1 / 0.14). 채움광을 줄인 만큼 주광을 올려 총량을 맞췄기 때문.
  중간톤이 목표 17%에 약 2%p 못 미치는 건 A 이전부터의 격차이고 C(공간 재조정) 항목이다.
  텍스처가 실제로 보이는지는 밀도가 아니라 **고주파 대비**로 잰다(픽셀 − 5×5 국소평균의
  평균절대값). 전체 σ는 스펙큘러와 명암 그라디언트가 지배해 "알베도를 어둡게 하면 디테일이
  늘어도 값이 내려가는" 잘못된 지표다. 실측: 재설계 전 오행 5종 평균 **0.14** →
  후 양 평균 **4.89** / 음 평균 **0.76**(양:음 6.43배).

  **[2026-08-25 성운 해상도 인하]** `NEBULA_TEX_W` 640 → **384**. 텍스처 생성이 로드를 동기로
  막으므로 해상도가 곧 첫인상 지연이다. 실측(생성 중앙값 / 8시야 중간톤, 캔버스 2560×1279):
  640 → 254ms / 15.59%, 512 → 163ms / 15.30%, **384 → 95ms / 14.87%**, 320 → 67ms / 14.60%.
  육안으로 640·512·384는 구름 형상도 레인도 구분되지 않고, 320에서 처음 물러진다(구름 윤곽이
  뭉개지고 레인이 굵은 방울로 번짐). 나이퀴스트와도 일치한다 — 주 밀도 최고 옥타브가
  `NEBULA_FREQ 3.2 × 2^5` = 102.4 주기/대원이라 하한이 W≥205이고, 384는 그 1.87배(여유),
  320은 1.56배(얇음). 640 대비 생성 시간 63% 절감, 중간톤 손실 0.72%p.

  기존 실측(93%/2% 2구간)은 v0.7 스크린샷 기준이었고, 위 3구간은 레퍼런스 이미지 재실측 결과로 갱신. **항성:행성 = 4~8:1, 글로우는 항성(본체)과 丙(태양)에만** — [F-1] 구 문구 "항성(본체)과 분신에만" → 분신 폐기로 항성 하나가 됐다가, [F-5-4, 2026-08-27]에서 **丙 하나를 다시 넣었다.**

  **[F-5-4] 왜 丙만 예외인가.** 이 규칙의 취지는 "아무 데나 빛나면 위계가 흐려진다"이지 "항성만 빛나야 한다"가 아니다. 열 일간 중 **하나에만** 붙이는 건 그 취지를 해치지 않고, 오히려 "태양"이라는 은유를 형태로 드러낸다.
  **항성 대비 얼마인가 — 실측(그룹 장면 거리 108, 캔버스 1540×1039)**

  | | 본체 지름 | 발광 반경 | 발광/본체 | 중심 최고 휘도 |
  |---|---|---|---|---|
  | 항성(나) | 74.3px | **106px** | 1.43 | **227** |
  | 丙 | 18.6px | **11px** | 0.59 | **104** |

  발광 반경 절대값 **9.6배**, 발광/본체 비 **2.4배**, 최고 휘도 **2.2배** — 나란히 놓고 촬영해 위계가 유지되는 것을 확인했다. 값은 `CORONA_SCALE 3.0`(항성은 7.0), `CORONA_OPACITY 0.45`(항성은 1.0).
  원본이 丙이면 **가상 성계 클론도 코로나를 단다** — 같은 사람의 복제인데 한쪽만 안 타면 그게 더 이상하다.

  **[F-1 실측 — 분신 폐기의 밀도 영향]** 같은 캔버스(898×1271)·같은 8시야·위상 고정(`timePhase` 제외, t=0)으로 A/B.
  같은 빌드 안에서 분신 메시만 껐다 켜서 기여를 격리했다(글로우는 자식이라 함께 꺼진다).

  | | 암흑 | 중간톤 | 밝음 |
  |---|---|---|---|
  | 분신 있음(HEAD) | 83.327 | 16.441 | 0.232 |
  | 분신만 숨김(같은 빌드) | 83.661 | 16.100 | 0.239 |
  | **F-1 반지(현재)** | **83.636** | **16.116** | **0.248** |

  - 분신 4개 + 글로우 4개가 만들던 중간톤은 **0.34%p**(전체의 2.1%)였다. 반지가 그중 0.016%p를 되돌려준다
  - 밝음은 오히려 +0.007%p **올랐다** — 반투명(0.35) 분신이 시야에 따라 항성·글로우 코어 위에 겹쳐
    밝은 픽셀을 알파 블렌딩으로 깎고 있었다는 뜻이다. 같은 빌드 격리 실험으로 확인했다(추정 아님)
  - 결론: 밀도 목표(83/17/0.2)에 대해 **중간톤이 0.34%p 멀어졌다.** 다만 §5.6이 확정한 대로 중간톤은
    성운 단독 작업(`NEBULA_FLOOR`)의 몫이고, 이 정도 폭은 그 조정 하나로 흡수된다 구현 시 정확한 임계값·측정 방법은 GALAXY-LAYOUT-SPEC.md 작업 기록(커밋 메시지) 참고 — 문서마다 측정 도구가 달라 절대치가 정확히 일치하지는 않는다

---

## 3. 현재 구현 상태 (v0.7)

단일 HTML. Three.js r128 (CDN), 바닐라 JS, 외부 의존성 없음.
**저장 있음** — 브라우저 저장소(★★ 5단계, 2026-08-28) + 파일 내보내기·불러오기(6단계, 같은 날).
(2026-08-28 이전 이 줄은 "저장 없음"이었다. 5단계 작업이 이 줄을 안 따라와 낡아 있었다.)

**동작하는 것**
- 일간 실계산 (`dayStem(y,m,d)` — 검증: 1990-01-01→丙, 1990-01-02→丁. 60일 주기라 두 날만 봐도 어긋나면 드러난다)
- 오행 상생/상극/비화 판정 및 엣지 생성
- 그룹 생성(2인 이상, 유형 7종), 그룹별 항성 판정, 궤도 공전
- **[2026-08-24] 그룹 수정·삭제** — 그룹 패널의 "그룹 수정 · 삭제"가 `openGrpSheet(null, g)`로
  같은 시트를 편집 모드로 연다(`openAddSheet(node)`와 같은 패턴). 이름·유형·상위 그룹·멤버를
  한 자리에서 바꾸고 삭제까지 한다. 그전까지는 **생성만 가능**했고 삭제는 "유효 멤버 2인 미만
  자동 삭제"에 우연히 걸리는 것이 유일한 경로, 이름 변경은 아예 불가능했다.
  삭제 확인창은 `removeGroupAt`의 재부모화를 미리 알린다("하위 그룹 N개는 상위로 올라갑니다") —
  모르면 "가족을 지웠는데 부부가 최상위로 튀어나왔다"로 보인다. 미분류는 `groups[]`에 없는
  파생값이라 편집 진입점을 주지 않는다
- **[2026-08-24] 순환 `parentId` 방어 3중** — 계층이 `parentId` 하나로만 표현돼 A→B→A 고리가
  생기면 재귀 세 곳(`effectiveMembers`/`renderSystemHTML`/`renderTree`의 walk)이 전부 무한루프에
  빠진다. (1) 상위 그룹 셀렉트에서 자기 자신과 모든 자손 제외(`descendantIds`) (2) `renderNav`의
  위생 검사가 조상 사슬을 훑어 고리를 끊음 (3) 재귀 자체에 `MAX_GROUP_DEPTH=12` 상한.
  (1)은 예방, (2)는 데이터 정정, (3)은 최후 방어 — 셋 다 실측 확인
- 그룹 관계 사주: 오행 밸런스 바, 순환형/긴장형/혼합형, 긴장 피격자, 결핍 오행
- 인물 CRUD, 그룹 다중 소속/이동, 실시간 갱신
- 입력: 년/월/일 셀렉트, **시각 12칸 그리드**, 도시 셀렉트
  — **[2026-08-24 개편]** 12지시 버튼이 "지지 큰 글자 + 시간대 8.5px"였는데, 사용자는 자기가
  몇 시에 태어났는지로 기억하지 자시·축시로 기억하지 않는다(8.5px는 이 파일에서 가장 작은
  값이라 실제로 안 읽혔다). **시간 표기를 주(13px/600), 지지를 보조(9.5px)로 뒤집었다.**
  표시 순서도 01시부터 이어지도록 자시(23–01)를 맨 뒤로 보냈다 — 구 순서는 첫 칸이 23시라
  시간순 스캔이 끊겼다. `n.birth.sijin` 인덱스는 그대로 두고 `SIJIN_ORDER`로 화면 순서만
  매핑(마이그레이션 회피). "시간 모름"은 시각이 아니므로 12칸 밖 점선 버튼으로 분리
- **[보류] 시:분 직접 입력**(→ 지지 자동 표시)은 **L2 착수와 함께** 한다(§5.6 "입력 문제 4건"의
  후보 (가). 그 방향으로 정해지면 지금의 12칸 격자가 이 입력으로 교체된다). 지금 엔진(L1)은
  시주를 쓰지 않아, 분 단위를 받아놓고 결과가 안 바뀌면 "입력이 무시된다"는 인상을 준다.
  엔진이 분을 실제로 쓰기 시작할 때가 맞는 시점(진태양시 보정, §6 L2)
- 줌 컨트롤, `fitGalaxy()` 자동 프레이밍 ([2026-08-27] 구 `fitAll()`)

**v0.7에서 추가/수정된 것**
- 궤도 평면 고정, 결정적 배치(`layoutGalaxy`), 포스 시뮬 제거
- 카메라 phi 클램프 `.42~.82`(고도 50~66°), 감도 절반 조정(`.006`→`.003`)
  — **둘 다 이후 개정됨**: 감도는 화면 비례 정규화(7cf9b69), 클램프는 `.25~1.25`로 완화
  (2026-08-24, GALAXY-LAYOUT-SPEC §6 phi 행)
- 데스크톱 좌측 패널 + X 버튼 + 재열기 버튼
- 그룹 계층(`parentId`), 접히는 트리, `effectiveMembers()`
- 라벨 반경 방향 배치(자전 역회전 상쇄), 고리 결정적 정렬

**v0.7에 남은 결함 (v0.8에서 처리)** — *상태는 2026-08-24 코드 실사 기준*
- ~~`camera.setViewOffset`이 줌 보정 없이 시프트만 함 → 데스크톱에서 우측 190px 잘림~~ → **처리됨(2026-08-24, 수정 1)**: 캔버스를 `left:380px`로 축소하고 `resize()`가 그 폭으로 드로잉 버퍼·aspect를 다시 잡는다. 레이캐스트 좌표 정규화도 `getBoundingClientRect()` 기준으로 함께 교체. 상세는 GALAXY-LAYOUT-SPEC §6 "패널 보정" 행
- ~~`fitAll`의 `Math.max(26,…)` 하한~~ → **처리됨(7단계)**: `distanceForRadius()`로 화면 채움률 65%를 FOV에서 역산
- ~~`focusNode`가 위치를 1회 스냅샷~~ → **처리됨(7단계)**: `focusedNode`/`focusedMesh` 참조를 `camTick()`이 매 프레임 추종
- ~~핀치 줌 시 `dragging`이 살아 있어 회전 동반~~ → **처리됨(7cf9b69, 수정 2)**
- ~~팬(이동) 수단 없음~~ → **처리됨(7cf9b69, 수정 3)**: 모바일 두 손가락, 데스크톱 우클릭 드래그
- **[대부분 처리, 2026-08-25]** 모바일 뷰 대응 — 구 개별 결함 2건(a7be8bc, 38da569)은 태그 패널
  개편으로 대상 요소가 사라져 무효가 됐다. 실측 개선: 태그 패널 196px(20.9vh) → 52px(5.6vh),
  그룹 시트 801px(86.0vh, `max-height` 상한에 걸려 "그룹 생성" 버튼이 잘림) → 715px(76.7vh,
  상한 미접촉), 멤버 목록 8행 362px → 2열 4행 178px, `#fab`/`#zoomctl`의 태그 패널 겹침 해소,
  행성 화면 지름 3.0~3.6px → 10.8px(모바일 뷰포트 기준). 남은 것은 실기기 제스처 확인 —
  특히 줌 `+/−`를 `⌖` 하나로 줄인 뒤 핀치만으로 충분한지. GALAXY-LAYOUT-SPEC §7 목록 8번과 같은 항목
- ~~**[2026-08-24 확인, §5.2] 드래그 감도가 화면 크기에 비례하지 않는다.**~~ → **처리됨(7cf9b69, 수정 4)**: `sensPhi=(PHI_MAX-PHI_MIN)/(.6*innerHeight)`. 아래 설명은 당시 진단 기록으로 보존한다. GALAXY-LAYOUT-SPEC §6은
  "상하 감도: 화면 높이 60% 드래그 = 전체 범위 (픽셀 고정값 금지)"라고 못박아뒀는데, 실제 코드
  (`cam.phi-=dy*.003`, `cam.theta-=dx*.006`)는 `innerHeight`/`innerWidth`를 전혀 참조하지 않는
  순수 픽셀 계수다. 바로 위 줄의 "감도 정규화"는 사실이 아니었다 — 실제로 한 일은 계수를 `.006`→
  `.003`으로 절반 낮춘 것뿐, 화면 크기 비례 정규화는 애초에 구현된 적이 없다

**데모 데이터**: 없다. ~~나(壬水) / 구성원B(庚金) / 구성원C(丙火) / 구성원D(甲木)~~
★★ 1단계에서 `demo-data.js`를 지웠고(2026-08-27), 저장소에는 사람 데이터가 하나도 없다.
처음 열면 첫 화면(자기 등록)으로 시작한다. 필요하면 로컬에 `my-data.js`를 만든다(README).

---

## 4. 코드 구조 (v0.7 내부 섹션)

```
명리 엔진        STEMS, STEM_EL, EL_INFO, GEN/OVR, dayStem(), relation(), STEM_BLURB
데이터           nodes[], edges[], groups[], RICH_N/RICH_E
Three.js         renderer, scene, starfield()
카메라           cam{}, camTick(), updateViewOffset(), fitGalaxy(), fitGroup(), focusNode(), zoomBy()
천체 표면        stemSurface(), SURF_EL/STEM_SURF, sBlob()/sBand()/sVBand()/sStroke()/sSpeck()
스프라이트       makeLabel(), makeGlow(), ringMesh()
노드/엣지        addNode(), addEdge(), rebuildRings(), layoutGalaxy()
그룹 분석        effectiveMembers(), analyzeGroup(), createGroup(), removeGroupAt(), GTYPE_CLOSER
성계 모드        clearStellar(), orbitCircle(), enterStellar()
그룹 칩/트리     renderChips(), renderTree(), selectGroup(), showGroup()
시뮬레이션       simulate()
선택 & 패널      showNode(), showEdge(), deselect(), closePanel()
시트             openAddSheet(), updateNodeIdentity(), openGrpSheet()
루프             loop()
```

---

## 5. 다음 작업 (우선순위 순)

### 5.1 [최우선] 은하 구조 재구현 — `GALAXY-LAYOUT-SPEC.md` 참조

v0.7의 원형 평면 배치를 §2.1의 3구역 구조로 교체. 이게 가장 큰 작업이며 `layoutGalaxy`, `enterStellar`, `simulate`, `orbitCircle`이 전부 바뀐다.

### 5.2 v0.7 잔여 결함 (§3 목록) — **[개별 결함 전부 처리, 2026-08-24]**

~~`setViewOffset` 교체, `fitAll` 하한 제거, `focusNode` 추종, 핀치/회전 분리, 팬 추가,
감도 정규화~~ 는 모두 처리됐다(수정 1~4 완료).

**남은 것은 "모바일 대응"뿐이며, 그것도 개별 결함 2건은 처리된 상태다**(§3 참고).
남은 범위는 레이아웃 **전반 점검** — 데스크톱 기준으로만 정해진 치수·제스처·패널 동작을
실제 모바일 화면에서 훑는 작업으로, 아직 개별 항목으로 쪼개져 있지 않다.
GALAXY-LAYOUT-SPEC §7 목록 8번과 같은 항목이다.

### 5.3 하단 태그 패널 — **[구현 완료, 2026-08-24 · 2026-08-25 전면 개편]**

`[그룹A(하위그룹A<나, 구성원B>) 구성원C 구성원D]` 중첩 표기. **3D는 그룹 단위까지만 표시하고, 전체 이름은 이 패널이 담당한다.**

**[2026-08-25 개편, f2b3706]** 사용자가 실물을 보고 "우주 위에 떠 있지 않다"고 지적해 세 가지를 고쳤다.
- **색상 위계 반전 수정.** `cap-d${depth%2}`(짝/홀 교대: 어두움 ↔ 흰색)에는 위계 신호가 없었고
  결과가 반전이었다 — 최상위 4개가 전부 `#22242f`라 패널 배경과 사실상 같은 색이 돼 안 보이고,
  유일한 깊이 2("하위그룹A")만 `#e7e8ef`로 화면 최고 밝기를 가져갔다.
  `Math.min(depth,2)`로 **단조 3단**(불투명 블록 > 반투명 워시 > 배경 없음)으로 교체.
  깊이 1에 순수 흰색을 쓰지 않은 이유: 그게 바로 반전의 원인이었다.
  부수 효과로 흰 배경 전용 글자색 3줄(`#15161e`/`#8a6410`/`#4b4d59`)이 통째로 불필요해졌다.
- **바 → 부유 캡슐.** `background`(.92 불투명) + `backdrop-filter` + `border-top` 셋이 합쳐져
  별도 UI 바를 만들고 있었다(`backdrop-filter`는 배경이 불투명이라 애초에 무효였다). 전부 제거해
  캡슐만 우주 위에 뜬다.
- **세로 4줄 → 가로 1줄.** 4줄의 원인은 넘침이 아니라 `.tb-sys{display:block}`이었다.
  `inline-flex` + 가로 스크롤 + 우측 48px 페이드 마스크. 실측 196px(20.9vh) → 52px(5.6vh).
  세로 스크롤은 어차피 죽은 코드였다(`scrollHeight 195 == clientHeight 195`).
  가로 스크롤을 택한 근거: 데모 4계의 캡슐 폭 합이 약 700px라 430px 화면에서 한 줄로는 반드시
  넘치는데, 넘침을 세로로 받으면 항성계가 늘 때마다 은하를 계속 뺏기고 가로로 받으면 높이가 불변이다.
- `.tb-newgrp`("+ 그룹")는 제거하고 §6의 FAB 생성 메뉴로 옮겼다 — sticky bottom이 세로 스크롤
  컨테이너 전제라 가로 전환과 양립하지 않고, 애초에 "그룹 추가"가 좌하단 회색 텍스트,
  "사람 추가"가 우하단 원형 FAB로 갈라져 있던 것 자체가 문제였다

**[2026-08-26 개편, 6a2e8bb] 포함관계 — 색이 아니라 기하가 원인이었다.**
색상 위계는 §1(위 항목)에서 이미 고쳤는데도 "그룹A이 나머지를 싸고 있다"가 안 읽혔다.
실측해보니 원인이 색이 아니었다.

| | 진단(D 이전) | 조치 후 |
|---|---|---|
| 자식이 부모 안에서 갖는 세로 여백 | **2.9px** (자식 높이의 8.7%) | 6.0px (20.2%) |
| 그룹명 → 첫 내용물 | 8.0px | 21px |
| 내용물끼리 | 6.0px | 8px |
| 두 간격의 비 | **1.3배** (형제로 읽힘) | 2.6배 |
| 부모/자식 형태 | 둘 다 완전 알약(999px) — 곡률이 동심으로 겹침 | 부모 알약 / 자식 각진 사각 |
| 텍스트 대비비 | — | 6.68 |

- **가로 삽입량 74px은 여백이 아니었다.** 내역이 `패딩 10 + 그룹명 텍스트 58 + gap 6`이라
  **순수 여백은 10px뿐**이고 나머지는 글자가 밀어낸 것이다.
- **기여도 분리 실험**: 여백만 / 명도차만 / 둘 다를 같은 화면에서 비교했다.
  **명도차는 자식을 "형체"로 만들고 여백은 그것을 "안에 든 것"으로 만든다.** 여백 없이
  명도차만 올리면 자식이 부모 막대의 **칸막이**처럼 보인다(포함이 아니라 분할). 둘 다 필요하다.
- **명도차를 밝은 방향으로는 못 벌린다.** 이름 글자(`#a8abbb`)의 대비비가 α.45에서 **1.49**로
  판독 불가가 되고, 밝은 캡슐 전용 글자색을 되살려도 2.87로 실용 하한(3:1)에 못 미친다.
  α.22(명도차 47)가 상한인데 그마저 3.02로 아슬아슬하다.
  → **방향을 뒤집어 "부모를 밝히고 자식을 어둡게 판다"(움푹 들어간 주머니).** 대비비 6.68,
  §1에서 없앤 3색 분기 부활 불필요, 부모 밝음 > 자식 어두움이라 §1의 단조 위계도 유지된다.
- **[§1 결정 일부 수정]** §1의 "깊이2+ 배경 없음"은 위계 반전을 잡으려던 것이었고 그 목적은
  달성됐다. 그런데 배경을 가진 것이 깊이1 하나뿐이라 **그것만 고립되는** 부작용이 남았다
  (사용자 지적). 이제 모든 깊이가 배경을 갖되 점점 어두워진다.
- 그룹명 뒤 **세로 구분선**(`.tb-div`). 콜론 대신 세로선을 쓴 이유 — 한글 그룹명 뒤 콜론은
  타이포그래피상 어색하고, 세로선이 "왼쪽=이름 / 오른쪽=내용"을 공간적으로 나눈다.
  내용물이 있을 때만 넣는다(빈 그룹에서 선이 매달리면 그것 자체가 오류처럼 보인다).
- 맨 앞 **"은하"**(`.tb-galaxy`) → `selectGroup(null)`. 캡슐이 아닌 평평한 텍스트 + 뒤 구분선으로
  "전체 ⟩ 개별 성계"로 읽히게 했다. 모바일에는 트리가 없어 여기가 유일한 전체 복귀 경로다.
- **자식이 하나뿐인 경우에도 성립한다** — 레퍼런스는 자식이 둘이라 쉬웠지만 실제 데모는
  "그룹A 안에 하위그룹A 하나"다. 3배 확대와 실제 크기 양쪽에서 포함으로 읽히는 것을 확인.
- 대가: 행 높이 57px → 59.8px(모바일 5.6vh → 6.4vh). §2가 확보한 "한 줄" 원칙은 유지..

**[2026-08-24 정정]** 아래 두 줄은 구현 전 초안이었고 실제로는 다르게 갔다 — 설계·구현 과정은
GALAXY-LAYOUT-SPEC §5.4, 코드는 `index.html`의 `renderSystemHTML`/`#tagbar`
참고.
- ~~같은 표기 문법을 3D 라벨 LOD에도 재사용~~ → 3D 분신 라벨은 **문법을 그대로 재사용하지 않는다.**
  각 행성이 이미 상시 이름 라벨을 달고 있어(버그 2) LOD 뒷단계(개별 이름·이름+일간)가 중복이라는
  게 드러나 **2단계로 축소**했다 — 멀면 라벨 자체를 끔, 중간 거리면 "그룹명만", 가까우면 "그룹명 +
  하위 그룹명"(`AVATAR_LOD_FAR_RATIO`/`NEAR_RATIO`, `cam.R_MAX` 비율 임계값). 태그 패널 쪽 표기
  문법(대괄호·소괄호·꺾쇠)은 텍스트 패널 전용으로 남았다.
- ~~괄호 표기는 깊이 3에서 한계, 깊이 4부터는 들여쓰기나 색 농도로 전환~~ → **[2026-08-24 해결]**
  당시 결론은 "표기 자체의 재검토가 필요하다"였고(문자열 재귀는 깊이 제한 없이 동작하지만
  깊이 3 이상에서 직속 멤버의 소속이 괄호 짝을 세어야 읽혔다), 실제로 표기를 갈아엎었다 —
  **대괄호 문법 → 중첩 캡슐**. 소속이 문자열 규칙이 아니라 "어느 알약 안에 있는가"로 보이므로
  깊이가 늘어도 읽는 법이 같다. 3단 중첩으로 실물 확인(GALAXY-LAYOUT-SPEC §7 목록 7번 해소).
  **[2026-08-24 재정정]** 위 첫 줄의 "3D 라벨 LOD와 문법을 공유하지 않는다"는 여전히 유효하다 —
  이제 태그 패널은 캡슐, 3D 중심 표식 이름표는 "그룹명 / 인원수" 2단으로 완전히 다른 표현이다

### 5.3-1 입력 시트 — **[E, 2026-08-26 형식 통일·컴팩트화, c06935c]**

**[2026-08-27] 세 가지가 더 바뀌었다.**

**(0) 두 시트를 한 창에 탭으로 합쳤다.** `#addsheet` + `#grpsheet` → `#createsheet` 하나 안의
`사람 추가 | 그룹 만들기` 두 탭. **두 시트의 내부 마크업과 id는 그대로 옮겼다** — 기존 핸들러가
전부 id로 잡고 있어 껍데기만 바꾸는 것이 회귀 위험이 가장 낮다. 바뀐 건 여닫기 경로뿐이다.
- **탭은 생성 모드에서만 보인다.** 수정 모드(사람 수정·그룹 수정)에서는 탭 줄을 감추고 그
  탭의 제목(`#as-title`/`#gs-title`)만 남긴다 — 무언가를 고치는 중에 다른 탭으로 넘어가면
  입력이 그대로 날아간다.
- ~~탭 전환 자체는 입력을 지우지 않는다(두 탭의 DOM이 동시에 살아 있다). 초기화는
  `openAddSheet`/`openGrpSheet`가 열릴 때만 한다.~~
  **[2026-08-28 정정] 이 문장이 틀렸다.** DOM은 살아 있었지만 **탭 버튼이 `openAddSheet`·
  `openGrpSheet`를 불렀고**, 그 함수들이 곧 초기화였다. 즉 "초기화는 열릴 때만"이라는 서술과
  달리 **탭 전환이 곧 열기**였다. 아래 (4)에서 구조로 고쳤다.
- 탭을 바꾸면 `scrollTop`을 0으로 되돌린다 — 이전 탭의 스크롤 위치가 남으면 새 탭이 빈 화면처럼 보인다.
- `#relsheet`(관계 보기)는 성격이 달라 별도 모달로 남긴다.
- 실측: 데스크톱 540×435(사람) / 540×584(그룹), 모바일 386×840에서 345×482 — 모두 중심 오차 0,0.
  여섯 상태(신규 사람 / 탭→그룹 / 탭→사람 / 사람 수정 / 그룹 수정 / 신규 그룹) 전부 확인.



**(4) 탭을 걷어냈다가 셋으로 되살렸다 [2026-08-28].**

하루 사이에 두 번 바뀐 자리라 경위를 남긴다.

| | 무엇 | 왜 |
|---|---|---|
| 아침 `e17ccb1` | 탭 줄 **제거**, FAB가 세 화면을 각각 엶 | **탭을 오가면 입력이 날아갔다**(실측: 표에 "유지검사" 입력 → 그룹 탭 → 사람 탭 → 사라짐) |
| 저녁 (이 커밋) | 탭 줄 **되살림**, 세 탭(사람 추가 / 여러 명 추가 / 그룹 만들기) | 원인이 탭이 아니었다 — 아래 |

**원인은 탭이 아니라 함수 하나가 두 일을 겸한 것이었다.** `openAddSheet`·`openBulkSheet`·
`openGrpSheet` 셋이 **"표시"와 "초기화"를 한 몸으로** 하고 있었고, 탭 버튼이 그 함수를 불렀다.
그래서 탭을 없애는 것으로도 증상은 사라졌지만(오갈 일이 없어지니까) **원인은 그대로**였다.

고친 방식 — 셋을 `reset*` / `open*` 짝으로 가르고 **탭 줄은 `setCreateTab`만 부른다.**

```
openAddSheet(node)  = resetPersonForm(node) + setCreateTab('person')  + 열기
openBulkSheet()     = resetBulkTable()      + setCreateTab('bulk')    + 열기
openGrpSheet(p,e)   = resetGroupForm(p,e)   + setCreateTab('group')   + 열기
탭 버튼             = setCreateTab(which) 만
```

- **보관소를 따로 만들지 않았다.** 지켜야 할 상태가 이미 전부 DOM 값 아니면 전역
  (`ptModel` · `pickedMembers` · `pickedType` · `fGroups` · `fNewGroups` · `fSijin` · `fSex`)이라,
  **reset을 안 부르는 것만으로 남는다.**
- **지연 초기화**: 이번에 창을 연 뒤 한 번도 준비된 적 없는 탭이면 그때 한 번만 채운다
  (`tabReady`). FAB "사람 추가"로 열고 탭으로 그룹에 처음 가면 유형 칩이 아직 없기 때문.
- **창을 닫으면 `tabReady`를 되돌린다.** 입력 유지는 **창을 연 동안**의 약속이다 —
  닫았다 여니 옛 표가 나오면 그게 더 놀랍다.
- 탭을 보일 때 **목록만** 다시 그린다(`renderFGroups` / `ptRenderGroups` / `renderGMembers`).
  옆 탭에서 사람·그룹이 새로 생겼을 수 있는데, 셋 다 현재 선택을 읽어 그리므로 체크는 유지된다.
- ~~`ptShowMode`~~ 은퇴 — 표와 한 명 폼이 한 탭 안에서 번갈아 보이던 전환기다. 각자 탭이
  되면서 `setCreateTab`이 그 일과 시트 폭(`.wide`)을 가져갔다.

**`gsStash`/`gsRestore`도 은퇴했다.** 그룹 시트의 "＋ 새 사람"이 이름·유형·상위·체크한 멤버를
통째로 복사해 뒀다가 돌아와 되붙이던 코드인데, **탭이 상태를 보존하므로 복사할 것이 없다.**
남는 약속은 하나뿐이라 플래그 한 개로 줄었다:

- `gsWantMember` — "＋ 새 사람"으로 넘어갔다 돌아오면 **방금 만든 사람을 자동 체크**한다.
- **손으로 탭을 옮겨 추가한 경우에는 체크하지 않는다.** 그룹을 건드릴 뜻이 없던 사람이 조용히
  멤버가 되면 안 된다. 그 사람도 멤버 목록에는 나타난다(체크만 안 된 채로).
- 순서 주의 — `setCreateTab('group')`을 **먼저** 부르고 그 뒤에 `pickedMembers.add`. 반대로 하면
  아직 준비 전인 탭에서 지연 초기화가 돌아 방금 넣은 체크가 지워진다.

**표 화면의 "그룹 만들기" 링크 제거.** 탭 줄에 같은 버튼이 상시 있으므로 하는 일이 100% 같았다.
(그룹 탭의 "＋ 새 사람"은 **남긴다** — 그쪽은 "돌아오면 자동 체크"라는 약속을 더 지므로 탭으로
대체되지 않고, 사람이 없다는 걸 깨닫는 자리가 멤버 목록 안이라 손도 더 가깝다.)

**실측(헤드리스, `my-data.js` 없는 사본): 37건 통과.** 탭 3개 · FAB 세 항목이 같은 창을 엶 ·
세 탭 왕복 후 입력 유지(`유지검사` / `표유지` / `그룹유지`) · ＋새 사람 자동 체크됨 ·
손 전환은 체크 안 됨 · 표 링크 없음 · 양식이 표 위 · 접힘 없음(수정 모드 포함) ·
390px에서 탭 3개가 한 줄(101/101/101px) · 표 탭에서 `.wide`.

**(5) 입력 항목 — 받을 수 있는 건 지금 다 받는다 [2026-08-28 방침].**

L2(만세력 엔진)가 붙으면 **성별·출생시각·출생지가 전부 필요해진다.** 그때 이미 등록된
사람들의 빈칸을 뒤늦게 채우려면 **등록한 사람 수만큼 다시 물어봐야 한다** — 남의 생년월일은
한 번 묻기도 어렵고 두 번은 더 어렵다(사용자 판단). 지금 계산에 안 쓰여도 **저장은 되므로**
입력 화면에서 받을 수 있는 것은 다 받는다.

- **한 명 화면: `<details id="f-more">` 접힘을 없앴다.** 시각·도시가 상시 노출된다.
  2026-08-26에 접은 근거("지금 계산에 안 쓰이는 필드가 시트의 42.5%를 차지")는 **지금
  쓰이느냐만 봤다.** 위 방침이 그 판단 기준을 바꾼다.
  - **실제로 달라지는 곳은 수정 모드다.** 추가는 이미 펼쳐서 열고 있었다
    (`openAddSheet`의 `fm2.open=!node`). 그런데 **빈 시각·도시를 뒤늦게 채우는 화면이 바로
    수정 모드**라, 거기가 접혀 있으면 방침이 무의미하다.
  - `updMoreSummary`(접힘 헤더 값 요약)도 함께 제거 — 값이 화면에 그대로 있는데 위에 또
    적으면 같은 것이 두 곳에 뜬다.
- **필수는 이름·생년월일뿐이다.** 나머지 칸에는 `선택` 배지를 붙였다 — 배지가 없으면 필수라는
  뜻이다. 빈칸을 부담으로 만들지 않되, **아예 안 보이게 두지도 않는다.**
- 표는 여섯 칸 그대로다(이미 전부 받고 있었다).

**(6) 표 양식 안내 [2026-08-28].** 표 **위**에 열 순서와 예시 한 줄을 세운다.
전에는 "복사해 붙여넣을 수 있습니다"만 있어 **어떤 순서로 적어야 하는지 알 수 없었다.**

```
열 순서   이름 · 그룹 · 생년월일 · 성별 · 시각 · 도시
예시      김OO  가족  1990-01-01  남  3시  서울
필수      이름 · 생년월일 — 나머지는 비워 둬도 됩니다(성별·시각·도시는 만세력이 붙으면 쓰입니다)
```

- **열 이름을 글로 박지 않고 `PT_COLS`에서 뽑는다**(`ptRenderFormat`). 붙여넣기 파서가
  `PT_COLS[startCol+ci]`로 열을 정하므로 **같은 배열**을 읽어야 안내와 동작이 안 갈린다.
- 붙여넣기 문구(`#pt-paste`)와 **자리를 나눈다** — 그 자리는 붙여넣은 뒤 "5줄을 받았습니다"가
  덮어쓰는데, 양식이 필요한 순간은 붙여넣기 전후 모두다.
- `ptUpdateSummary`의 빈 상태 문구에서 "이름과 생년월일만 있으면…"을 뺐다 — 양식 안내와
  **같은 말이 한 화면에 둘** 있었다. 필수 항목은 한 곳에서만 말해야 고칠 때도 안 어긋난다.
- 예시 이름은 `김OO` 같은 중립 표현 — 파일을 여는 사람이 실제 인물로 오해하면 안 된다
  (CLAUDE.md 개인정보 규칙).

**(1) 바텀시트 → 화면 가운데 모달.** 사용자 지시. 바텀시트는 한 손 엄지 도달을 위한 형태인데
이 시트들의 내용(날짜 3칸·시각 12칸·멤버 목록)은 **읽고 고르는** 것이라 화면 아래에 붙을
이유가 없었다. 데스크톱에서는 세로로 긴 창의 맨 아래에 붙어 시선이 아래로 끌려 내려갔다.
폭 `min(560px, 100vw-28px)`, 높이 `min(86vh, 780px)`, 여닫기는 `translate(-50%,-50%)+scale`과
불투명도. 데스크톱 전용 좌표 보정(`left:380px`)은 폐기 — 폭이 이미 묶여 있어 필요 없다.
실측: 데스크톱 540×419(중심 오차 0,0) / 모바일 386×840에서 345×465(중심 오차 0,0, 세로 안 넘침).

**[함정 하나 — 실측으로 발견]** 바텀시트는 화면 밖으로 밀어내 닫았지만 가운데 모달은
제자리에 `opacity:0`으로 남는다. `visibility` 트랜지션 하나에만 맡기면 **트랜지션이 안 도는
상황에서 닫힌 모달이 영원히 클릭을 삼킨다** — 백그라운드 탭에서 `closeSheets()` 500ms 뒤에도
`opacity:1 / visibility:visible`이 그대로였고(탭이 숨겨지면 CSS 트랜지션이 멈춘다) 화면 정중앙의
`elementFromPoint`가 시트 내부 `.memrow`를 집었다. `prefers-reduced-motion`에서도 같은 일이 난다.
`pointer-events:none`을 함께 걸어 해결(트랜지션 대상이 아니라 클래스가 빠지는 즉시 적용된다).

**(2) 사람 추가 중 새 그룹 만들기.** 지금까지는 **이미 있는 그룹만** 고를 수 있었다 —
첫 사람을 넣는 순간에는 고를 그룹이 하나도 없다. 아래 §5.6의 "그룹 만들다 사람이 없으면
입력이 날아간다"와 정확히 대칭인 문제다.
그룹 칩 줄 끝에 `＋ 새 그룹` 칩을 두고, 누르면 **한 줄만 펼쳐진다**(이름 입력 + 유형 선택 +
만들기). 확정한 그룹은 금색 대기중 칩으로 붙고 ✕로 뺄 수 있다. **저장 버튼을 누르기 전에는
`groups[]`에 넣지 않는다** — 이름만 적고 시트를 닫으면 아무 일도 없어야 하기 때문.
저장 시 `[나, 이 사람]` 2인으로 만든다(§7.2 "나 없는 그룹 금지" + "2인 미만 제거" 둘 다 충족).
**나를 수정 중일 때는 칩을 감춘다** — 멤버가 나 하나뿐인 그룹은 저장 직후 사라진다.
시트 위에 시트를 겹치지 않은 이유는 §5.6의 반대편 항목과 같다(입력 상태 보존 구조가 커진다).

사람 추가와 그룹 만들기가 "형식도 높이도 다르다"는 지적. 대조해보니 차이의 일부는 필연이고
일부는 우연이었다.

**필연** — 사람은 생년월일이 있어야 일간이 나오고, 그룹은 유형·계층이 필요하다. 그리고
"그룹 선택"과 "멤버 선택"은 같은 관계(사람↔그룹)의 양면이지만 **선택 대상의 타입이 반대**라
(사람은 그룹 목록에서, 그룹은 사람 목록에서) 한 필드로 합칠 수 없다.

**우연** — `.chipsrow`와 `.typegrid`의 CSS가 완전히 동일했고(같은 컴포넌트가 이름만 둘),
관계 필드가 사람은 2번째·그룹은 4번째였고, 각주가 사람에만 있었다.

**하나의 시트로 합치지 않았다.** 공통 필드가 "이름" 하나뿐이라 그 다음부터 전부 분기하고,
유형 세그먼트를 넣으면 **두 경로 모두에 +48px**가 붙는데 탭 수 이득은 0이다(FAB 메뉴에서
이미 고른다). 드문 실수 하나를 위해 상시 비용을 지우는 거래다. 대신 **구조를 통일**했다:

```
제목 → ① 이름 → ② 관계 → 분류 → ③ 상세(접힘) → 저장
        사람: 그룹 칩 / 그룹: 멤버 선택
```

**컴팩트화의 근거는 코드에 있었다.** `dayStem(y, m, d)`는 **날짜만** 쓴다.
`birth.sijin`/`birth.city`의 유일한 사용처는 인물 패널의 표시 한 줄이고, 각주가 이미
"정밀 엔진 연결 시 사용"이라 밝히고 있었다 — **지금 계산에 안 쓰이는 필드가 시트의
42.5%(329/775px)를 차지**하고 있었다. 접어서 뺐다(각주도 그 안으로).

**접힘 헤더가 두 가지를 항상 보여준다.** 닫힘이 기본이라야 컴팩트화 효과가 있는데 그러면
처음 쓰는 사람이 안에 뭐가 있는지 모른다. 그래서 **(1) 필드 이름**("태어난 시각 · 도시")과
**(2) 현재 값 요약**("시간 모름 · 서울")을 함께 띄운다. 기본값일 때도 요약을 띄우는 이유 —
"비어 있다"가 아니라 "이렇게 정해져 있다"를 알아야 열지 말지 판단한다. 시트를 열 때마다
접힘은 닫힌 상태로 되돌린다.

**멤버 목록에 높이 상한(200px + 내부 스크롤).** 인원수에 비례해 무한히 자라던 것을 묶었다.
실측: 20인에서 내용 316px → 그리드 200px 고정, 시트 662px로 상한 미접촉.

| 모바일 430×932 | E 이전 | E 이후(닫힘) | E 이후(펼침) |
|---|---|---|---|
| 사람 시트 | 788px (84.5vh) | **436px (46.8vh)** | 790px (84.8vh) |
| 그룹 시트 | 715px (76.7vh) | **684px (73.4vh)** | 765px (82.1vh) |

**높이를 같게 만드는 것은 목표로 삼지 않았다.** 두 시트는 동시에 보이지 않아 직접 비교되지
않고, 억지로 맞추면 사람 시트에 빈 공간이 생긴다. 지적의 실체는 *형식*의 우연한 차이와
**둘 다 화면을 덮던 것**이었고, 둘 다 86vh 상한에서 벗어나면서 해소됐다.

⚠ **[보정 1건]** 처음 적용했을 때 사람 시트 **펼침**이 838px로 86vh 상한에 걸렸다 — 접힘
껍데기(요약 헤더·테두리·안쪽 여백)가 더해진 만큼이다. 안쪽을 조여 해소했다(필드 간격
14→9px, 시진 칸 패딩 9→7px, "태어난 시각" 라벨 제거 — 요약 헤더가 이미 말한다).
**접힘을 도입할 때는 닫힘만이 아니라 펼침 높이도 재야 한다.**

### 5.4 관계 보기 (구 "궁합 보기") — **[구현 완료, 2026-08-24]**

- 행성 클릭 → 인물 패널 → `관계 보기` → 상대 N명 선택
- **[2026-08-24 정정]** 아래 두 줄은 검증 없이 잘못 적었던 내용이다. `edges[]`를 실제로 확인해보니
  데모에서 가족 4명(나·구성원B·구성원C·구성원D)끼리만 생성 시점에 명시적으로 `addEdge`돼 있고, 이후
  "사람 추가" UI로 새로 넣은 사람만 기존 전원과 자동 연결된다(`addbtn` 핸들러). 구성원H·구성원G와
  구성원E·구성원F은 서로도, 가족과도 `edges[]`에 없다(당시 표기 "그룹C(구성원G·구성원H)/미분류(구성원E·구성원F)"는
  이후 데모 구성이 바뀌어 갱신 — 구성원E은 "그룹B"으로, 나는 "그룹C"로 옮겨졌고 미분류엔 구성원F만
  남았다. `edges[]` 사실관계 자체는 그대로다) — `관계 보기`에서 아무 두 명이나 고르면
  `edges.find(...)`가 `undefined`를 반환하는 경우가 흔할 것이다. ~~2명이면 1:1 해석(`showEdge`
  재사용)~~ → **2명이면 `relation(a.el, b.el)`을 그 자리에서 즉석 계산**한다(`analyzeGroup`이 이미
  쓰는 패턴과 동일) — 저장된 edge 객체 존재 여부와 무관하게 항상 결과가 나와야 하기 때문.
  **[구현 시 재확인]** "showEdge의 템플릿을 살짝 손볼 필요는 있다"고 적었던 것도 과했다 —
  `showEdge(e)`는 `e.a`/`e.b`/`e.kind`/`e.label`/`e.rich` 다섯 필드만 읽고 `edges[]` 소속 여부는
  전혀 확인하지 않아서, 즉석 계산한 `{a, b, kind, label, rich}` 객체를 그대로 넘기면 **`showEdge`를
  한 글자도 안 고쳐도 됐다.**
- 3명 이상이면 `analyzeGroup` N명 분석 — 새 함수 `showRelationView(members)`. `showGroup`과 본문
  문단(중심축·쏠림·순환형/긴장형·결핍 오행)은 `analysisInsightBody(A)`로 뽑아 공유하되, 유형 태그·
  `GTYPE_CLOSER` 인사이트 박스는 뺐다(즉석 선택엔 유형이 없음 — 미분류에서 겪은 "undefined" 노출과
  같은 함정을 피함)
- `analyzeGroup`은 **노드 배열이 아니라 `{id, members}` 형태의 그룹 모양 wrapper**를 받는다
  (`analyzeGroup(g)`가 내부에서 `effectiveMembers(g)`를 호출 — `g.id`/`g.members`가 필요).
  임시 선택 집합을 감쌀 때 `{id:'임시고유값', members:[...]}` 정도의 최소 wrapper만 있으면
  되므로 엔진 자체를 바꿀 필요는 없다는 결론은 그대로 유효하다 — "노드 배열만 받는다"는
  표현이 부정확했을 뿐.
- 제3자 조합(나 없는 쌍) **전면 허용**. Phase 1~2는 모든 데이터가 내 계정 안에만 있으므로 §7.3의 1인칭 규칙이 적용될 상황이 아님
- 제3자 쌍은 관계 성질 서술까지만. 행동 제안("먼저 다가가세요")은 대상이 내가 아니라 어색하므로 제외
- 이름을 "궁합"으로 하면 L4(합·충·형·파·해) 수준을 기대하게 되나 결과는 L1 → **"관계 보기"**로 명명. §8의 유료 전환 지점이 될 자리
- 관계선 클릭은 유지하되 버튼을 주 경로로

**구현 메모 (2026-08-24)**
- 대상 선택 UI: 새 시트 `#relsheet` — 기존 `g-members`의 `.memrow`(색 점+이름+일간·오행+체크마크)를
  그대로 재사용. 검색 UI는 만들지 않음(기존 다중선택 시트들도 지금 규모에서 검색 없이 충분 —
  20명대 후반~30명 넘는 실사용 데이터가 나오면 재검토)
- 미분류·다중소속 인물은 목록에서 특별 취급 없음 — `relation()`/`analyzeGroup()`이 그룹 소속과
  무관하게 오행만 보므로 배지·구분 불필요
- **"그룹으로 저장"**: 2명(showEdge 경로)·3명 이상(`showRelationView`) 결과 모두에 붙인다.
  `showEdge` 자체는 안 건드리고, 호출부(`relbtn` 핸들러)에서 결과 위에 버튼을 별도로 덧붙이는
  방식이라 `showNode`의 기존 관계 목록(`rel-item`) 클릭 같은 다른 `showEdge` 호출부는 영향 없음.
  `openGrpSheet(presetIds)`가 선택 인원을 미리 체크된 채로 열어준다(기존 "+ 새 그룹" 호출부는
  인자 없이 호출해 빈 상태 그대로)
- **"가상 성계에 담기"는 아직 안 만듦** — §5.5가 아직 없어서 가리킬 데가 없다. `showRelationView`의
  버튼 줄(`.panelbtns`)에 "그룹으로 저장" 하나만 넣어뒀고, §5.5 완료 후 그 옆에 추가하면 되는
  자리만 비워둠
- **[2026-08-24 수정]** "저장되지 않는 임시 조회" 안내(`.tip`)는 처음엔 `showRelationView`(3명
  이상)에만 넣었으나, 2명(showEdge) 경로도 똑같이 임시 조회이고 "그룹으로 저장" 버튼까지 있어
  안내가 없으면 더 헷갈린다는 지적으로 양쪽 다 넣었다. `showEdge` 자체는 여전히 안 건드림 —
  `relbtn` 핸들러가 버튼과 함께 이 안내도 같이 덧붙이는 방식이라, `showNode`의 기존 관계 목록
  (`rel-item`) 클릭(저장된 관계)에는 이 안내가 안 붙는다(실측 확인)

### 5.5 가상 성계 — **[구현 완료, 2026-08-24]**

- 슬롯 **1개**. 비우기 / 인물 빼기 / 더하기 / 전체 초기화
- **더하기·빼기 시 실시간 재분석** — 사람 하나 넣고 뺄 때 오행 밸런스 바가 움직이고 긴장형↔순환형이 바뀌는 걸 보는 것이 이 기능의 핵심 재미
- 복제본은 원본과 무관. 원본의 그룹 소속은 그대로 유지
- "은하로 가져오기" → 이름 입력 + 나 자동 합류 → 새 그룹 생성 (가상 성계는 그대로 남음)
- 슬롯을 늘리는 건 실시간 재분석으로 비교 욕구가 해소되는지 본 뒤 판단

**구현 메모** — 설계·상세 근거는 GALAXY-LAYOUT-SPEC §4 참고. 요약:
- 복제본은 참조 기반(`{ref, mesh, label, rings, tail}`, `virtualMembers[]`가 `nodes[]` 밖 별도
  배열) — §7.4가 정의한 "복제 노드"의 첫 실제 구현. §7.4의 3D 다중소속 복제가 이걸 일반화할 전제
- ~~고리는 `rebuildVirtualRings()`로 분리(조합 내부 기준)~~ → **무효(2026-08-26, B).** 예외의
  근거가 발신량이었는데 새 고리 규칙이 발신량을 다루지 않아 예외가 소멸했다. 클론도 일반 노드와
  같은 `applyEgoRings()`를 쓴다(§2.4)
- "은하로 가져오기"는 `openGrpSheet(presetIds)`를 그대로 재사용(§5.4 "그룹으로 저장"과 동일 메커니즘)
- 방위각은 180°(-X축)로 확정. "방위각으로 가시성을 조절한다"는 접근 자체를 폐기했다 —
  `cam.theta` 자유 회전 때문에 특정 각도가 "안 보이는 자리"가 될 수 없다(GALAXY-LAYOUT-SPEC §4·§7 목록 3번 참고)
- 새로고침 지속성은 "유지 안 함"으로, 라벨은 "상태를 담은 자동 이름"으로 각각 확정(§7 목록 4·5번)

### 5.7 해설 강화 — **[1단계 완료, 2026-08-25]**

조사 결과(코드 실사): 원안이 바뀐 게 아니라 **새 화면들이 원안을 물려받지 않았고**, 데모 인물이
4명 → 8명으로 느는 동안 `RICH_N`은 baseline 이후 한 번도 수정되지 않았다(`git log -S` 확인).
`showNode`/`showGroup`의 본문 조립부는 baseline과 사실상 동일하다. 즉 문구가 줄어든 게 아니라
**자동 문장이 차지하는 비중만 커졌다.**

- 수기 해설이 있는 화면: 인물 패널·관계 패널 **뿐**이고, 그것도 초기 가족 4명·6쌍에 한함
- 나중에 만든 세 화면(그룹·관계 보기·가상 성계)은 수기 0%, 자동 71~100%
- 28쌍 중 `edges[]`에 있는 건 6쌍뿐 — 나머지 22쌍은 폴백 문장 3개를 돌려 씀

**1단계 — 계산해놓고 버리던 것부터 (완료)**
- `rel2`(그룹 내부 전 쌍의 생/극/비화)는 **참조 0회**였다. 이제 "긴장선"·"기운이 도는 길"
  두 문장의 근거로 쓴다. 발신자별로 묶고 **사슬로 이어지면 이어 붙인다** — 오행의 생·극이 각각
  5원 순환이라 그룹 내부 관계가 사슬을 이루는 경우가 많다(데모 "그룹A"의 극이 정확히
  나→구성원C→구성원B→구성원D)
- 상한은 **발신자 3명 AND 쌍 4개**(먼저 걸리는 쪽). 발신자 수만 막으면 한 발신자가 여러 명에게
  발신할 때 쌍 수가 안 잡힌다(8인 실측에서 발신자 3명인데 5쌍). 초과분은 **클릭하면 펼쳐지는**
  "외 N쌍 (이름·이름)" — 텍스트로만 알리면 정보가 있다는 것만 알리고 접근은 막는 꼴이 된다
- `relation()`의 `from`을 관계 문장에 반영. "한쪽이 다른 쪽을 살리는 상생의 흐름" →
  "구성원H가 구성원G를 살립니다 (금 → 수)". `showEdge`가 방향을 **내부에서 다시 계산**하므로
  호출부가 필드를 기억할 필요가 없다
- `saengGive`/`geukGive`/`geukRecv`를 `analyzeGroup` 반환에 추가하고 동어반복을 근거로 교체 —
  "기운을 가장 많이 발산하는 사람입니다"(중심축의 정의를 되풀이) → "살리는 쪽 1회, 누르는 쪽
  1회로 가장 많이 발신합니다"
- **문체 원칙: 주어는 항상 사람, 오행은 괄호 근거.** "나의 물이 구성원C의 불을 누릅니다"처럼
  오행을 주어에 놓으면 술어 나열이 되고 §2.9 "데이터 프로덕트처럼"에서 멀어진다. 2단계 카피
  교체 때도 이 원칙을 유지한다
- 2인 그룹은 쌍 서술을 생략(관계 패널과 완전 중복)하되 **"두 사람의 관계 자세히 보기"** 링크를
  단다. 기존 경로는 "그룹 → 멤버 칩 → 인물 패널 → 관계 항목"인데 마지막 단계가 `edges[]`에
  그 쌍이 있을 때만 존재해, 사용자가 만든 2인 그룹은 막다른 길이었다(실측 확인)

**2단계 — 카피 확충 (완료, 2026-08-25)**
- 관계 폴백 3개 → **15개**. 25가 아니다 — 오행 상생·상극이 각각 5원 순환이라 두 오행 사이의
  관계는 하나로 정해지고 방향도 고정된다(수생목은 있어도 목생수는 없다). **생 5방향 + 극
  5방향 + 비화 5 = 15**를 코드로 확인 후 작성. `EL_PAIR` 테이블, `pairPhrase(form:'long')`에서만 쓴다
  (`short`는 쌍 조각을 콤마로 잇는 자리라 카피를 넣으면 문장이 화면을 넘긴다 — 1단계 원칙인
  "주어=사람, 오행=근거"는 양쪽 모두 그대로)
- 문체: 자연물의 그림에서 시작하되 **그 그림이 관계의 성질(무엇을 주고 무엇을 소모하는가)로
  이어지게** 썼다. 비유에서 끝나면 §2.9가 금지한 신비주의다. 생은 "주는 쪽이 무엇을 잃는지"를,
  극은 "어떤 조건에서 생산적이 되는지"를 반드시 포함한다(§7.3 완곡 표현)
- `RICH_E`의 톤을 참고하되 베끼지 않았다 — `RICH_E`는 특정 두 사람에 대한 서술이고 이건 오행 쌍
  일반이라 층위가 다르다. 개인 사정("2029년 재물 대운")은 여기 들어갈 수 없다
- 자리표시자에 조사를 실었다: `{A/이}`=이/가, `{A/은}`=은/는, `{A/을}`=을/를, `{A/과}`=과/와.
  카피에 조사를 하드코딩하면 이름 받침에 따라 "구성원F와", "구성원E가"가 나온다(실측으로 발견)
- `GTYPE_CLOSER`를 유형 7 × 역학 3 = **21문장**으로. 같은 문장에 형용사만 바꾸지 않고 역학마다
  다른 처방을 쓴다 — 순환형은 마찰 0인 상태의 위험(견제 부재), 긴장형은 긴장을 분업으로 바꾸는
  법, 혼합형은 축이 둘로 갈린 상태의 운용법. tip 헤더도 `가족 · 혼합형 INSIGHT`로 바꿔 어느
  조합의 조언인지 보이게 했다
- **검증: 데모 28쌍 전수 출력에서 중복 문장 0건, 21문장도 중복 0건.** 동일 오행 쌍인
  구성원F↔구성원H(둘 다 辛금)는 "구성원F과 구성원H는 같은 날을 세웁니다 — 기준이 같아 말이 잘 통하다가,
  한 번 어긋나면 서로 물러설 각도가 없습니다 (금·금)"로 나온다

**3단계 — 톤 분리 (완료, 2026-08-25)**
셋이 `analysisInsightBody` 하나를 그대로 쓰던 것을 `mode`로 갈랐다. 성격이 다르기 때문이다.

| 화면 | 성격 | 처리 |
|---|---|---|
| 그룹 패널 | 사용자가 **실제로 유지하는** 관계망 | 조언 유효 — 기존 톤 유지 + `GTYPE_CLOSER` 박스 |
| 관계 보기 | "이 사람들만 놓고 보면 어떤가" 즉석 질문 | **관찰까지만.** 실제 그룹이 아닌 조합에 행동 지침은 근거가 약하다 |
| 가상 성계 | "누가 들어오면 이 판이 풀리나" 실험 도구 | 관찰 + **직전 상태 대비 변화**를 맨 앞 줄에 |

- 용어도 가른다: **그룹 패널만 "성계"**다. 관계 보기·가상 성계는 궤도도 중심 표식도 없는 임시 묶음이라
  "조합"으로 부른다("이 그룹의 긴장선" ↔ "이 조합의 긴장선")
- 조언 절 제거 예: "긴장을 가장 많이 받는 자리는 나(받는 쪽 1회) — **이 사람의 컨디션이 성계의
  조기경보입니다**" → "긴장을 가장 많이 받는 자리는 나 — 받는 쪽 1회로 이 안에서 가장 많습니다"
- **가상 성계 델타**: §5.5가 "사람 하나 넣고 뺄 때 긴장형↔순환형이 바뀌는 걸 보는 것이 이 기능의
  핵심 재미"라고 못박아뒀는데 화면은 매번 현재 상태만 보여줬다. `vsPrev` 스냅샷과 비교해
  `구성원E을 넣자 — 순환형 → 혼합형, 극 0 → 1, 생 1 → 2, 중심축 구성원B → 구성원E, 없는 기운 3 → 2.`
  를 맨 앞에 낸다. `refreshVirtualSlot()`이 모든 변경(담기/빼기/비우기/일괄담기)의 단일 통로라
  렌더 끝에서 스냅샷을 갱신하면 누락이 없고, 누가 들고 났는지는 멤버 id 집합 차이로 구하므로
  조작 함수를 계측할 필요가 없다. 비우면 `vsPrev=null`로 되돌려 다음 담기를 "첫 담기"로 취급
- `GTYPE_CLOSER` 박스는 원래부터 `showGroup`에만 있었다(코드 확인) — 관계 보기·가상 성계는
  유형이 없어 쓰지 않으므로 그대로 뒀다
- **검증**: 같은 인원(나·구성원B·구성원E)을 세 화면으로 각각 열어 대조. 관계 보기·가상 성계 본문에서
  조언 표현(하세요/살피세요/확인하세요/조기경보) **0건**

**엔진을 올려야만 되는 것**: 개인 서술의 개인화(지금 개인에 대해 아는 건 일간 1글자뿐이라
같은 일간이면 같은 화면 — 데모의 구성원F·구성원H가 둘 다 辛금이라 문장이 한 글자도 안 다르다),
신강신약·십신(L3), 합·충·형·파·해(L4), 시기 서술(L5).

### 5.6 백로그

> **[2026-08-28] 이 제목 아래에 목록이 없다.** 실제 백로그 목록은 아래 ★★ 절 **뒤**에 떨어져
> 있다("localStorage 저장, 공유 이미지 내보내기…"로 시작하는 불릿). 문서를 고치다 절이 사이에
> 끼면서 제목과 내용이 갈라진 것이고, 옮기면 다른 문서의 "§5.6" 참조가 어긋나므로 지금은
> **길잡이만 둔다.** 항목을 더할 때는 그 목록에 넣을 것.
>
> 아래 **★ L2 입력 문제 4건**은 백로그가 아니라 **L2 착수 조건**이라 여기 붙어 있다.

## ★ L2(만세력 엔진) 착수 시 반드시 함께 다룰 것 — 입력 문제 4건 [2026-08-27 기록]

> **지금 되는가 — 결정만 지금 한다. 구현은 아니오.**
> **선행: 만세력(L2).** 문제 1·2·3은 **시각과 출생지를 계산에 쓰기 시작해야** 비로소 의미가
> 생긴다(지금 `dayStem(y,m,d)`은 날짜만 본다). 다만 **입력 UI는 지금 손대야 하는 경우가
> 있다** — 문제 2에서 (가) 분 단위 입력을 고르면 지금의 12칸 격자가 교체되고, 그건 데이터
> 형식이 바뀌는 일이라 **저장 형식 변환**이 함께 필요하다(CLAUDE.md "저장 항목을 바꿀 때는
> 변환을 함께 넣는다"). 문제 4(성별)는 입력 쪽이 이미 끝났다.

**넷은 서로 엽혀 있어 따로 풀 수 없다.** 앞의 셋은 시각, 넷째는 성별이다.
시각 셋은 **지금은 의미가 없다** — 현재 엔진(L1)의
`dayStem(y,m,d)`는 **날짜만** 쓰고 시각을 아예 안 본다. 시각이 계산에 들어가는 순간
동시에 결정해야 하는 문제들이다.

> **[2026-08-28] 문제 4(성별)는 입력 쪽이 해결됐다.** 이제 받아서 저장한다.
> L2가 할 일은 저장된 값을 **쓰는 것**과, 값이 비어 있는 사람을 다루는 것으로 줄었다.

### 문제 1 — 출생지에 따른 시간 보정(진태양시)

한국은 **동경 135°(일본 표준시)**를 쓰는데 국토가 그보다 서쪽에 있다. 실제 태양시와의 차이는
`(135 − 경도) × 4분`이다.

| 도시 | 경도 | 보정 |
|---|---|---|
| 서울 | 126.978 | 약 **32분** |
| 부산 | 129.075 | 약 **24분** |
| 강릉 | 128.876 | 약 24분 |
| 목포 | 126.392 | 약 34분 |

**준비는 끝나 있다** — [2026-08-27] 출생지 목록을 60개 도시로 늘리며 **경도를 함께 저장**하게
해 뒀다(`CITY_LON`, `birth.lon`). 지금은 표시만 하고 계산에 안 쓴다. 목록 밖 도시(직접 입력)와
"모름"은 `lon: null`이므로 L2는 그때 보정을 건너뛰면 된다.

**[2026-08-28] 도시를 모를 때는 큰 문제가 아니다.** 위 표에서 보듯 **한국 안에서는 진태양시
차이가 30분 안팎**이고(서울 32분 ~ 부산 24분, 폭이 10분 남짓), 모르면 **서울로 잡아도 오차가
작다.** 아래 문제 2·3의 시각과는 무게가 다르다 — **시각은 네 기둥 중 하나가 통째로 비지만,
도시는 이미 있는 값이 몇 분 어긋날 뿐**이다. 해외 출생은 이야기가 다르므로 그때 다시 본다.

### 문제 2 — 시간대 경계

새벽 3시에 태어났으면 **축시(01–03)인지 인시(03–05)인지 확정할 수 없다.**
보정을 해도 마찬가지다 — **보정 폭(24~34분)과 사람이 "3시쯤"이라고 기억하는 오차가 비슷한
크기**라서, 정밀하게 계산해도 경계가 그 오차 안에 들어온다.

**후보 방향 셋. L2 설계 때 고른다.**

**(가) 분 단위 입력.** 지금 입력은 **두 시간짜리 칸 12개**를 고르는 방식이라 3시 5분과 2시 55분이
아예 구분되지 않는다. 분을 받으면 정확히 아는 사람은 정확한 결과를 받는다.
→ **이 방향으로 가면 지금의 입력 UI(`SIJIN` 12칸 격자, `#f-sijin`)가 바뀐다.**
   시:분 입력 + 지지 자동 표시 형태가 된다(§5.3-1의 보류 항목과 같은 내용).

**(나) 두 결과를 나란히 보여주고 사용자가 고르게 한다.** "축시라면 이렇고, 인시라면 이렇습니다."
→ **가장 단순하다.** 질문을 만들 필요가 없고, 아래 (다)의 순환 문제도 구조적으로 안 생긴다.
   경계에 걸린 사람에게만 두 장을 보여주면 된다.

**(다) 질문으로 좁힌다.** 명리에서 실제로 쓰이는 "시주 추정" 방식. 쓴다면 아래 네 가지를
반드시 지킬 것 — **이 주의사항이 이 항목의 핵심이다.**
- **순환을 만들지 말 것.** 성격을 물어 시주를 정하고 그 시주로 성격을 알려주면 사용자는
  **자기가 방금 한 답을 되돌려받는다.** 결과가 아니라 메아리다.
- **성격을 묻지 말 것.** "적극적이신가요" 같은 질문은 **누구나 그렇다고 답한다.**
  직업 계열·형제 관계처럼 **사실로 답할 수 있는 것**이 낫다. 시주가 자녀·말년·직업과
  연결되므로 근거도 있다.
- **시스템이 판정하지 말 것.** 참고 정보로만 제시하고 최종 선택은 사용자가 한다.
- **질문 목록은 조사 후에 정한다.** 어떤 질문이 유효한지는 명리 지식이 필요하다 —
  실제 상담에서 쓰이는 방식을 조사한 뒤 정할 것. 지어내지 말 것.

**[2026-08-28 보강 — (다)를 고를 경우의 추가 조건]**
- **무엇을 물을지**: 형제 관계 · 직업 계열 · 부모와의 관계처럼 **사실로 답하는 것**.
  시주가 자녀·말년·직업과 연결되므로 근거가 있다.
- **어디까지 말할지**: 시스템은 **확정하지 않는다.** "가능성이 높다"까지만 하고,
  **두 결과를 나란히 보여주고 사용자가 고르게 하는 것**도 방법이다((나)와 섞을 수 있다).
- **정확도를 기대하지 말 것.** 역술인도 여러 번 대화하며 좁힌다. 한 번의 질문 세트로
  맞히는 기능이 아니라, **모름보다 나은 추정**을 주는 기능이다.

### 문제 3 — 시간 모름

네 기둥 중 **하나가 비는 경우**를 어떻게 다룰 것인가. **유파마다 다르므로 결정이 필요한
사항**이다(맞는 답이 하나 있는 문제가 아니다).
- 시주를 **빼고 세 기둥**으로 본다
- **정오로 가정**한다
- **"시주 없음"으로 명시**하고, 시주에 의존하는 해석은 아예 내보내지 않는다

지금 UI에는 이미 "시간 모름"(`sijin: 12`) 선택지가 있고 전체 입력의 기본값이다 —
즉 **이 경우가 예외가 아니라 다수**일 수 있다는 뜻이다. L2가 시주를 쓰기 시작하는 순간
"모름"인 사람들의 화면이 무엇을 보여줄지가 곧바로 문제가 된다.

**[2026-08-28] "모름"을 정직한 선택지로 남긴다.** 위 세 안 중 어느 쪽으로 가든,
**세 기둥만 보고 "시주는 모릅니다"라고 말하는 길을 없애지 않는다.** 추정으로 채운 시주를
확정처럼 보여주는 것보다, 비었다고 말하는 쪽이 정확하고 §2.9(데이터 프로덕트처럼)에 맞는다.

### ★ "결과가 안 맞으면 보정한다"는 성립하지 않는다 [2026-08-28]

시각 문제를 이야기할 때 나오기 쉬운 발상이라 여기 못박아 둔다.

**사주는 계산 결과다.** 생년월일시를 넣으면 여덟 글자가 결정되고, 그 사이에 조정할
손잡이가 없다. **고칠 수 있는 것은 입력값뿐이다.** "결과가 안 맞으니 결과를 손본다"는
계산을 버리는 것이고, §6의 철칙("계산과 해석을 분리한다")을 정면으로 어긴다.

고칠 여지가 실제로 있는 자리는 **시각의 경계 문제 하나**다(문제 2). 그리고 그건
**보정이 아니라 미확정값을 확정하는 것**이다 — 03:00 언저리에 태어난 사람의 시주가
축시인지 인시인지는 **처음부터 정해지지 않았던 값**이지, 계산이 틀린 게 아니다.
이 구분을 흐리면 "안 맞으면 고쳐 드립니다"가 되고, 그 순간 결과를 신뢰할 근거가 사라진다.

### 문제 4 — 성별 입력 칸 ✅ **해결 [2026-08-28]**

명리의 **대운(大運) 방향**은 성별과 연간(年干)의 음양으로 정해진다(양남음녀 순행, 음남양녀
역행). 즉 L2가 대운을 다루는 순간 **성별이 필수 입력**이 된다. 경도(`lon`)와 같은 처지라 —
나중에 사람들에게 다시 물을 수 없으므로 **입력받을 때 함께 받아야** 한다.

**[2026-08-28] 두 곳에 넣었다.** 첫 화면(`#ob-sex`)과 인물 시트(`#f-sex`)가 같은
`renderSex()`를 공유한다. 저장 위치는 `birth.sex`.

- **기본값을 두지 않는다.** 하나를 미리 켜 두면 안 보고 넘긴 사람의 값이 그리로 괳는다.
  처음 구현이 `null`을 "안 밝힘"과 공유해 **첫 화면을 열자마자 "안 밝힘"에 불이 들어있었다**
  (숫자가 아니라 스크린샷으로 발견했다). 지금은 미선택 = `undefined`, "안 밝힘" = `null`로
  화면에서만 갈라놓고, 저장할 때 둘 다 `null`로 모은다 — L2에게는 둘 다 "값 없음"이다
- **왜 묻는지를 칸 옆에 밝힌다**(`.fieldwhy`) — 이유 없이 성별을 물으면 거부감이 생긴다
- 인물 시트의 성별 칸은 **접힌 구역 안**에 있어, 접힌 헤더 요약(`updMoreSummary`)에
  "성별 미입력 / 안 밝힘 / 남 / 여"를 함께 찍는다 — 안 그러면 **칸이 있다는 것 자체를 모른다**

**남은 것 (L2 착수 시):** 성별이 비어 있는 기존 인물을 어떻게 다룰지.
일괄 안내나 배지는 **저장 기능이 들어온 뒤에 판단한다**(2026-08-28 결정).

---

## ★★ 남에게 테스트시킬 수 있는 상태로 [2026-08-27 결정]

**순서: 데모 데이터 분리 → 깃허브 업로드 → 배포 → 첫 화면 → 저장 기능 → 내보내기·불러오기 → 연속 입력.**
**[2026-08-28] 7단계까지 전부 끝났다** — 새로고침해도 남고, 파일로 빼서 다른 기기에서 되살릴 수 있고,
여러 명을 시트 한 번으로 넣는다. 남은 것은 L2(만세력 엔진)와 백로그의 C(붙여넣기 일괄 입력)다.
(첫 화면은 원래 계획에 없었다 — 배포해 놓고 보니 빈 우주만 뜼고 할 일을 알 수 없어 끼워 넣었다.
 6·7단계도 그렇다 — 5단계를 쓰다 보니 "지우면 다 날아간다"와 "열 명이면 열 번"이 드러났다.)

### 1단계 — 데모 데이터 분리 ✅ **완료 [2026-08-27]**

**한 일** — 사람·관계선·그룹 데이터를 HTML에서 떼어 세 갈래로 나눴다.

| 파일 | 내용 | 저장소 |
|---|---|---|
| `index.html` | 앱 코드. 데이터 **없음** | 올라감 |
| `my-data.js` | 실제 인물 | **`.gitignore`** |

HTML은 `my-data.js` 하나만 불러온다. **없으면 첫 화면(자기 등록)으로 시작한다**(4단계 참고. 그 전에는 빈 은하가 떴다).
[2026-08-28] 처음엔 가공 데이터를 담은 `demo-data.js`를 함께 올렸으나 **삭제했다** — 파일을 여는
사람에게는 "가공"이라는 표시가 안 보여 실제 데이터로 오해할 여지가 있었다. 데모가 필요해지면
1990-01-01 같은 명백한 가짜 값으로 그때 새로 만든다. `.gitignore`에는 레퍼런스 이미지(1~3.png)도 함께 넣었다 —
저작권이 불분명하고 각 1.6~2.6MB다. 무엇을 보고 만들었는지는 §2.3에 글로 남아 있다.

**함께 지운 것 — `RICH_N` / `RICH_E`.** 특정 가족의 사주 해석과 관계 해석이 하드코딩돼 있었다
(재물 대운 연도, 자녀 성격 평가, 훈육 분담까지). **생년월일보다 민감했다.** 예시로도 부적절해
자리표시 없이 폐기했고, 화면은 `STEM_BLURB`와 `analyzeGroup`이 만드는 계산 문장으로 떨어진다.

**가공 데이터는 일간이 같게 만들었다.** `dayStem`이 60일 주기라 같은 일간을 내는 다른 날짜가
많다. 열 명의 일간을 그대로 유지해 **오행 구성·관계·해설이 안 바뀐다** — 예시로 보면서 검증할 수
있다. 시드에는 일간을 안 적는다(생일에서 계산). 적어 두면 생일과 어긋난 값이 두 벌 생긴다.

**빈 상태 크래시 두 건도 고쳤다**(실측으로 찾았다):
- `analyzeGroup`이 0명 그룹에서 `reduce` 초기값 없이 돌아 던졌다 → 빈 분석 결과를 돌려준다
- **"나"가 없으면 `layoutGroupOrbits`가 `me.el`을 읽다 죽는다** → 가드 + `applySeed`가 보장

**남은 것 — 커밋 기록.** 파일에서는 지웠지만 `fb0588d`·`cda5464`·`d9fe178` 세 커밋의 **메시지에
생년월일 표가 그대로 있다.** 2단계(깃허브) 전에 아래 셋 중 하나를 정해야 한다:
- **(가)** 커밋 1개짜리 새 저장소로 시작 — 확실하지만 111개 커밋의 기록을 잃는다
- **(나)** `git filter-repo`로 소급 수정 — 기록은 남지만 `RICH_N`이 **최초 커밋부터** 들어 있어
  사실상 전체 재작성이고, 빠뜨리면 되돌릴 수 없다
- **(다) 추천** — 기록은 **비공개** 저장소에 두고, 공개용(`group-saju`)은 (가)처럼 새로 시작

### 2·3단계 — 깃허브·배포 ✅ **완료 [2026-08-28]**

주소: **https://shyquisyz.github.io/group-saju/** — 백업이 목적이고 아직 남에게 안 준다.
`master` 브랜치를 `main`으로 밀었고, GitHub Pages가 루트의 `index.html`을 직접 서빙한다(빌드 없음).

**이 단계에서 밟은 것 — 공개 저장소에 데이터가 올라갔다.** 분리했다고 보고했지만
`demo-data.js`가 함께 푸쉬되어 배포된 페이지에 사람 목록이 그대로 떴다. 저장소를 지우고
다시 만들어 해결했다. 교훈은 CLAUDE.md에 규칙으로 남겨 둔다 —
**접근을 막는 것으로는 부족하고 데이터 자체를 없앨 뒤 grep으로 0건을 확인한다.**

### 4단계 — 첫 화면(자기 등록) ✅ **완료 [2026-08-28]**

배포된 상태를 열면 빈 우주만 보이고 무엇을 해야 할지 알 수 없었다. 더 큰 문제가 아래에 있었다.

**[실측으로 찾은 막힌 길] 빈 은하에서 앱의 추가 경로로 첫 사람을 넣어 보니
`{isMe:false}` 하나가 생기고 "나"가 없는 채로 남았다.** 항성이 없고 구이
안 잡히며(`layoutGroupOrbits`가 가드에 걸려 반환), 화면에는 "나"로 바꿀 방법이 없다 —
즉 **빈 상태에서 시작한 사용자는 정상 상태에 도달할 수 없었다.**
"첫 번째로 등록된 사람을 자동으로 나로 삼는다"는 짐작도 틀렸다 — 그 로직은 시드에만 있었고
UI 경로에는 없었다.

**한 일**

- **첫 화면 `#onboard`** — 인물 0명일 때만 뜼고 **닫는 수단이 없다.**
  "모든 관계는 당신을 기준으로 계산됩니다"를 한 줄 밝힌다 — 건너뛰기를 안 두는 대신
  **왜 필요한지를 말한다**(2026-08-28 지시)
- **`isMe`를 만드는 경로는 여기뿐이다.** 인물 시트의 저장은 `isMe:false`를 **명시적으로**
  넘긴다 — 전엔 안 넘겨서 결과적으로 false였고, 그건 의도가 아니라 누락이었다
- **"나 혼자" 안내 `#nextstep`** — 인물이 나 하나일 때 패널에 "아직 당신뿐입니다 … [사람 추가]"
  카드가 붙고 ＋ 버튼이 맥동한다. 둘이 되면 둘 다 사라진다.
  모바일은 패널이 하단 시트라 기본 높이에서는 카드가 접힌 아래로 밀려 안 보였다(390x844 실측) —
  혼자일 때만 `tall`로 열어 해결했다
- **성별 칸** — §5.6 문제 4 참고
- **내 카드의 역할 표시** — 그룹 밖에서 내 카드를 열면 "불타는 행성"으로 찍혔다.
  나는 은하의 유일한 항성(§2.1)이므로 "☀ 나 · 이 은하의 항성"으로 고쳤다 —
  첫 화면 뒤 사용자가 가장 먼저 보는 카드라 더 눈에 띄었다
- **서비스명 통일** — 헤더가 아직 "관계도 / RELATION SAJU"였다. §0.5에서 정한
  "그룹사주(가칭) / GROUP SAJU"로 바꿨다(`<title>` 포함)

**남은 것** — 빈 가상 성계 고리가 혼자일 때도 항성 옆에 떠 있다(설명 없는 빈 타원).
기존 동작이라 이번에 건드리지 않았다 — 2인 미만일 때 감출지 판단할 것.

### 5단계 — 저장 기능 ✅ **완료 [2026-08-28]**

브라우저 저장소(localStorage)에 사람·관계선·그룹을 둔다.

**[형식 — 새로 만들지 않았다]** 시드 형식(`applySeed`가 읽는 모양) 그대로 쓴다 —
불러오는 코드를 따로 안 짜도 되고 `my-data.js`와 형식이 두 벌로 갈라지지 않는다.
버전(`v`)과 `MIGRATIONS` 자리를 만들어 둑다 — 구조를 바꿀 사람이 함수를 더하고 `SAVE_VER`을 올린다.

**[용량 — 실측 2026-08-28]** 30인(그룹 8·관계선 29·시각 필드 포함) = **7,931바이트**.
같은 브라우저에서 64KB씩 밀어 보니 **5,177,344바이트**에서 `QuotaExceededError`.
즉 30인은 한계의 **0.15%**다. 압축·분할·정리 정책은 필요 없다.
그러면서도 실패 처리를 둔 이유는 용량이 아니라 **비공개 모드처럼 쓰기 자체가 막히는 환경** 때문이다.

**[저장 지점을 `renderNav` 한 곳으로 모았다]** 사람·그룹이 바뀌는 경로가 전부 여기를
지나간다. 변경 지점 여섯 곳에 각각 붙이는 것보다 **빼뜨릴 수 없다**는 게 결정적이었다.
부팅 중에는 `saveSuspended`가 막는다 — 안 그러면 복원 도중의 절반짜리 상태가 저장본을 덮어쓴다.

**[잡은 함정 셋]**
- **아이디 재발급** — `addNode`는 key가 없으면 `'n'+(seq++)`를 쓴다. 복원한 사람이
  n0~n9인데 `seq`가 0으로 남아 있으면 **다음 사람이 n0을 다시 받아** 관계선·소속이
  엉뚱한 사람을 가리킨다. 복원 뒤 `seq`를 최대값 너머로 밀어 막았다
  (실측: 복원 후 seq 2, 셋째가 n2, 중복 없음)
- **빈 저장본** — 사람 0명일 때 쓰면 담을 것도 없이 `my-data.js`를 가린다.
  첫 구현에서 실제로 그랬다 → 0명이면 쓰지 않는다
- **그룹 0개일 때 그룹 트리가 통짜로 비었다** — `renderNav`의 유일한 호출자가
  `createGroup`이라 그룹이 없으면 부팅 내내 한 번도 안 돌았다. 데모 데이터에는 항상
  그룹이 있어 안 드러났는데, 저장본으로 "나 혼자"를 복원하자 드러났다 — 트리 안에 있는
  **모두 지우기 버튼에 닿을 수 없었다.** 배포본 실측으로 발견해 부팅 끝에 `renderNav()`를 추가

**[실패 경로 — 빈 은하로 조용히 떨어지지 않게]**
읽기 실패를 "저장본 없음"과 섞으면 첫 화면이 떠서 **사용자는 데이터가 날아간 줄 안다.**
그 구분이 `loadSave`에서 가장 중요한 지점이다.

| 상태 | 처리 |
|---|---|
| `none` | 저장본 없음 — `my-data.js`를 쓰고, 없으면 첫 화면 |
| `ok` | 복원 |
| `corrupt` | 원본을 **지우지 않고** `:corrupt`로 옛기고 사유와 함께 알린다 |
| `newer` | `v`가 더 큼 — **저장을 꺼서 덮어쓰지 않는다**(실측: v99 심고 사람 등록 → 저장본 v99 그대로) |
| `blocked` | 읽기 자체가 예외 — 한 번만 알리고 앱은 계속 돌다 |

설계안에는 "첫 화면 **대신** 안내"라고 적었으나, 그러면 사람 0명에 첫 화면도 없어
**빠져나갈 길이 없는 화면**이 된다. 알림 바(z-index 14)를 첫 화면(12) 위에 엹어 둘 다 보이게 했다.

**[`my-data.js` 규칙]** 배포에는 파일이 없어(`.gitignore`) 충돌 자체가 안 생기고
**로컬에서만** 겹친다. 저장본이 이기되, 이겼을 때
**"my-data.js는 무시됩니다 [파일에서 다시 불러오기]"를 띄운다** — 조용히 무시하면
파일을 고쳐도 안 바뀌는 이유를 한참 뒤지게 된다.
(실측 8900: 1차 `none` → 10인·7그룹 로드·저장됨, 2차 `ok` → 알림과 버튼 노출)

**[시각 필드 — 필드만, 읽는 로직 없음]** 인물 `createdAt`·`updatedAt`, 그룹 `createdAt`.
화면에도 정렬에도 판단에도 안 쓴다(2026-08-28 지시: "로직은 붙이지 말고 필드만").
지금 심는 이유는 **나중에 심으면 기존 인물의 값을 영영 못 받기 때문**이다(경도·성별과 같은 논리).
값이 없으면 `null` — **지어낸 시각을 채우지 않는다.** "없다"와 "모른다"를 구분해 두어야
나중에 토큰·수정 정책을 붙일 때 쓸 수 있다.

**[모두 지우기]** 그룹 트리 하단. **일부러 작고 흐리게** 둔다(`--fs-xs`, 강조색 없음) —
못 찾는 것보다 실수로 누르는 쪽이 위험하기 때문이다. **두 단계 확인**이고
두 번째 버튼은 첫 번째와 **다른 자리**에 생긴다(연타로 지나가지 않게).
`confirm()`을 안 쓴 이유 — (1) 문구를 못 꾸미어 "무엇이 몇 개 사라지는지"를 못 보여준다.
(2) 엔터로 통과하기 쉽다. **우리 키만** 지운다 — `clear()`는 같은 주소의 다른 것까지 날린다.
백업(`:corrupt`)도 함께 지운다 — **거기에도 생년월일이 있다.**

**[안내 문구 — 맥락에 맞게 다르게]** 같은 문장을 반복하지 않는다.
- 첫 화면(`.privnote`): "지금 쓰고 계신 브라우저에만 저장됩니다 … **다른 기기나 다른 브라우저에서는
  보이지 않고**, 반대로 이 기기를 함께 쓰는 사람은 볼 수 있습니다"
  — 폰에서 열었다가 데이터가 없어졌 다고 놀라는 것을 막는 게 목적(2026-08-28 지시)
- 사람 추가 시트(각주 옆): "이분의 생년월일도 이 브라우저 안에만 남습니다"
  — 앞은 "내 정보가 어디 가나", 뒤는 "남의 정보를 넣고 있다"가 맥락이다

**[암호화는 하지 않았다]** 열쇠를 같은 브라우저에 두면 잠그는 시늬일 뿐이고,
비밀번호를 받으면 잃었을 때 복구가 없다. **못 지킬 보호를 약속하는 것보다 어디 있는지
정확히 알리는 편이 낫다** — CLAUDE.md "감추지 말고 없앤다"의 반대편 짝이다.

~~**남은 것** — 내보내기/가져오기는 **답을 못 받아 넣지 않았다.**~~
→ **[2026-08-28] 6단계에서 처리했다.** 이 절이 지적한 구멍(캐시 지우기 한 번에 사라지고
서버가 없어 백업 수단이 하나도 없음)이 그 작업의 출발점이었고, 예상대로 형식이 시드와 같아
비용이 작았다. 아래 6단계 참고.
가상 성계는 설계대로 저장하지 않는다(조합을 시험하는 임시 작업대) — 백업 파일에도 안 담는다.

---

### 6단계 — 내보내기 · 불러오기 ✅ **완료 [2026-08-28]**

5단계가 남긴 구멍을 메운다 — 브라우저 저장소는 **방문 기록·쿠키를 한 번 지우면 사라지고**
서버가 없어 백업 수단이 하나도 없었다. 열 명을 넣은 노동이 그 한 번에 날아간다.

**[형식]** 저장본(=시드) 그대로에 봉투 셋만 덧댄다 — `app` · `v` · `exportedAt`.
- `app`이 **잘못된 파일을 가려내는 유일한 표식**이다. 저장본 판정("people 배열이 있는가")만으로는
  남의 JSON이 통과한다. 백업 파일은 **밖에서 오므로** 저장본보다 검증이 엄격해야 한다.
- **개수를 봉투에 안 적는다.** 적으면 진실이 두 벌이 되고, 미리보기가 봉투를 믿으면
  거짓말하는 파일에 속는다. 화면에 띄우는 수는 **파싱 결과의 배열 길이**로 센다.
- 파일명 `group-saju-backup-YYYY-MM-DD.json` — 메신저·파일앱을 거치며 한글이 깨지는 경로가
  있어 ASCII로 둔다. 날짜가 있어야 여러 개 쌓였을 때 최신을 고른다.

**[경로 둘 — 파일이 주, 텍스트가 보조]** 주 사용처가 "카톡으로 자기한테 보내두고 다른 기기에서
연다"인데, 카톡 → 다른 기기 브라우저로 **파일**을 넘기는 단계 수가 기기마다 다르다.
파일이 막히면 텍스트를 대화창에 붙여 보내고 반대편에서 복사해 넣는 길로 우회한다.
**둘은 `parseBackup()` 하나를 탄다** — 두 벌로 갈라지면 한쪽만 고쳤을 때 조용히 어긋난다.

**[불러올 때 — 묻는다]** 설계 초안은 "덮어쓰기 하나만"이었으나 **사용자 지시로 뒤집었다**:
> "덮어쓰기 고정은 위험하다. 열 명 넣어놓고 파일 불러왔다가 다 날아가면
> 그게 지금 걱정하는 바로 그 상황이다."

확인 화면에서 **합치기 / 지우고 불러오기**를 고른다(안전한 쪽이 위). 지금·파일의 사람 수와
파일을 만든 시각을 함께 보여준다 — 모두 지우기와 같은 원칙(무엇이 몇 개 사라지는지).
사람이 0명이면 잃을 것이 없으므로 확인을 건너뛴다.

**[합치기 규칙]**
- **이름과 생년월일이 모두 같으면 같은 사람으로 보고 건너뛴다.** 이 기준을 **확인 화면에
  그대로 적는다** — 안 적으면 왜 어떤 사람이 안 들어왔는지 알 수 없다.
- id로 못 맞춘다. `n0`은 기기마다 독립 발급이라 **다른 기기의 `n0`과 이 기기의 `n0`은
  아무 관계가 없다.**
- **파일의 "나"는 지금의 나에 대응시킨다.** 일반 인물로 강등하면 그 사람이 속한 그룹이 전부
  §7.2("나 없는 그룹 금지")를 깬다. 이름·생일이 다르면 합치기 전에 화면에서 알린다.
- 그룹은 **이름이 같으면 건너뛴다.** 멤버까지 비교해 부분 병합하면 사용자가 무엇이 어떻게
  바뀌었는지 추적할 수 없다 — 예측 가능한 쪽을 골랐다.
- **열쇠를 n0부터 다시 매긴다.** 양쪽 다 `n0`을 쓰므로 그대로 두면 겹친다.

**[잘못된 파일이 지금 데이터를 못 건드리게 — 3단]**
1. **`parseBackup`은 순수 함수다.** 전역(`nodes`/`groups`/`edges`)도 localStorage도
   읽지 않고 쓰지 않는다. 실패해도 화면이 그대로인 것이 이 격리로 보장된다.
2. **검증 13종** — `app` 표시 / 버전 / 실재하는 날짜 / `isMe` 정확히 1명 / key 중복 /
   유형 목록 / 없는 사람 참조 / 그룹 상위 고리 / 크기·인원 상한.
   - **`applySeed`의 "isMe 없으면 첫 사람을 나로" 폴백을 쓰지 않는다.** 그건 우리가 만든
     시드에 대한 관용이고, 외부 파일에 적용하면 **엉뚱한 사람이 나가 된다.**
   - **날짜 검증이 특히 중요하다.** `dayStem`은 이상한 값을 받아도 안 던지고 **조용히 다른
     일간**을 낸다(60일 주기라 화면으로 못 잡는다).
   - **화이트리스트로 재구성한다** — 파일 객체를 그대로 안 쓰고 아는 필드만 옮겨 담는다.
     `applySeed`가 `birth`를 `Object.assign`에 넘기므로 이상한 키가 흘러드는 길을 막는다.
3. **적용은 저장소에 쓰고 `location.reload()`.** 화면을 직접 고치다 예외가 나면 반쪽 상태가
   `renderNav` → `saveState`를 타고 **저장본을 덮어쓴다.** `wireWipe`의 "지웁니다"가 쓰는
   방식과 같고, 부팅 경로를 그대로 타므로 **불러오기 전용 복원 코드를 안 만든다.**

**[되돌리기]** 직전 저장본을 `groupsaju:save:prev`에 남긴다. 알림 바와 그룹 트리 **양쪽**에
노출한다 — 잘못 덮어썼다는 걸 깨닫는 건 대개 몇 분 뒤라 알림만으로는 늦다.
`clearSave`가 `:prev`도 지운다 — **거기에도 생년월일이 있다**(`:corrupt`와 같은 논리).

**[첫 화면의 두 번째 선택지]** 사용자 지시:
> "기기를 옮기는 게 주된 사용처인데 그때 온보딩부터 다시 하게 만들면 안 된다."

"이미 쓰던 데이터가 있으세요? [내보낸 파일 불러오기]". 시트 z-index를 **13**으로 올려
첫 화면(12) 위에 뜬다. 사람이 0명이면 `openBackup`이 내보내기 구역을 스스로 감춘다.
**자리는 폼 위다** — 처음엔 "시작하기" 아래 뒀는데 카드가 화면보다 길어 **스크롤해야만
보였다**(1501×812 실측). "이걸 다 다시 입력해야 하나"라고 느끼는 순간은 맨 위다.

**[안내 문구 — 자리마다 다르게]**
- **첫 화면**: 소실 조건을 밝히되 **행동을 요구하지 않는다** — 아직 백업할 것이 없다.
- **3명째**: 알림으로 한 번 + [지금 내보내기]. 1명은 첫 화면 직후라 안내가 이미 많고,
  2명은 "다음 할 일" 카드가 사라지는 시점이라 겹친다. 3명이면 다시 넣기 아까운 노동이
  쌓였다. **임의로 정한 값이다.**
  실제로는 **부팅 끝의 `renderNav()`에서도 한 번 뜬다.** 설계안은 "부팅 중엔 안 뜬다"였는데
  코드를 고치지 않고 의도를 이쪽으로 바꿨다 — 이미 3명 넘게 쌓은 기존 사용자는 "3명째가 되는
  순간"이 지나서, 부팅을 막으면 **영영 안내를 못 받는다.**
- **백업 시트**: 파일에 생년월일이 그대로 들어가고 **카톡으로 보내면 카톡 서버에도 남는다**고
  적는다. 카톡으로 보내라고 권하는 이상 이 경고 없이 내보낼 수 없다.

**[겹침 버그 — 실측으로 잡음]** "합쳤습니다" 알림을 백업 권유가 덮었다. 다른 기기에서 처음
불러오는 경우가 정확히 그 조합이다(저장본 없음 → 불러오기 → 새로고침 → 3명 이상 → 권유 플래그
없음 → 부팅 끝 `renderNav`가 권유를 띄우며 결과 알림을 지운다). `applyBackupSeed`가 권유
플래그를 미리 세워 막는다 — **방금 백업에서 되살린 사람에게 "백업해 두세요"는 할 말도 아니다.**

**[실측 2026-08-28]**
- 잘못된 파일 13종 전부 의도대로 거부, **전역 상태 불변 확인**(비교로 검증)
- 내보내기 → 파싱 왕복 **무손실**(사람·그룹·관계선 값 일치. 키 순서만 다르다)
- 합치기: 겹치는 3명 건너뛰고 새 2명 추가, 이름 같은 그룹 1개 건너뜀, `isMe` 1명 유지,
  키 중복 없음
- 지우고 불러오기: 9명·2그룹 → 3명·0그룹, `:prev`에 9명 보존, 되돌리기로 9명 복구
- **배포본(https://shyquisyz.github.io/group-saju/)에서**: 저장소를 완전히 비운 상태
  (= 다른 브라우저와 같은 조건)에서 실제 `File` 객체로 불러와 **6명 전부 그대로 복원**
  (이름·생일·일간·관계선 15개·도시/경도 일치)
- 콘솔 오류 0건

**[★ 파일 저장 — 동작 확인됨. 그리고 그 과정에서 사고를 하나 밟았다]**

처음엔 "파일이 안 떨어진다"고 판단했다. 다운로드 폴더에 `.tmp`만 있고 최종 파일명이 없었다.
**틀린 진단이었다** — 잠시 뒤 `group-saju-backup-2026-08-28.json`(1,523바이트)이 실제로
저장됐고, **떨어진 곳이 다운로드 폴더가 아니라 이 저장소 루트였다.** 브라우저가 마지막에 쓴
다운로드 위치를 기억하기 때문이다.

**그래서 무엇이 위험한가.** 로컬에서 이 저장소를 열어 두고 내보내기를 누르면 **등록한
사람들의 생년월일이 든 파일이 저장소 루트에 떨어진다.** 실제로 그다음 `git add -A`가
그걸 스테이징했고 커밋에까지 들어갔다(`create mode 100644 group-saju-backup-…json`).
푸시 전에 발견해 `git reset --soft`로 되돌렸다. 이번에 담긴 것은 검증용 가짜 데이터였지만,
**2·3단계에서 `demo-data.js`로 밟았던 사고와 정확히 같은 경로**다 — 그때 얻은 규칙
("접근을 막지 말고 데이터를 없애라")이 이 경로는 못 막았다. 파일이 **앱에 의해 새로 생기기**
때문이다.

**조치**: `.gitignore`에 `group-saju-backup-*.json`과 `*-backup-*.json`을 넣었다.
`git check-ignore`로 세 가지 이름(날짜가 다른 것, 접두어가 다른 것 포함)이 전부 걸리는 것을
확인했다. 앱 쪽은 안 고쳤다 — 파일명을 바꾼다고 다른 폴더에 떨어지지 않고, 저장 위치는
브라우저가 정하는 것이라 코드로 제어할 수 없다.

**아직 확인 못 한 것**: iOS Safari에서 `<a download>`가 파일을 남기는지. 텍스트 복사 경로는
배포본에서 동작을 확인했다("복사했습니다" 알림 확인).

---

### 7단계 — 연속 입력 ✅ **완료 [2026-08-28]**

한 명씩 시트를 열고 닫아야 해서 **열 명이면 열 번**이었다.

**[후보 넷을 비교해 둘을 골랐다]**

| | 판단 |
|---|---|
| **A. 연속 입력**("저장하고 계속") | **채택.** 가장 싸고 즉시 효과 |
| **B. 표(스프레드시트)** | **버렸다.** 390px에 8열이 안 들어가 **모바일에서 못 쓴다.** 모바일 대응이 이미 미결(§5.2)인데 데스크톱 전용 입력을 만들면 그 자체가 부채다. 시각 12칸·도시 60개 optgroup을 셀에 넣는 것도 전부 새 문제 |
| **C. 붙여넣기(여러 줄 일괄)** | **다음 후보로 남긴다.** 이득이 가장 크지만 파서·미리보기·오류 UI가 필요해 분량이 크다. 6단계의 검증·미리보기·확인 UI를 물려받으면 싸진다 |
| **D. 그룹 시트 안 새 사람** | **채택.** 아래 참고 |

**A와 C는 경쟁하지 않는다** — C는 **명단이 이미 있는 사람**(카톡·메모·엑셀에서 가져온다),
A는 **머릿속에서 꺼내는 사람**이다.

**[A — 무엇을 남기고 무엇을 비우나가 전부다]**
- **이름은 비운다** — 유일하게 반드시 다른 값
- **생년월일은 남긴다** — 바꿀 확률보다 비슷할 확률이 높고(가족은 연대가 몰린다) 셀렉트라
  값이 화면에 보인다
- **그룹 체크는 남긴다** — "이 그룹에 다섯 명 넣기"가 정확히 이 흐름이고 여기가 가장 큰 이득이다
- **성별은 비운다** — 앞사람 값이 남으면 **안 보고 넘긴 사람의 값이 그리로 굳는다**
  (§5.6 문제 4 "기본값을 두지 않는다"가 여기서도 유효하다)
- **카메라를 움직이지 않는다** — 매번 날아가면 시트에 가려 보이지도 않으면서 어지럽기만 하다
- **"OO 추가됐습니다 · 지금까지 N명" 줄이 필수다.** 시트가 안 닫혀 **화면이 아무것도 안
  변하므로**, 이게 없으면 저장됐는지 알 수 없어 같은 사람을 두 번 넣는다
- 그룹 시트에도 대칭으로 넣었다. 이름·멤버는 비우고 **유형과 상위 그룹은 남긴다**

**[D — 어려웠던 것은 문제가 아니라 전제였다]**
§5.6 백로그는 이 항목을 "시트 위에 시트"로 전제해 셋(입력 보존 · `#dim` z-index ·
자동 체크)이 함께 필요하다고 봤다. **멤버 격자 끝의 한 줄로 바꾸니 셋 다 사라졌다** —
시트를 안 닫으니 보존할 것이 없고, 시트가 하나라 겹칠 일이 없다.
**이름과 생년월일만 받는다**(`dayStem(y,m,d)`이 날짜만 쓴다). 나머지는 나중에 그 사람 카드에서.
**사람은 즉시 만든다** — 사람 시트의 새 그룹(대기 목록)과 반대다. 그룹은 이름·유형 두 값뿐이라
대기시킬 수 있지만, 사람은 만들자마자 천체·라벨·관계선이 붙는 실체라 "저장 안 된 사람"이라는
상태를 새로 만들 이유가 없다. 그룹 만들기를 취소해도 사람은 남는데 **그게 맞다** —
사용자가 하려던 일이 "이 사람을 등록하는 것"이었다.

**[실측 — 클릭 수]** 배포본에서 다섯 명을 실제로 넣었다:
**8클릭**(FAB → 사람 추가 → 이름 칸 → 저장하고 계속 ×4 → 저장) + 이름 타이핑 5회.
기존 방식은 사람마다 FAB → 사람 추가 → 이름 칸 → 저장 = **20클릭**.

**[그룹 칸 — 후보 목록과 오타 경고, 2026-08-28 개정]**

처음엔 native `<datalist>`를 붙이고 **없는 이름은 오류로 막았다.** 둘 다 고쳤다.

- **후보는 직접 만든다**(`#pt-sugg`). native datalist는 **쉼표를 쓰는 순간 후보가 0**이 된다
  — 브라우저가 칸 전체를 한 덩어리로 매칭하기 때문이다. 실측: 그룹이 가족/대학 동기/회사 팀일
  때 `"가족, 대"`의 후보가 0개였다. 여러 그룹에 넣는 흐름이 이 앱의 핵심인데 거기서 도움이
  사라진다. 도시는 쉼표를 안 쓰고 목록 밖도 그대로 저장되므로 native를 유지한다.
- 매칭은 **시작 일치 → 부분 일치 → 편집거리(오타)** 순이고 **공백을 무시**한다.
  실측: `가족`→`우리 가족`(중간 일치) · `대학동기`→`대학 동기`(공백) · `가죽`→`가족`(오타) ·
  `우리 가족, 동기`→`대학 동기`(쉼표 뒤 조각만 본다, 이미 고른 이름은 뺀다).
- **없는 이름을 오류에서 경고로 내렸다.** 오류로 막으면 **그 사람이 아예 등록되지 않는다** —
  사용자가 원한 것은 "사람은 들어가되 그룹에 못 들어갔다는 걸 아는 것"이다. 이제 노란
  "없는 그룹" 배지가 붙고 그 사람은 **미분류**로 들어간다. 표에서 유형을 못 받는 사정은
  그대로여서 없는 이름으로 그룹이 생기지는 않는다.
- **빈 칸은 경고하지 않는다.** 아무것도 안 쓴 것은 오타가 아니라 의도다(사용자 지시).
- **저장을 누르면 한 번 묻는다** — 노란 칸과 요약만으로는 지나치기 쉽고, 지나치면
  "그룹에 넣은 줄 알았는데 안 들어간" 상태가 된다. 화면이 안 변하는 종류의 손실이라
  수동 표시로는 부족하다고 봤다. `"우리가족들"이라는 그룹이 없습니다 / 오타사람 님은
  미분류로 들어갑니다 / [돌아가서 고칩니다] [그대로 등록합니다]`.
  조사는 `particle()`로 받침에 따라 갈린다(`대학동아리`→"라는").

**남은 것 — C(붙여넣기)와 그 앞의 실측 하나.**
`addbtn`은 새 사람마다 **기존 전원과 관계선을 만들고**(`nodes.forEach(o=>addEdge(n,o))`),
`addEdge`는 **엣지 하나마다 `THREE.Line`과 전용 머티리얼을 따로 만든다** — 즉 엣지 수 =
드로우콜 수다. 10명 45개 / 30명 435개 / 50명 1,225개. 데모가 8~10인이라 한 번도 드러난 적이
없고, **일괄 입력이 이 상황을 처음으로 실제로 만든다.**
→ **C 착수 전에 엣지 수 대비 프레임 시간을 실측할 것**(CLAUDE.md대로 `gl.finish()`로 감싼다.
`performance.now()`로 `render()`만 재면 CPU 제출 시간만 재고, 더 많은 일을 하는 쪽이 더 빨라
보이는 함정을 이미 한 번 밟았다). 느리면 처방은 셋 — 머티리얼 공유(kind 3종) /
엣지를 한 `LineSegments`로 병합 / 활성 그룹 것만 그리기. **재고 정한다.**
저장 용량은 문제없다(실측 30인·엣지 29개 = 7,931바이트, 한계의 0.15%).

---

- ~~localStorage 저장~~ **완료(★★ 5단계, 2026-08-28)** · ~~내보내기/가져오기~~ **완료(★★ 6단계)**
- 공유 이미지 내보내기, 만세력 엔진 L2 연결
- **★ 행성별 코멘트 [2026-08-28 기록] — 이 목록에서 유일하게 지금 바로 되는 항목.**
  **지금 되는가 — 예. 선행 없음.** 서버도 엔진도 필요 없다.
  인물 패널의 해설을 **더 살리는** 방향이다. §5.7이 "엔진을 올려야만 되는 것"으로 개인 서술의
  개인화를 지목했지만(같은 일간이면 문장이 한 글자도 안 다르다), 그건 **사람마다 다르게**
  쓰는 이야기다. **일간 열 가지 자체를 두껍게 쓰는 것**은 지금 데이터로 가능하다 —
  행성 생김새(§2.3 F-5)는 열 개가 전부 다르게 만들어 뒀는데 **글은 그만큼 안 따라갔다.**
  손댈 자리: `STEM_BLURB`(일간 한 줄)과 `showNode`의 본문 조립부.
  문체는 §5.7 2단계 원칙을 그대로 따른다 — **주어는 사람, 오행은 괄호 근거**,
  자연물의 그림에서 시작하되 **그 그림이 관계의 성질로 이어지게**(비유에서 끝나면 신비주의).
  주의: 인물 패널은 §5.7 3단계의 톤 분리에서 **"조언 유효"** 쪽이다(그룹 패널과 같은 급).
  관계 보기·가상 성계와 문구를 공유하지 않는지 확인할 것.
- 그룹 유형별 지표 세트 분화(현재 `GTYPE_CLOSER`는 마무리 문장 1줄만 다름) — L3 이후
- 근일점 위상 = 세운 연동 — L5 이후. 그전엔 날짜 기반 임의 위상으로 형태만
- ~~**그룹 만들기 중 "새 사람" 추가**~~ → **완료 [★★ 7단계, 2026-08-28].**
  아래는 착수 전 기록으로 보존한다. **어렵다고 본 근거가 전제 때문이었다**는 사례라 지우지 않는다.
  [2026-08-26 기록, E에서 범위 밖으로 뺌]
  **문제**: 그룹을 만들다 "이 사람이 아직 없네"가 되면 **입력이 통째로 날아간다.** 지금은
  시트를 닫고 → FAB → "사람 추가" → 저장 → FAB → "그룹 만들기" → **이름·유형·이미 고른
  멤버를 처음부터 다시** 해야 한다. FAB를 두 번 거치고 그 사이 입력이 보존되지 않는다.
  실제로 자주 나올 흐름이다(그룹은 보통 사람보다 나중에 만들어지는데, 만들다 보면 빠진
  사람이 발견된다).
  **해법 방향**: 합침이 아니라 국소 조치 — 멤버 목록 끝에 "+ 새 사람" 행을 두고 그 자리에서
  사람을 추가한 뒤 그룹 시트로 돌아온다.
  **[2026-08-27] 반대편은 처리됐다** — 사람 추가 시트에서 그 자리에 새 그룹을 만들 수 있다(§5.3-1).
  그쪽은 새 그룹이 이름·유형 두 값뿐이라 한 줄로 끝났지만, 이쪽은 사람이 이름·생년월일·시각·
  도시를 요구해 한 줄에 안 들어간다 — 그래서 여전히 남는다.
  **왜 E에서 뺐나**: 시트 위에 시트가 겹치는 구조라 (1) 그룹 시트의 입력 상태를 보존했다가
  복원하는 로직 (2) `#dim` z-index와 두 시트의 스택 순서 (3) 새로 만든 사람을 멤버 목록에
  자동 체크 — 세 가지가 함께 필요하다. E의 목적(형식 통일·컴팩트화)과 성격이 달라 분리했다.
  **[2026-08-28 결말] 셋 다 필요 없었다.** "시트 위에 시트"가 전제였고, 멤버 격자 끝의
  **한 줄**(`#g-newp-row`)로 바꾸니 (1)은 시트를 안 닫으니 보존할 것이 없고 (2)는 시트가
  하나라 겹칠 일이 없고 (3)만 한 줄로 남았다. 이름·생년월일만 받는다 — `dayStem(y,m,d)`이
  날짜만 쓰므로 시각·도시·성별은 지금 어떤 계산에도 안 들어간다(이 시트가 그 셋을 접어 둔 것과
  같은 근거). CLAUDE.md "은유를 그림으로 옮길 때 — 방식부터 맞춘다"가 UI에도 적용된 사례다:
  **어려웠던 것은 문제가 아니라 전제였다.**
  **[2026-08-28 후속 — 두 번 더 바뀌었다]** ~~한 줄(`#g-newp-row`)~~ → **한 명 화면**
  (시각·도시·성별을 못 받아 그 사람만 반쪽으로 들어갔다) → **탭 전환**(§5.3-1 (4)).
  지금 남은 것은 (3) 자동 체크뿐이고 그것도 플래그 하나(`gsWantMember`)다 —
  ~~`gsStash`/`gsRestore`~~ 는 탭이 상태를 보존하면서 통째로 사라졌다.
- **시기 기반 표식 — L5(대운·세운) 도달 후 재검토 [2026-08-26 기록].** "최근에 주의할 사람",
  "올해 잘 풀리는 사람" 같은 표식. B(고리 재설계)에서 보류한 항목이다.
  **위성 충돌 은유를 쓸 자리** — 고리가 정적인 중력 흔적이라면 시기 표식은 지금 일어나는
  사건이므로, 충돌 섬광·파편처럼 일시적인 형태가 맞다. 고리와 시각 언어가 겹치지 않는다.
- **밀도(§2.9) 보정은 성운 단독 작업이다 [2026-08-26 확정].** C에서 실측으로 확인했다 —
  중간톤을 만드는 것의 **95.7%가 성운**이고 은하 전체(천체·궤도선·고리·글로우)는 **0.77%**뿐이다
  (단일 시야 기준: 전체 17.06% / 성운만 16.33% / 은하만 0.77%). 그래서 공간을 넓히든 좁히든,
  천체를 키우든 줄이든 중간톤은 거의 안 움직인다 — B에서 고리를 22개→7개로 68% 줄였을 때
  14.90→14.93으로 불변이었던 것이 그 증거다. C 전후도 14.93 → 14.87로 불변.
  ~~**처리 방법**: 캔버스를 고정하고 `NEBULA_FLOOR` 하나로 맞춘다.~~
  → **[2026-08-27] 이 백로그 항목은 종결됐다.** 성운 불투명도를 1.0 → 0.22로 낮추기로 하면서
  **중간톤 목표치(17%) 자체가 폐기**됐기 때문이다(§2.9). 중간톤은 16.36% → 0.79%가 됐고,
  그 지표는 이제 성운을 아예 못 본다(0.22와 완전 제거의 차이가 0.08%p인데 화면으로는 확실히
  구분된다 — 성운 휘도가 L=25 아래로 내려가 "암흑" 버킷에 통째로 들어갔다).
  다시 수치 기준이 필요하면 임계값부터 새로 잡을 것. 기록용 구 출발점:
  8시야 평균 85.03 / 14.87 / 0.10(캔버스 1904×1026), 구 목표 83 / 17 / 0.2.
- ~~**C(공간 재조정) 착수 시 밀도 출발점 [2026-08-26 기록].**~~ → C 완료. 위 항목으로 대체. §2.9 8시야 평균이
  **암흑 84.96 / 중간톤 14.93 / 밝음 0.10**(캔버스 1904×1015)이다. 목표는 83 / 17 / 0.2이므로
  중간톤이 약 2%p, 밝음이 절반 모자란다. A·B에서 보정하지 않고 남긴 값이다 —
  C가 공간을 넓히면 어차피 전부 다시 계산되기 때문. **이 값을 출발점으로 삼을 것.**
- **envMap 도입 시 금속 재질 상한 재검토 가능 [2026-08-26 기록].** `STEM_SURF`의
  `metalness`는 0.40에서 묶여 있는데 이건 디자인 판단이 아니라 **구조적 제약**이다 — 씬에
  환경맵이 없어 금속이 반사할 대상이 없고, PBR상 `metalness`가 1에 가까우면 확산광이 0이라
  결과가 검정이 된다(실제로 辛에 .92를 줬다가 거의 검게 렌더돼 되돌렸다). 지금은 금속감을
  낮은 `roughness`가 만드는 좁은 하이라이트로만 내고 있다. 큐브맵이든 절차적 그라디언트든
  환경맵을 넣으면 庚·辛의 금속 표현이 한 단계 올라갈 여지가 있다. 렌더 파이프라인 변경이라
  별도 작업으로 다룰 것

---

## 6. 사주 엔진 로드맵 (L1 → L6) — v1과 동일

| 단계 | 내용 | 상태 |
|---|---|---|
| **L1** | 일간 → 오행 → 상생상극 | ✅ 구현됨 |
| **L2** | 만세력: 4주 8자, 음↔양력, 절기 기준 월주(절입 시각), 진태양시 보정, 야자시 | 미착수 — **착수 전에 §5.6의 "입력 문제 4건"을 읽을 것.** 출생지 보정·시간대 경계·시간 모름·성별 네 가지가 얽혀 있어 엔진 설계와 함께 결정해야 한다 |
| **L3** | 지장간, 십신, 12운성, 신강신약, 오행 점수화 | 미착수 |
| **L4** | 합·충·형·파·해, 삼합/방합, 원진 (궁합 핵심) | 미착수 |
| **L5** | 대운(성별+년간 순역), 세운, 월운 | 미착수 |
| **L6** | LLM 해석 — L1~L5의 구조화된 팩트를 문장화 | 미착수 |

**철칙: 계산과 해석을 분리한다.**
- 계산은 반드시 코드로. LLM에게 계산시키지 말 것 (실제 사례: 손으로 푼 구성원D 일간 乙木이 엔진 검증에서 甲木으로 정정됨)
- 정확도가 신뢰의 전부. 절입 시각·진태양시·야자시가 틀리면 커뮤니티에서 박제됨. KASI 데이터 또는 검증된 오픈소스 사용
- 해석 일관성: 관계 유형별 룰 템플릿 고정, LLM은 문장화만

**중요 특성**: 원국(타고난 8자)은 평생 불변, 매년 바뀌는 건 운(대운/세운) 레이어. → 데이터는 한 번 입력하면 영구 유효하고, "올해의 성계 예보"가 자연스러운 재방문/구독 포인트.

---

## 7. 데이터 모델 원칙

### 7.1 인물 ≠ 계정 (필수)
- 내가 등록한 "구성원B", "어머니"는 **인물 노드**(비회원, 내 데이터)
- 나중에 가입하면 자기 노드를 **클레임**할 수 있게 설계
- 이 분리가 QR 상호등록·공유 그룹의 전제

### 7.2 에고센트릭 우선 (v2 개정)
- 1단계는 "나 중심" — 모든 데이터가 내 계정 안에만 존재
- **~~나 없는 그룹도 생성 가능~~ → 폐지.** 나 = 은하의 유일한 항성이므로 나 없는 그룹은 항성 자리가 비어 모순. 그 용도는 **가상 성계**가 대체한다
- **~~기존 그룹에서 내가 빠지려 하면 막고 "그룹을 삭제하시겠습니까"로 유도~~ → 정정(2026-08-24):
  막기만 하고 삭제로 유도하지 않는다.** 빠져나가는 경로 자체를 없애면 "나만 빠진 그룹"이라는
  상태가 애초에 생기지 않아 삭제를 물을 상황이 없다. 나 없는 조합을 보고 싶은 수요는 **가상
  성계(§5.5)**가 이미 받으므로, 그룹을 지우게 만드는 건 사용자에게 손해다(그룹은 남기고 조합만
  따로 보면 된다). 그래서 안내 문구도 삭제 제안이 아니라 대안 제시다 —
  "나 없는 조합은 가상 성계에서 볼 수 있습니다".
- **[구현 완료, 2026-08-24]** 가드 3중 구성:
  1. **그룹 생성 시트(`openGrpSheet`)** — "나"를 `pickedMembers`에 기본 포함해 체크된 채로 열고,
     그 행에 `.locked`(금색 테두리 + "고정" 배지)를 붙여 클릭해도 해제되지 않게 했다.
     `presetIds`로 열리는 경로("관계 보기 → 그룹으로 저장", "가상 성계 → 은하로 가져오기")에서도
     나를 강제 합류시킨다 — §5.5의 "나 자동 합류"가 가상 성계 전용 규칙이 아니라 그룹 생성
     전체의 규칙이 됐다
  2. **인물 수정 시트(`renderFGroups`)** — 편집 대상이 나일 때, **이미 `g.members`에 들어 있는**
     그룹의 칩만 잠근다. 이 기준이 핵심으로, 같은 시트에서 방금 체크해 넣은(아직 저장 전) 그룹은
     되돌릴 수 있고 잠기는 건 진짜로 빠져나가는 경우뿐이다
  3. **`addbtn` 멤버십 동기화 / `grpbtn`** — UI를 우회해도 막히는 최종 방어선
     (`if(!want&&has&&!n.isMe)`, `grpbtn`은 2인 검사 **전에** 나를 합류시켜 "나+1명"이 정상
     통과하도록 함)
  이유는 모달(`alert`)이 아니라 시트 안 상시 노출 문구(`.guardnote`)로 보여주고, 잠긴 항목을
  누르면 그 문구가 깜빡인다 — 다중 선택 UI라 반복 탭마다 모달이 뜨면 흐름이 끊긴다.
- **[적용 범위 — 부작용을 인지하고 유지하기로 결정, 2026-08-24]** 부모-자식 구분 없이 **모든
  그룹**에 똑같이 적용한다. 자식 그룹은 3D 항성계가 아니라(§7.4 "부모-자식은 중첩으로 표현하고
  복제하지 않는다") "항성 자리가 빈다"는 근거가 직접 닿지 않지만, 위 결정 문구가 그룹 종류를
  나누지 않았고 `removeGroupAt`이 부모 삭제 시 자식을 조부모로 재부모화하므로 자식만 열어두면
  그 경로로 나 없는 최상위 항성계가 생긴다.
  **부작용**: "아이들(구성원C, 구성원D)" 같은 나 없는 하위 그룹을 만들 수 없다.
  **결정**: 이 부작용을 감수하고 현재 규칙을 유지한다 — 재부모화 경로로 나 없는 최상위가
  생기는 것을 막는 쪽이 우선이고, 그런 조합을 보고 싶은 수요는 **가상 성계**가 담당한다.
  ("자식 그룹은 예외" 규칙은 넣지 않는다. 나중에 뒤집고 싶으면 이 결정을 명시적으로 되돌릴 것 —
  실사용에서 걸렸다는 사례가 근거로 필요하다.)
- **[나 삭제 금지, 구현 완료 2026-08-24]** `delbtn`("이 사람 삭제")은 편집 대상이 나일 때
  **버튼 자체가 사라지고** 그 자리에 같은 톤의 이유 문구가 선다(`#del-guard`). 비활성 버튼으로
  남기지 않은 이유: 비활성은 "조건이 맞으면 언젠간 눌린다"는 인상을 주는데 이건 영구 금지다.
  근거는 그룹 이탈보다 강하다 — 나가 사라지면 `layoutGalaxy()`가 `if(!me)return`으로 그대로
  빠져나가 은하 전체가 성립하지 않는다. 핸들러 첫 줄에도 `if(editTarget.isMe)return`을 둬
  UI 우회를 막는다(confirm 창보다 앞이라 대화상자도 뜨지 않는다).
- **[데모 데이터 정합, 2026-08-24]** "그룹C"을 `[구성원H, 구성원G]` → `[나, 구성원H, 구성원G]` 3인으로
  바꿨다. `createGroup` 직접 호출이라 시트 가드에 안 걸려 **앱의 기본 데이터만 이 규칙을 위반한
  채** 남아 있었고, 그 상태를 두면 데모를 보는 사람이 규칙을 오해한다. 궤도 영향 없음 —
  §3.1의 R_g는 중심인물의 일간에서 나오는데 §2.2 점수 룰상 최고점자가 여전히 "구성원H"라
  대표 오행이 그대로다(R_g=9 유지, 실측).
- 공유 그룹은 Phase 3

### 7.3 프라이버시
- 생년월일시는 민감정보
- 시스템이 실존 인물에 대해 "이 사람이 긴장을 만든다"고 출력함 — **혼자 보는 건 괜찮지만 공유 시 문제**
- **Phase 1~2: 제한 없음.** 모든 데이터가 내 계정 안에만 있으므로 제3자 조합도 전면 허용
- **Phase 3에서 발효**: 클레임된 노드만 공개 범위를 따름. 자기 사주 공개 범위는 본인이 결정 / 미공개 시 상대에게는 오행·일간 등 라이트 정보만 / 부정 판정은 완곡 표현 / 상세 풀이는 1인칭 시점
- **지금 할 일**: 노드 스키마에 `claimedBy`, `visibility` 필드를 미리 넣되 로직은 붙이지 않는다. 나중에 마이그레이션을 피하기 위함 (§7.3 "설계 단계에서 반영, 나중에 붙이지 말 것")

### 7.4 복제 노드 (v2 신설) — **[구현 완료, 2026-08-24]**
- 인물은 데이터상 **1개**, 화면상 인스턴스는 **N개**(소속 그룹 수 + 가상 성계)
- 원국이 하나이므로 1:1 오행 관계는 어느 인스턴스에서 보든 동일. 달라지는 건 맥락 문장뿐
- 수정은 전 인스턴스 반영. ~~삭제는 두 액션으로 분리~~ — **의도적으로 이번 범위에서 제외.** "이
  그룹에서만 빼기"는 인물 수정 시트의 그룹 체크박스로 이미 가능했다(해당 그룹만 체크 해제).
  `delbtn`("인물 완전 삭제")의 의미도 다중소속과 무관하게 계속 명확했다 — 다중소속 렌더링이
  되려면 반드시 있어야 하는 선행조건이 아니라 별도 UX 편의(원클릭 단축) 개선으로 판단해 미룸
- `showNode(n)` → `showNode(n, groupContext, targetMesh)`로 확장. **둘 다 선택 인자**라 기존
  호출부는 전부 무변경으로 그대로 동작(`groupContext`는 `activeGroup`으로, `targetMesh`는 `n.mesh`
  로 폴백). 형제 그룹 다중소속의 추가 인스턴스를 클릭했을 때만 클릭 핸들러가 명시적으로 넘겨
  "가족의 구성원B를 누르면 가족 맥락, 그룹B의 구성원B를 누르면 그룹B 맥락"을 만족시킨다
- **부모-자식 그룹은 중첩으로 표현하고 복제하지 않는다.** 자식 그룹 멤버는 자식 안에만 표시. 형제 그룹 간 다중소속만 복제
- 같은 인물의 복제끼리 얇은 점선(동일인 링크)으로 연결 — 줌아웃에서 "구성원B가 가족과 직장 양쪽에 있다"가 한눈에 보임

**구현 메모** — 설계·상세 근거는 GALAXY-LAYOUT-SPEC §3.7 참고. 요약:
- "주 인스턴스(기존 n.mesh/n.orbit 그대로) + 추가 인스턴스(§5.5 스타일 참조 클론)" 구조 — 그룹
  1개 소속(압도적 다수)은 코드 경로가 지금과 완전히 같다
- 고리는 §5.5와 다르게 조합 내부 재계산이 아니라, `rebuildRings()`(은하 전체 기준)가 이미 계산한
  개수·색을 인스턴스에 복제(그 사람의 진짜 발신량은 어느 그룹에서 보든 같으므로)
- 검증용 데모 데이터 "그룹B"(나·구성원B·구성원E, 기존 인물만 사용) 추가 — 구성원B가 그룹A·동네
  모임 형제 그룹 다중소속의 실제 사례가 됨
- 동일인 점선은 픽셀 히스토그램 실측(불투명도 1.0까지 올려도) bright/mid 변화가 감지 한계
  이하 — 1px 선이라 화면 전체 대비 차지하는 픽셀 수 자체가 극히 작기 때문. LOD는 붙이지 않음

---

## 8. 서비스 로드맵

**Phase 1 — 도구** (APLA 종료 후 착수, 설 연휴 2027.2 런칭 목표 → 10월 착수 / 12월 MVP / 1월 베타)
- 회원가입 없는 웹앱, 로컬 저장. 만세력 L2~L3 + 은하 UI. 결과는 공유 이미지
- "설날 가족 성계 뽑기" 콘텐츠로 시장 반응 테스트

**Phase 2 — 계정**
- Supabase 인증, 내 은하 저장, 유료 리포트(L4~L5 + LLM 해석 PDF). 에고센트릭 유지
- **토큰 경제 → §8.1** · **운영자 대시보드 → §8.3** · **주기적 콘텐츠 → §8.4**

**Phase 3 — 네트워크**
- QR 상호등록, 노드 클레임, 공유 그룹. §7.3 프라이버시 규칙이 여기서 발효
- 여기서부터 "관계 SNS" 카테고리
- **사용자 간 연결(전화번호·QR) → §8.2**

**수익 구조**: 개인 무료 → 관계 보기 일부 무료 → **그룹 풀리포트 유료**(건당 5,900~9,900원 또는 연운 구독). 명절이 성수기, 공유 이미지가 바이럴 채널.

---

### 8.1 토큰 경제 — 지급과 소모 규칙 [2026-08-28 결정]

> **지금 되는가 — 아니오.**
> **선행: 서버 + 로그인.** 브라우저 저장만으로는 **원리상 불가능하다** — 저장소를 지우고
> 다시 시작하면 무료분을 계속 받는다(우리가 만든 "모두 지우기" 버튼이 바로 그 경로다).
> 잔액을 클라이언트에 두는 한 막을 방법이 없다. **Phase 2**에 속한다.

**지급**
- **최초 가입 시 3명분.**
- **추천인 제도** — 추천인을 입력하면 **입력한 사람과 추천인 양쪽에 각 1명분.**
  **초대한 쪽도 받는 것이 요점**이다. 한쪽만 주면 퍼뜨릴 동기가 없다.

**소모**
- 등록은 1명분.
- **수정은 부분 소모**(1/2 또는 1/3명분). **비율은 미정** — 정할 때 근거를 함께 적을 것.
- 예외로 둘 후보: **QR 대면 교환은 토큰 없이**(§8.2 — 대면 확산을 유도하는 자리다).

**★ 기록 구조 — 잔액만 저장하지 말 것**

잔액 하나만 두면 **과거를 소급할 수 없다.** "언제 · 누가 · 몇 개를 · 왜 받고 썼는지"를
**건별로** 남기고(원장 방식), **잔액은 그 기록의 합으로 구한다.**
- 이렇게 두면 운영자 대시보드(§8.3)는 **읽어서 보여주기만** 하면 된다.
- 반대로 잔액만 두면, 대시보드를 붙이려는 시점에 **과거가 이미 없다.**
- 이건 새 원칙이 아니다 — 경도(`lon`)·성별(`sex`)·시각(`createdAt`)을 쓰지도 않으면서
  미리 심어 둔 것과 **같은 논리**다. **나중에 심으면 그 값을 영영 못 받는다.**

**★ 환불 규정 — 토큰을 설계할 때 함께 정한다**

**결제가 붙는 순간 환불 규정이 법적으로 필요해진다.** "이미 사용한 토큰은 환불 불가" 같은
규칙을 **나중이 아니라 설계 시점에** 정한다. 건별 기록이 있으면 "얼마를 언제 썼는가"가
증명되므로 그 규정을 **집행할 수 있다** — 규정과 기록 구조는 한 몸이다.

---

### 8.2 사용자 간 연결 — 전화번호와 QR [2026-08-28 결정]

> **지금 되는가 — 아니오.**
> **선행: 서버 + 로그인.** QR 교환은 **서로 다른 브라우저 사이의 데이터 교환**이라
> 서버 없이 성립하지 않는다(6단계 백업 파일이 사람 손을 거쳐 옮겨지는 것과 다르다).
> **Phase 3**에 속한다.

**목적** — 내가 등록해 둔 "아내"와 **실제 아내의 계정을 같은 사람으로 잇는다.**
§7.1("인물 ≠ 계정 … 나중에 가입하면 자기 노드를 클레임")이 여기서 실물이 된다.

**왜 전화번호인가** — 이름은 **동명이인**이 있고 생년월일도 **겹칠 수 있다.**
**전화번호는 유일하다.** 카카오 로그인이면 번호를 받을 수 있어 **자동 매칭도 가능**하다.

- **필수가 아니라 선택.** 열 명을 등록하는데 번호까지 요구하면 **포기율이 올라간다.**
  넣은 사람만 나중에 연결된다 — 안 넣었다고 잃는 것은 없다.
- **★ 알림 방향에 주의.** "누가 당신을 등록했습니다"를 **미가입자에게 보내면 스팸**이다.
  상대가 **가입한 시점에** "이미 세 분이 당신을 등록해두셨어요"로 알린다.
  안전하고, 오히려 **반가운 발견**이 된다.

**QR 프로필 교환** — 실제로 만났을 때 서로 등록한다. **토큰 없이 가능하게** 해서
**대면 확산**을 유도한다.

**NFC는 검토했으나 제외한다.** 웹 NFC는 **안드로이드 크롬 전용**이고 **iOS는 지원하지
않는다.** 게다가 양쪽이 페이지를 열고 있어야 해서 QR보다 번거롭다.
**"만나서 QR 찍기"만으로 같은 효과**가 난다.

---

### 8.3 운영자 대시보드 [2026-08-28 결정]

> **지금 되는가 — 아니오.**
> **선행: §8.1 토큰 시스템(= 서버 + 로그인).** 볼 대상이 없으면 화면도 없다.

토큰 **발행량 · 소비량 · 환불량 · 수익**을 본다. **감으로는 알 수 없는 값들**이다.

§8.1대로 토큰 기록을 **건별로** 남기면, 이 화면은 **그 기록을 읽어 보여주기만** 하면 된다.
즉 **이 항목의 난이도는 대부분 §8.1의 기록 구조에서 이미 결정된다** — 여기서 새로 설계할
것은 거의 없고, 반대로 §8.1이 잔액만 남기면 이 화면은 만들 수가 없다.

---

### 8.4 주기적 콘텐츠 — 재방문 유도 [2026-08-28 결정]

> **지금 되는가 — 아니오.**
> **선행: 만세력(L2) → 대운·세운(L5).** 지금 엔진은 **태어난 날만** 계산해
> **시간 축이 아예 없다** — `dayStem(y,m,d)`이 전부이고, "이번 달"이라는 개념이
> 코드에 존재하지 않는다.

**주기는 월 단위까지가 자연스럽다.** 일진(하루 단위)이 명리에 있기는 하나,
**매일 다른 얘기를 하려면 근거가 얇아진다** — §2.9가 금지한 신비주의로 미끄러지는 자리다.

근거는 §6에 이미 있다: **"원국(타고난 8자)은 평생 불변, 매년 바뀌는 건 운 레이어"**,
그래서 "올해의 성계 예보"가 자연스러운 재방문·구독 포인트가 된다.
관련 백로그 두 개가 같은 선행 조건을 공유한다 — **"근일점 위상 = 세운 연동"**(L5 이후),
**"시기 기반 표식"**(L5 도달 후 재검토).

---

## 9. 작업 방식 (토큰 절약 — 반드시 지킬 것)

- **수정은 `str_replace` 부분 편집으로.** 전체 재생성은 구조가 근본적으로 바뀔 때만
- **요청은 모아서 한 턴에.** 3건을 한 번에 주는 것이 3턴에 나누는 것보다 훨씬 저렴
- **설명은 짧게** — 바뀐 것만 요약
- **검증(구문·초기화 체크)은 큰 수정 후에만**
- **§5.1 은하 재구현은 규모가 커서 부분 편집으로 감당이 안 될 수 있음. Claude Code 이관을 권장**

### 9.1 기술 검토 시 규칙
사실(문서/코드에서 확인된 것) / 추론(가정이 섞인 것) / 의견(평가)을 명시적으로 구분해 표기할 것. 명세를 모르는 시스템에 대해 표준 아키텍처의 가정을 근거로 단정하지 말 것. 확인 가능한 사항은 결론이 아닌 질문 형태로 제시할 것.
