# 사주은하 — 작업 규칙

저장소 `group-saju`. 서비스명 **사주은하는 확정**이다(2026-08-31, 근거는 HANDOFF §0.5).
[2026-08-30] ~~그룹사주~~ → **사주은하**. **표기만 바꾼다** — 저장소 이름 `group-saju`,
배포 주소, `localStorage` 키(`groupsaju:*`), `BACKUP_APP='group-saju'`, 백업 파일명 접두어는
전부 그대로다. 건드리면 기존 사용자의 저장 데이터와 이미 내려받은 백업 파일이 깨진다.

**[2026-09-04] 작업 폴더가 옮겨졌다.** 저장소 사본은 이제
`D:\work\web service\사주은하\sajugalaxy.com\` 에 있다.
**이 저장소 사본의 옛 경로는 기록이 없다** — `.git/config`에도 남지 않아 확인하지 못했다(2026-09-04).
아래 `Galaxy saju`는 이 저장소가 아니라 **별개의 아카이브 저장소**다.
**폴더 이름일 뿐이다** — 저장소 이름(`group-saju`)·원격(`https://github.com/shyquisyz/group-saju.git`)·
배포 주소는 그대로다. 폴더 이름 `sajugalaxy.com`은 아직 **도메인이 아니다**
(2026-09-04 실측: `CNAME` 파일 없음, `https://shyquisyz.github.io/group-saju/` 가 리다이렉트 없이 200).
- **경로에 한글과 공백이 둘 다 들어 있다.** 명령에 쓸 때는 반드시 통째로 따옴표로 감싼다 —
  bash `cd "D:/work/web service/사주은하/sajugalaxy.com"`, PowerShell `Test-Path "$p\index.html"`.
  (2026-09-04 두 셸에서 실행해 확인)
- **한글이 든 검색·치환 문자열은 명령줄에 직접 쓰지 말고 파일로 넘긴다** —
  `grep -F -f 패턴파일`, 또는 파이썬/heredoc 스크립트. 셸·콘솔 인코딩을 거치면서 깨진다.
- **아카이브 저장소도 함께 옮겨졌다**: 옛 `Galaxy saju` → `D:\work\web service\사주은하\구사주은하\`.
  거기에 은유 레퍼런스 `1.png`(甲)·`2.png`(戊)·`3.png`(辛)이 있다.
  이 저장소에는 없다 — `.gitignore`가 막는다.

## 파일 구성

HTML 1개 + 데이터 3개 + 문서 3개 + 생성기 1개. **앱은 빌드·의존성 설치가 없다.**

| 파일 | 역할 |
|---|---|
| `index.html` | 구현 전부. Three.js r128(CDN) + 바닐라 JS |
| `solar-terms.js` | 24절기 절입 시각 표(1900~2100, 4824건). **자동 생성 · 손으로 고치지 말 것** |
| `my-data.js` | 실제 인물. **`.gitignore`** — 없으면 첫 화면(자기 등록)으로 시작한다 |
| `*-backup-*.json` | 앱의 "파일로 저장"이 만드는 백업. **`.gitignore`** — 아래 ★ 참고 |
| `HANDOFF-v2.md` | 프로젝트 인계 문서 — 컨셉·로드맵·다음 작업 |
| `GALAXY-LAYOUT-SPEC.md` | 은하 레이아웃 사양서 — 좌표·궤도·카메라 수치 |
| `README.md` | 저장소 첫 화면 |
| `tools/gen-solar-terms.mjs` | 위 표의 생성기. **손으로 돌린다** — `npm i lunar-javascript@1.7.7` 필요 |

**`solar-terms.js`는 선택 파일이 아니다.** `my-data.js`는 없어도 앱이 돌지만, 이건 없으면
사주 계산이 성립하지 않는다. `index.html`보다 **위에서** 적재해야 한다(`window.SOLAR_TERMS`).

**생성기의 의존성은 앱의 의존성이 아니다.** `tools/`는 표를 다시 만들 때만 손으로 돌리는
별개 도구다. 배포되는 것은 생성 **결과**뿐이고, 브라우저는 여전히 아무것도 설치하지 않는다.
그래도 생성기를 저장소에 남기는 이유는 — 표 24,120자는 사람이 읽을 수 없어서,
이 파일이 없으면 다음 세션이 **"그 값이 어디서 왔는가"를 확인할 방법이 없다.**

**★ 개인정보** — 실제 인물의 생년월일·출생시각·출생지·성별은 `my-data.js`에만 둔다.
HTML이나 커밋 메시지에 적지 말 것. **한 번 올라가면 커밋 기록에 남아 되돌릴 수 없다.**

**★ 백업 파일도 같은 급이다 [2026-08-28 추가 — 실제로 밟고 넣었다].**
앱의 "파일로 저장"이 만드는 `group-saju-backup-*.json`에는 등록한 사람들의 생년월일이
그대로 들어 있다. **브라우저는 마지막에 쓴 다운로드 폴더에 저장하므로, 저장소를 열어 두고
내보내기를 누르면 저장소 루트에 떨어진다.** 실제로 그랬고 `git add -A`가 그걸 커밋에까지
넣었다(푸시 전에 발견해 되돌렸다).
- `.gitignore`가 막고 있다. **`git add -A` 전에 `git status`를 눈으로 볼 것.**
- 이 위험은 `my-data.js`와 성질이 다르다 — 그건 사람이 만드는 파일이라 안 만들면 그만이지만,
  **이건 앱이 만든다.** "데이터를 없애라"는 규칙만으로는 못 막는다.

## 문서 규칙

**문서 상태 표시를 바꿀 때는 요약 표와 본문을 항상 함께 갱신한다.**

`HANDOFF-v2.md` §0 표와 `GALAXY-LAYOUT-SPEC.md` §6·§7 목록은 본문 절(§5.x, §7.x, §3.x)의
**요약**이다. 본문을 `[미구현]` → `구현 완료`로 고치면서 요약 쪽을 그대로 두는 사고가
세 번 반복됐다(2026-08-24 실사에서 §0 표의 "다중소속", "고리 가상 성계 예외" 두 행이
본문과 어긋난 채 발견됨). 한쪽만 고치면 다음 세션이 낡은 쪽을 믿고 이미 구현된 걸
다시 만들거나, 안 된 걸 됐다고 보고한다.

- 상태 표시를 고쳤으면 **커밋 전에 다른 문서에서 같은 항목을 grep**해 함께 고쳤는지 확인할 것
- 실제 구현 상태가 갈리면 **본문 절이 우선**한다. 다만 그건 "요약을 무시해도 된다"가 아니라
  "요약을 본문에 맞춰 고쳐라"라는 뜻이다
- `[미구현]`/`[부분구현]`/`[구현 완료]`를 붙이거나 뗄 때는 **코드로 확인한 근거**(함수명·
  줄 위치·실측값)를 같이 적는다. 문서만 보고 상태를 옮기지 말 것

## 전역 선언 순서 — const TDZ

**새 `const`/`let` 전역을 추가할 때는 파일 상단 선언 구역에 둔다.**

함수는 호이스팅되지만 `const`는 안 된다. 초기 데이터 생성 코드(`layoutGalaxy`, `createGroup`,
`rebuildRings` 등을 부르는 구역)보다 아래에 선언하면, 그 함수들이 TDZ 오류로 **조용히 멈춘다** —
예외가 콘솔에만 찍히고 화면은 "그냥 좀 비어 보이는" 상태가 되기 때문에 놓치기 쉽다.
`typeof` 가드도 TDZ는 못 피한다(TDZ 변수에 `typeof`를 쓰면 그것도 던진다).

이 코드베이스에서 세 번 밟았다 — `curOff`, `panelEl`(둘 다 `panel` 참조), `MAX_GROUP_DEPTH`.
단일 HTML에 선언 순서와 실행 순서가 뒤섞여 있어 구조적으로 반복된다.

**거울상도 있다 — 초기화 코드를 위로 올리는 것.** [2026-08-27] 데이터를 시드 파일로 빼면서
`applySeed()` 호출을 파일 위쪽에 뒀더니, `createGroup` → `renderNav` → `renderTagbar`가
아직 초기화 안 된 `const tagbarEl`을 읽어 **TDZ 오류로 조용히 멈췄다.** 그룹이 7개가 아니라
1개만 생기고 블룸 컴포저도 null이 됐는데 화면은 "그냥 좀 비어 보이는" 상태였다.
구 구현이 사람은 위(3564행)·그룹은 아래(3967행)에서 만들던 게 우연이 아니었다.
**선언을 아래 두지 않는 것만큼 초기화를 위로 올리지 않는 것도 지켜야 한다.**

**추가 후 반드시 브라우저에서 초기 로드를 확인할 것.** 데모 그룹 수·노드 수가 기대값인지가
가장 빠른 신호다(정상: `groups.length === 4`, `nodes.length === 8`). `MAX_GROUP_DEPTH` 때는
그룹 4개가 0개가 된 것으로 발견했다.

## 은유를 그림으로 옮길 때 — 방식부터 맞춘다

**레퍼런스 한 장이 열두 번의 시도보다 싸다.**

`STEM_BLURB` 같은 은유를 3D로 옮기는 작업은 방식이 셋이다 — **표면**(텍스처만) /
**구슬 안**(겉면을 반투명하게 열고 그 안에 3D) / **표면 밖**(겉면은 불투명, 3D가 바깥으로 솟음).
**방식이 틀리면 그 안에서 아무리 고쳐도 안 된다.** 모양·색·개수·간격을 아무리 바꿔도
같은 계열의 실패로 수렴한다.

실제로 그랬다 —
- **甲**(큰 나무): 표면에 나무를 그리는 방식으로 세 번 실패했다(너무 작다 → 너무 밝다 →
  극 왜곡). `1.png`(유리구슬 안 테라리움)를 받고 **구슬 안** 방식으로 바꾸자 풀렸다.
  (레퍼런스 세 장은 `D:\work\web service\사주은하\구사주은하\` 에 있다 — 2026-09-04 실측)
- **戊**(큰 산): 표면 밖으로 솟은 산맥으로 **열두 번** 실패했다(성게 / 두리안 / 딱지 / 이빨 /
  호박등 / 초콜릿 / 감자 / 납작함 / 얼룩 / 빈 정면 / 호두 / 소행성). `2.png`(유리구슬 안
  알프스)를 받고 보니 **없는 문제를 풀고 있었다** — 원한 것은 "표면에 산이 솟은 행성"이 아니라
  "구슬 안에 담긴 큰 산"이었다. 방식을 바꾸자 두 번 만에 됐다.

그러니 —
- **세 번 고쳤는데 같은 계열의 실패가 반복되면 방식을 의심한다.** 열두 번은 너무 늦다.
  "이번엔 뾰족해서, 이번엔 뭉툭해서"가 아니라 **"공에 뭔가 붙어 있다"에서 못 벗어난다**가
  신호다. 증상이 아니라 **증상의 공통점**을 본다.
- **막히면 레퍼런스를 요청한다.** 추측을 열 번 더 하는 것보다 이미지 한 장을 묻는 게 싸다.
  이건 실패가 아니라 정보 요청이다.
- 10번째 시도의 진단이 실측으로 뒷받침돼도(戊: +Z 정점 최대 반경 1.076) **방식이 틀렸으면
  그 진단으로는 못 푼다.** 진단이 맞다는 것과 문제가 맞다는 것은 다르다.

## 명령 결과가 아닌 것을 결과처럼 말하지 않는다

**명령 결과를 얻지 못했으면 얻지 못했다고 보고한다. 추측한 값을 결과인 것처럼 제시하지 않는다.**

특히 **계정명·주소·경로·ID처럼 사용자가 그대로 쓸 값**은 반드시 **명령 출력에서 읽은 것**이어야
한다. 형식이 뻔해 보여도(`https://<계정>.github.io/<저장소>/` 같은) 조합해서 내놓지 않는다.
사용자는 그 값을 그대로 주소창에 넣거나 남에게 전달한다 — 틀리면 그 사람의 시간이 날아가고,
더 나쁘게는 **틀렸다는 사실 자체를 한참 뒤에야 안다.**

지켜야 할 것:
- 명령이 실패하면 **실패한 출력을 그대로** 붙인다. 요약하거나 "안 됩니다"로 바꾸지 않는다.
- 확인하지 않은 것은 "확인했다"고 쓰지 않는다. **안 한 것과 못 한 것을 구분해서** 적는다.
- 값을 유추해 보여줘야 할 때는 **유추라고 명시**하고, 그 값을 결론에 쓰지 않는다.
  가장 안전한 것은 아예 안 쓰는 것이다.
- 보고 전에 한 번 되짚는다 — **"이 문장의 근거가 되는 명령 출력이 이 대화 안에 있는가?"**
  없으면 그 문장을 지운다.

## 공개될 파일에서 민감 정보를 지울 때 — 감추지 말고 없앤다

**접근을 막는 것으로 충분하지 않다. 데이터 자체를 없애야 한다.**

조건문·플래그·별도 파일 분리는 **파일을 여는 사람에게 무력하다.** 공개 저장소의 파일은
누구나 그대로 내려받아 읽는다 — 화면에 안 나온다고, 기본값이 아니라고, 다른 파일에 있다고
해서 안 보이는 게 아니다. 커밋 기록에 들어가면 파일을 지워도 되돌릴 수 없다.

**제거 후 반드시 `grep`으로 0건임을 확인한다.** 눈으로 훑지 말고 명령으로 세고, 그 출력을
보고에 그대로 붙인다(위 "명령 결과가 아닌 것을 결과처럼 말하지 않는다" 참고).

무엇을 세는지 미리 정해 둔다 — **연도 · 도시명 · 인물 이름 · 그룹명**이 기본이고,
데이터 구조 자체(`birth:{y:`, `name:'` 같은 것)도 함께 본다.

두 가지를 구분할 것:
- **지워야 하는 것**: 실제든 가공이든 **사람 정보의 형태를 띤 값**. 가공 데이터라도 파일을
  여는 사람에게는 "가공"이라는 표시가 안 보인다 — 실제로 오해를 산 적이 있다(2026-08-28).
- **남겨야 하는 것**: **앱 기능**에 쓰이는 값. 출생지 드롭다운의 도시 목록 60개,
  그룹 유형 일곱(연인·부부·가족·친척·친구·동료·기타) 같은 것. 이건 개인정보가 아니라
  선택지다. grep에 걸리더라도 지우면 앱이 깨진다 — **왜 남겼는지 보고에 함께 적는다.**

설계 이력 주석에 남는 데모 인물 이름도 정리한다. 개인정보는 아니지만 파일을 읽는 사람이
실제 데이터로 오해한다 — `구성원B` `그룹A`처럼 중립 표현으로 바꾼다.

## 배포 주소는 사용자의 데이터가 사는 곳이다 — 건드리기 전에 확인한다

**배포 주소(https://shyquisyz.github.io/group-saju/)에서 검증할 때는 반드시 순서를 지킨다.**
그 주소의 `localStorage`에는 **사용자가 실제로 넣은 사람들이 들어 있다.** 로컬 사본과 달리
여기서 지운 것은 되돌릴 곳이 없다 — 서버가 없다.

```
1) localStorage.getItem('groupsaju:save') 를 먼저 읽는다
2) 있으면  backupText() 를 통째로 챙겨 둔다  ← 검증 전에. 예외 없이
3) 검증한다
4) 끝나면 챙겨 둔 내용으로 되돌린다
```

- **저장본이 있으면 온보딩(`#ob-start`)을 누르지 않는다.** 첫 화면이 안 떠 있어도 그 버튼은
  눌리고, 그러면 **`isMe`가 둘이 된다**(§2.1 위반). 화면으로 안 보이는 사고다.
- **`localStorage.removeItem` 계열을 검증 정리로 쓰지 않는다.** 정리하려면 3)에서 챙긴
  내용으로 **되돌린다**. 지우기는 사용자만 한다.
- 데이터를 마음껏 지워도 되는 곳은 **`my-data.js`를 뺀 로컬 사본**(포트 8902)뿐이다.

**2026-08-28에 실제로 밟았다.** 표 입력을 배포본에서 검증하며 저장본 확인을 건너뛰었다.
사용자가 32분 전에 등록한 인물이 있었는데 그 위에 검증용 5명을 얹었고(그 과정에서 `isMe`가
2명이 됐다), 마지막에 "정리"한다고 `groupsaju:save`를 지웠다. 메모리에 원본이 남아 있어
`birth`·`createdAt`까지 그대로 복구했지만, **새로고침이 한 번만 더 있었으면 복구 못 했다.**
같은 세션의 앞선 검증에서는 "저장본: 없음"을 먼저 확인하고 시작했다 — 절차를 아는데
한 번 빼먹은 것이라 규칙으로 고정한다.

## 저장 항목을 바꿀 때는 변환을 함께 넣는다

**`serializeState()`가 쓰는 필드를 늘리거나 · 이름을 바꾸거나 · 의미를 바꾸면, 같은 커밋에서
`MIGRATIONS`에 함수를 더하고 `SAVE_VER`을 1 올린다. 없으면 기존 사용자의 데이터가 안 열린다.**

변환이 없으면 `loadSave`가 `migfail`로 떨어진다. 앱은 저장을 끄고 원본을 보존하지만,
**사용자 화면에는 자기 데이터가 안 뜬다.** 서버가 없어서 우리가 대신 고쳐 줄 방법도 없다.

- **필드를 더하기만 해도 변환이 필요하다.** 없는 필드를 읽는 코드가 `undefined`로 조용히
  도는 것이 더 나쁘다. 성별을 나중에 넣었다면 옛 인물은 `sex: null`이어야 한다 —
  "없다"와 "모른다"를 구분해 둔 것이 여기서 값을 한다.
- **빠진 필드를 채울 때 "없음"으로 때우지 말 것.** 알 수 있는 값이면 계산해 넣는다.
  2026-08-28에 밟았다 — `lon`을 `null`로 채웠더니 `applySeed`가 `lon !== undefined`일 때만
  파일 값을 쓰고 아니면 도시 목록을 조회하는데, **`null`은 `undefined`가 아니라 조회를
  건너뛴다.** `city`가 '서울'인데 경도가 `null`로 굳었다(L2 진태양시 보정이 쓸 값이다).
- **변환 함수는 입력을 고치지 말고 새 객체를 돌려준다.** 체인 중간에서 실패했을 때 원본이
  성해야 `migfail`이 원본을 보존할 수 있다.
- **넣었으면 옛 저장본을 실제로 심어서 열어 본다.** 문서만 보고 됐다고 하지 말 것 —
  이 경로는 배포하고 몇 달이 지나서야 처음 돈다.
  `localStorage.setItem('groupsaju:save', JSON.stringify({v:0, people:[…]}))` 후 새로고침.
- **`MIGRATIONS`를 비워 두지 않는다.** 키가 하나도 없으면 `migrate()`의 루프가 0바퀴 돌아
  **한 번도 실행된 적 없는 코드**가 된다. 그래서 `'0'`(v0→v1)을 실제 내용으로 채워 뒀다 —
  세상에 v0 저장본은 없지만 체인을 태워 볼 수 있어야 한다.
- **버전을 올리면 옛 배포본에서 `newer`가 되어 저장이 꺼진다.** 코드를 먼저 배포한다.
- **백업 파일도 같은 체인을 탄다**(`parseBackup` → `migrate`). 반년 전에 내보낸 파일이
  오늘 앱에서 열려야 하므로, 변환을 빠뜨리면 저장본만이 아니라 **백업까지 못 읽는다.**

## 검증 규칙

- 수치·동작 주장은 **실측**으로 뒷받침한다(픽셀 히스토그램, `getBoundingClientRect`,
  콘솔 측정). 문서와 커밋 메시지에 측정 방법과 값을 남긴다
- 브라우저 확인이 필요하면 `python -m http.server`로 로컬 서빙 후 접속한다
  (`file://`는 확장 도구가 접근하지 못한다). ~~확인이 끝나면 서버를 내린다~~
  → **[2026-08-27 정정] 서버는 켜 둔 채로 끝낸다.** 작업이 끝나면 사용자가 같은 주소로
  직접 테스트한다. 내려 버리면 사용자가 매번 다시 띄워야 한다.
  **[2026-08-28] 포트**: `group-saju`(이 저장소, 실인물 데이터 `my-data.js` 포함)를
  **8900**에 서빙한다. 구 포트 8899는 아카이브 저장소라 파일 이름이
  예전 것(`relation-saju-stellar-v07.html`)이다 — 거기서 확인하면 낡은 앱을 보게 된다.
  **[2026-09-04] 아카이브 위치 갱신**: ~~`D:\work\web service\Galaxy saju\`~~ →
  `D:\work\web service\사주은하\구사주은하\`.
  데이터 없는 첫 실행 확인은 `my-data.js`를 뺀 사본을 따로 서빙해서 한다
  (실제 저장소에서 `my-data.js`를 지우면 사용자 데이터가 날아간다)
- **한 장면에서 정한 값은 다른 장면에서 검증한다.** 이 앱에는 축척이 크게 다른 장면이 둘
  있고(은하 조망 카메라 ~250 / 성계 장면 ~40~60), 한쪽에서 좋아 보이는 값이 다른 쪽에서
  깨지는 일이 반복됐다:
  - **블룸 문턱**(2026-08-27) — 성계 장면에서 과노출 픽셀 수로 0.55/0.55/0.78을 잡고,
    은하 조망(항성 30px)에서 다시 확인해 통과시켰다.
  - **멤버 궤도 반경**(2026-08-27) — 성계 장면 크기를 맞추려 절대값으로 바꿨는데, 은하 조망의
    항성·행성 픽셀이 안 변한다는 것을 따로 재서 확인해야 했다(카메라 267.4 → 267.4).
  - **`PLANET_SCALE`** — 성계 장면만 보고 올리면 은하 조망의 행성이 같이 커진다(6.4 → 13~18px).
    그래서 배율 조정을 궤도 반경 쪽에서 했다.
  포커스(사람 클릭, ~50px)까지 셋으로 보면 더 안전하다.
- 데스크톱/모바일 분기(`@media (min-width:768px)`)가 있는 변경은 **양쪽 다** 확인한다.
  창 리사이즈가 안 먹는 환경에서는 해당 크기의 iframe으로 미디어 쿼리를 태울 것

## 3D 화면 측정 함정

**측정 전 반드시 확인할 것.** 이 코드베이스에서 잘못된 측정값 때문에 진단이 통째로 틀어진
적이 세 번 있다. 두 번은 `updateMatrixWorld` 누락으로 raycast·라벨 측정이 어긋났고,
세 번째(2026-08-25)는 rAF 정지 상태에서 잰 값으로 UI 진단 전체를 잘못 보고했다.

- **`document.visibilityState`가 `hidden`이면 rAF가 완전히 멈춘다** — 카메라가 미수렴
  상태다. `camTick`을 명시적으로 여러 번 돌려 수렴시킨 뒤 측정할 것.
  실측 근거: 백그라운드 창에서 2.3초간 `requestAnimationFrame` 콜백이 **0회**였다.
  그 상태에서 잰 `cam.r`은 92.5(`fitAll` 목표 166.3에 수렴 전)였고, 그 거리로 계산한
  "행성 8.16px / 항성 47.2px"를 그대로 보고했다. 실제 값은 2.99~3.57px / 21.2px다.
  `cam.theta`/`cam.phi`를 바꿔도 `camera.position`이 갱신되지 않아, 시야를 4개로 바꿔가며
  잰 히스토그램이 **네 번 모두 같은 값**으로 나왔던 것도 같은 원인이다.
- **`simulate()`는 인자를 안 받는다 — `simulate(1/60)`은 조용히 dt=0으로 돈다.**
  `simulate()`는 전역 `t`와 `simPrevT`의 차이로 dt를 만든다. 그런데 `t`를 미는 것은 rAF 루프이고,
  창이 hidden이면 rAF가 멈춰 있으므로 **`t`가 안 흐른다**. 측정 스크립트에서 `simulate(1/60)`을
  아무리 돌려도 인자는 버려지고 dt=0이라 시간이 지나는 모든 것이 정지한다 —
  성계 끌어당김(`pull`), 멤버 공전, 페이드가 전부 초기값에 멈춘 채로 측정된다.
  `camTick(dt)`는 인자를 받으므로 카메라만 수렴해, **카메라는 맞고 장면은 안 맞는** 상태가 된다.
  이게 가장 고약하다 — 화면이 그럴듯해서 틀린 줄 모른다.
  **2026-08-27에 이걸로 틀린 값을 보고했다**: 그룹별 천체 크기 편차를 "6.3배"로 보고했는데,
  성계가 원점으로 안 끌려온 상태(`pull` 0)에서 잰 값이었다. 바르게 재니 **4.5배**였다.
  올바른 방법 — `for(let i=0;i<N;i++){t+=1/60; simulate(); camTick(1/60);}`
  **확인법**: 활성 그룹의 `pull`이 1인지 본다. 0이면 시간이 안 흐른 것이다.
- **`performance.now()`로 렌더 시간을 재면 CPU 제출 시간만 잰다 — `gl.finish()`가 필요하다.**
  WebGL 호출은 큐에 쌓이고 GPU가 나중에 처리한다. `renderer.render()`를 감싸 시간을 재면
  **명령을 큐에 넣는 시간**만 남는다. 2026-08-27에 이걸 밟았다 — 블룸 비용을 재는데
  **더 많은 일을 하는 쪽이 더 빠르게** 나왔다(라벨 별도 패스 0.334ms < 라벨 포함 0.813ms).
  순서가 뒤집히면 그건 거의 항상 이 함정이다.
  올바른 방법 — 워밍업 몇 프레임 → `gl.finish()` → 타이머 시작 → N프레임 → `gl.finish()` → 타이머 끝.
  고치고 나니 0.138 / 0.237 / 0.278ms로 순서가 맞았다.
- **`renderer.render()` 없이 `project()`나 raycast를 쓰면 `matrixWorld`가 낡아 값이 튄다.**
  `camera.updateMatrixWorld(true)`를 강제할 것(`true` = 자식까지 강제 갱신).
- **측정값이 예상과 크게 어긋나면 코드를 의심하기 전에 측정 조건부터 재확인한다.**
  위 세 건 모두 코드가 아니라 측정 쪽이 틀렸다.
- **파일을 고친 뒤에는 캐시를 깨고 다시 연다** — `?v=<아무거나>`를 붙일 것.
  `python -m http.server`가 `Last-Modified` 기반 304를 돌려주기 때문에, 주소가 같으면 브라우저가
  **옛 파일을 그대로 다시 쓴다.** 2026-08-27에 이걸 밟았다 — envMap·림라이트·metalness를 고치고
  잰 첫 측정이 `scene.environment: false`, 조명 2개, metalness 0.4로 나와 "패치가 안 먹었나"를
  한참 뒤졌는데, 파일은 멀쩡했고 브라우저가 옛 스크립트를 돌리고 있었다.
  **증상**: 방금 넣은 전역/튜너블이 `undefined`이거나 옛 값 그대로다. 코드를 의심하기 전에 이걸 볼 것.
- **화면 비율에 의존하는 지표는 캔버스 크기를 함께 고정하고 기록한다.** 픽셀 히스토그램이
  그렇다 — 같은 텍스처·같은 카메라인데 캔버스 폭만 2560→2180으로 바뀌어도 중간톤이
  14.87%→15.14%로 움직인다(2026-08-25 실측). 데스크톱 패널 개폐만으로도 캔버스 폭이
  달라지므로, 커밋에 절대치를 남길 때는 캔버스 크기를 같이 적을 것.

## 코드 규칙

- **부분 편집으로 수정한다.** 전체 재생성은 구조가 근본적으로 바뀔 때만
- 주석에 **왜**를 남긴다 — 특히 되돌린 시도, 폐기한 대안, 임의로 정한 상수의 근거.
  기존 주석이 `[v2 수정]`, `[버그 N 수정]`, `[§7.4]`, `[수정 N]`, `[모바일 대응 N번]`
  형식으로 출처를 달고 있으니 같은 형식을 따를 것
- 튜닝 상수는 `V2_TUNABLES`에 모으고 옆에 근거를 적는다
- 커밋 메시지는 한국어. 무엇을 왜 바꿨는지 + 실측값
