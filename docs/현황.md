# 사주은하 현황 [2026-09-09]

회차마다 인수인계 블록을 손으로 옮기지 않으려고 만든 파일이다.
**로드맵의 정본은 `HANDOFF-v2.md` §5-0 「로드맵 현황」이다** — 여기는 그것을 요약하고
회차 이력·미결을 덧붙인다. 둘이 어긋나면 `HANDOFF-v2.md`가 우선한다.

## 로드맵 (HANDOFF §5-0 기준, 2026-09-08 대조)

완료
1. 합·충 (L4 첫 절반) — §6.4
2. 그룹 상성란 — 전용 절 없음(`index.html` 209행 주석이 유일한 기록)
3. 개인 확장 1차 (해석 코너 넷: 어울리는 일 / 돈 스타일 / 관계 스타일 / 힘이 되는 기운) — §6.5
4. 모바일 실측 — §5.12 · §5.13
5. 카드 · OG · 공유 기능부 — §5.9 · §5.10 · §5.14 · §5.15
6. A. 패널 개편 (2026-09-04) · 7. 보안·안정성 점검 · 8. 계측 심기(스위치 꺼짐)

진행 중 (앞이 안 끝나면 뒤로 안 간다)
1. 폰 확인 — 실기기
2. **B. 타로 카드** — 2026-09-05에 교체 완료. 다만 ⑨~⑫ 후속(명패 3요소·미해결 4건)이 남아 있다
3. **C. 문구 현실 연결** ← **지금 여기**
4. 계측 도입 — 코드는 심었고 **사용자가 계정·사이트 코드를 넣는 일**이 남음
5. 뿌리기 설계 → 6. 뿌리기

확산 후
- 연운 (+월운은 검토) — §6 L5 / 대운 — §6 L6
- 병행 가능: 일주 캐릭터 · 색방위. 구상 단계: 코드 교환

## 확산 전 대기열
- A 모바일 패널 개편·보안·계측 — 완료(계측 계정 입력만 사용자 몫)
- B 카드 타로풍 디자인 — 진행 중 (⑫ 명패 3요소가 다음)
- C 해석 문구 개정 — 진행 중 ← **지금 여기**
- D 뿌리기 설계 (공유 문구·뿌릴 곳) — 대기
- E 뿌리기 — 대기

## 지금 위치
**B21에서 밀린 작업분을 전부 커밋했다** — 코드 `fd05c90` · 그림 `719fdec` · 보고서 `eb29b5f`.
`index.html`은 회차별로 가르지 못해 한 커밋으로 뒀다(B14가 넣은 호출을 B17이 끊어서,
나누려면 지금 파일에 없는 줄을 되살려야 한다 — B21 보고서 3절).
**push는 안 했다** — `origin/main`은 `ecbb17d`, 로컬이 **10커밋** 앞선다.
**다음은 C7**(여덟 코너 문구 개정). 그 전에 밀린 눈 확인 3건이 있다 — 카드 위 설명 5줄 · C11 문구 · B12 명패.

**B 카드 ⑫까지 끝났다** — 명패 맨 윗줄이 "내가 보는 ○○" / "○○가 보는 나"로,
맨 아랫줄 앞 글자가 한자에서 "무쇠·바위" 같은 쉬운 말로 바뀌었고,
⑪이 남긴 세로 정렬 임의 보정 +3px을 없앴다(5색 세로차 ±1px 이내).
남은 카드 미결은 아랫줄 뒷 글자(지지 한자 `申`)를 어떻게 할지 하나다.
**B20에서 윗줄을 34px로 키웠다** — 이 저장소가 정한 화면 최소 글자 **15px**(`--fs-sm`)을
미리보기 축소 배율 0.4516으로 되돌린 값이고, 아랫줄(50px)의 **68%**라 위계도 지킨다.
캔버스 1235 → **1248**, 카드 y 140 → **153**. 미사용 코드 3건은 **이미 지워져 있어 지운 것 0건**.
남은 것: 카드 창 여유가 **2px**뿐, 폰(390)에서는 미리보기가 더 줄어 윗줄이 9.65px이 되는 것.

~~**B19에서 그것을 위아래 두 줄로 나눴다** — 윗줄은 명패 윗줄과 같은 문자열(24px),
아랫줄은 `<역할>`(**50px, 모든 카드 공통**), 사이 4px. 52px에서는 「<판을 뒤집는 재간둥이>」가
496px로 2px 넘쳐 열 개가 다 드는 50px로 통일했다(B18의 36~52px 들쭉날쭉 해소).
`descH = 31+4+65+10 = 110` 계산 → 저장 캔버스 **1080×1235**, 카드 y **140**.
카드 494×905·그림 칸 327×409·서명 블록 130 그대로, 화면 그림 칸도 148×185로 안 눌렸다.~~
(윗줄 24px 부분은 B20에서 34px로 바뀌었다. 나머지는 그대로다.)

**B15에서 "카드 한 장 위에 설명을 붙일 자리"를 쟀다**(조사 전용) — 카드 위 여백은 **30px뿐**이라
한 줄도 1px 모자란다. 캔버스를 **+41px**(1125 → 1166) 키우면 24px 한 줄이 들어가고 카드 높이 905는 그대로다.
저장 카드에는 **이미 이름이 들어가 있다**(명패 윗줄 24px) — 설명에 이름을 붙여도 새 위험은 없다.

**B14에서 그림 설명 두 줄이 카드 창에 들어갔다** — 카드 미리보기 바로 아래, 13px 두 줄.
넘침도 없앴고(시트 상한을 카드 창에만 92vh로 올림 + 간격 정리), 묵은 것 둘도 치웠다
(낡은 webp 경로 주석 정정, 공회전 함수 `wireHapChung` 삭제).

C 시리즈 문구 개정은 **C11에서 마무리됐다.** 여덟 코너에서 십신 이름 **잔존 0건**,
45자 초과는 **2꼴**(둘 다 ⑧ 오행 뜻풀이 — 그룹 패널 공용이라 손대지 않기로 한 것),
개수 표현은 ④·⑤가 **"둘까지는 이름, 셋부터는 개수"로 통일**됐다.
다음 후보는 ⑧ 오행 뜻풀이 축약 + `wireHapChung` 정리(둘 다 그룹 패널까지 함께 보는 회차)이고,
그다음은 로드맵상 **계측 도입 → 뿌리기 설계**다.
~~`index.html`은 C8·C10·C11 작업분이 미커밋이다(마지막 커밋 `3836841`).~~
→ **[2026-09-09 B21 정정] 미커밋 0건.** 그 작업분은 `ff6f34a`에, B14·B17~B20 작업분은 `fd05c90`에 들어갔다.

## 회차 이력 (docs/reports/ 파일명 기준)

B 시리즈 (카드)
- B1~B2d — 타로 그림 10장 생성·재생성
- B3~B3i — 카드 합성, 테두리·비율·미리보기 폭·캔버스 높이
- B4a~B4b — 그림 칸 세로형(351×528) 설계·구현
- B5~B10 — 테두리 5장·그림 10장 생성과 재생성(화풍 통일까지)
- B11(09-07) — pic-10 콩 크기 3회 시도 후 보류 / B12(09-07) — pic-10 확정
- B11(09-08) — 그림 상단 정렬 + 명패 확장 / B12(09-08) — ⑫ 명패 3요소 **지시문 초안**(미실행)

C 시리즈 (문구)
- C1 — 관계 문구 개정표 초안(승인용). 합충이 3종이 아니라 "3종 + 없음" 네 상태임을 실측
- C2 — 조각 구조 신설·본문 교체 (`REL_PIECE` 10 / `HC_PIECE` 3 / `REL_DESC` `{b,c}` / `relLead()`),
  `REL_TAKE`·`REL_STRENGTH` 삭제
- C3 — a줄 40개 목록 추출 (읽기 전용)
- C4 — 합충을 앞 문장으로 분리, 상관 조각 축약("뒷심이 짧은 재간둥이"), 관계 패널 딱지 제거
- C5 — b·c줄 20행 진단 (유지 15 / 손볼 것 5)
- C6 — 개정안 5건 치환·검증 통과 (40가지 이상 0건, 최장 38자·평균 24.3자, c줄 "대신" 10/10,
  자가진단 58/58). 상관 c는 "새로 재미난 게 보이면 하던 걸 두고 갑니다"로.
  경계선 3건(편재 "판" / 정관 "지키다" / 정인 "잔소리")은 유지 결정.
  십신 간 겹침 2쌍(겁재↔식신 b, 정관↔정인 c)은 허용.
- C7 — 여덟 코너 문구 전수 추출·진단(읽기 전용). 생일 150,481건을 합성해 실제 호출,
  문자열 7,858개 수집(이상값 0건). 문장 98행 중 **유지 42 / 손볼 것 56**, 개정안 초안 56건.
  큰 문제 셋: ①만 말투가 명사형이라 다름 / ②③이 십신·신강신약 용어를 괄호로 노출 /
  ④⑧에 45자 넘는 문장(최장 67자). 미사용 코드 정리 — `hcChipHtml`·`hcSeq`·`.hcchip`/`.hcwhy` CSS 삭제,
  `wireHapChung`은 호출 2곳이 남아 있어 되돌렸다. 자가진단 58/58, 콘솔 오류 0건.
- C7.5 — 복구 검증 후 안전점 커밋. c1e8031 대비 삭제 89줄을 전수 분류해
  "딱지 관련 37 + C2~C6 문구 교체분 52, 설명 안 되는 삭제 0"을 확인했다.
  `wireHapChung` 정의 1·호출 2 살아 있음, `hcSeq`/`hcChipHtml`/`.hcchip`·`.hcwhy` CSS 0곳.
  자가진단 58/58 · 콘솔 오류 0 · 여덟 코너 8/8. **커밋 `3836841`**(20파일, push 안 함).
  `CLAUDE.md`에 "문자열 검색은 `grep -a`" 한 줄 추가(아직 미커밋).
- C8 — 여덟 코너 문구 치환. C7 개정안 56 + ① 추가 2 = **58건 전부 반영, 보류 0**(코드 편집 47곳).
  ⑤~⑧의 "임의 수정 금지" 주석은 2026-09-09 Teo 승인으로 해제 표시했다.
  인칭을 "이 사람"으로 통일(코너 안 2곳, 코너 밖 13곳은 목록만).
  재검증 146,828표본 — 십신·신강신약·억부·지장간 잔존 0, "당신" 0, 이상값 0.
  남은 것: ⑥ 해설 층 `정재 N · 편재 N.` 23건(개정안 없어 그대로) / 45자 초과 6꼴(최장 68자, 분기표 D).
  자가진단 58/58(옛 문구를 박아 둔 시험 2건은 대조 문자열만 갱신), 콘솔 오류 0. 12,016 → 12,026줄.
- C9 — ⑥ 용어 잔존·길이 초과 정리안(읽기 전용). `정재/편재` grep 23줄 중 **화면에 나오는 건 2줄**뿐임을
  가려냈고(3436 ⑥ 해설 / 9680 사주 표 도움말), 검증 구멍의 원인이 C8 검증이 아니라
  **C7 진단이 ⑤·⑥·⑦의 해설 층을 98행 표에 안 넣은 것**임을 밝혔다.
  C8이 적은 "④ 55번 67→68자로 늘었다"는 잘못된 대조로, 같은 조건이면 **82 → 68자로 줄었다**고 정정.
- C10 — C9 승인분 치환 4곳. ⑥ 해설을 `돈과 실속을 다루는 기운(재성)은 모아 지키는 쪽 N · 크게 키우는 쪽 N입니다.`로,
  ④는 "둘까지는 이름, 셋부터는 개수" 규칙 적용(C8의 문장 쪼개기 되돌림), ⑤는 동률 5개일 때만 개수로.
  ⑤ 동률 3·4개는 실측 42자·45자라 유지(4개는 경계선). 재검증 146,828표본 —
  **십신 잔존 0**, 45자 초과 6꼴 → **2꼴**(최장 68 → 46자), 어미는 31,445문장 중 31,097 정상에
  예외 39종류가 전부 의도된 것(① 물상 10 · ⑧ 뜻풀이 28 · 청유 1). 자가진단 58/58, 콘솔 오류 0.
  12,026 → 12,046줄.
- C11 — 개수 표현 규칙 통일 + 미사용 코드 정리. ⑤ 동률 줄을 ④와 같은 규칙으로 통일
  (셋 42→35자, 넷 45→35자. 길이가 아니라 규칙 통일이 근거). ④ 결여 1개는 전용 한 문장으로
  (`반면 …기운(식상)은 여덟 글자 안에 하나도 없습니다.` 최장 32자) — "갈래가 하나입니다" 어색함 해소.
  미사용 코드 셋 중 **지운 것 0건** — `hcChipHtml`·`.hcchip`/`.hcwhy` CSS는 C7에서 이미 지워져 있었고,
  `wireHapChung`은 호출 2곳(9981·10062)이 남아 보류. 재검증 146,828표본 —
  ㄱ 규칙 일치 ✅ / ㄴ 45자 초과 2꼴(⑧ 지정 제외분뿐) / ㄷ 어미 예외 39종류로 C10과 동일, 새 예외 0 /
  ㄹ 용어 잔존 0. 자가진단 58/58, 콘솔 오류 0. 12,046 → 12,056줄.
- B ⑫ — 명패 맨 윗줄·아랫줄 교체 + 정렬 실측. 윗줄은 `isMe`로 갈라 "내가 보는 ○○"/"○○가 보는 나",
  본인이 안 끼면 이름 둘. 아랫줄 앞 글자는 `STEM_BLURB` 첫 도막(개인 패널 ①과 같은 문구)을 재사용해
  `庚申` → `무쇠·바위 申`(뒷 글자는 지시대로 한자 유지). ⑪의 임의 보정 **+3px 삭제** — 세 블록 모든 줄의
  잉크 상자로 중앙을 잡아 5색 세로차 0~1px. ⑪이 실패했던 명패 상자 경계 측정도 좌표계를 맞춰 다시 해
  **위·아래 차이 5색 전부 0**. 이물 픽셀 네 변 0, 카드 높이 905 유지, 자가진단 58/58, 콘솔 오류 0.
  캡처 `docs/reports/2026-09-05-card-b/b12-명패윗줄-5색.jpg`. 12,056 → 12,087줄.
- B13 — 카드 그림 설명문 조사(읽기 전용, 코드 무수정). 카드가 실제로 읽는 파일은
  `assets/card-b/pic-01~10-*.png`임을 `CARD_PIC`로 확정(옛 판 `01~10-*.png`·`web/`·`web2/`는 안 쓰임,
  2185행 주석의 "web/*.webp"는 낡았다). **파일 번호가 십신 순서와 거꾸로**다(비견 09 … 정인 01).
  10장을 직접 보고 관찰 기록을 남겼다 — 글자는 열 장 다 0건, 공통은 한지 바탕·먹선·2:3.
  카드에 얹힐 때 잘리지 않고 **가로로 1.19배 늘어난다**(원본 2:3 → 칸 4:5).
  자리 실측: 카드 안은 여유 16~37px(한 줄 37px 필요 — 안 들어감, 넣으면 높이 905가 깨짐),
  모달은 폭 490px·한 줄 37자인데 **지금도 4px 넘쳐 스크롤이 있다**(600·1024 값 동일, max-height 774 고정).
- G1 — 2주치 밀린 작업분 커밋(로컬만, push 없음). **미커밋 누적 해소.**
- B14 — 그림 설명 두 줄 적용. `CARD_PIC_DESC` 10개를 십신 번호로 찾아 쓰고,
  왼쪽/오른쪽 십신은 `cardText`의 `side()`에 `g`를 실어 보내 알아낸다(파일 번호는 거꾸로라 못 쓴다).
  넘침 4 → 블록 넣어 53 → **0**(`#cardsheet`만 상한 92vh·840px로, 미리보기 아래 여백 14→6).
  다른 시트 상한은 774 그대로. 짝 검증 3겹(파일명·카드 18장·창 10번) 전부 어긋남 0.
  청소 둘 — 2199행 webp 경로 주석 정정, `wireHapChung` 함수와 호출 2곳 삭제(패널·합충 표시 그대로).
  자가진단 58/58, 콘솔 오류 0, 카드 높이 905 유지. 12,087 → 12,137줄.
  **미커밋**: `index.html`(B14분) · `docs/현황.md` · B14 보고서.
- B15 — 카드 위 설명 자리 조사(코드 무수정, md5로 확인). 캔버스 1080×1125 안에서 카드는
  (30,30,494,905)·(556,30,494,905) — **위 여백 30px, 배경만 있고 글자 0**(16,200점 훑어 밝은 점 0개).
  캔버스 24px이면 설명 열 문장이 **10/10 한 줄**(카드 폭 494, 최장 475)이지만 세로가 1px 모자라,
  캔버스를 **+41px** 키우면 된다(카드 905 불변) → ㉮ 조건부 가능.
  화면 배율 0.452라 카드 한 장이 223px — 겹쳐 얹기는 자리 없음(14px), 위쪽 두 칸으로 두면 11px에서 10/10.
  **저장 카드에 이름이 이미 그려진다**(명패 윗줄 24px). B13의 "상한 774 고정" 오기도 그 문서에 정정 추가.
- B16 — 비교 그림 3장(코드 무수정, md5 동일). 아래 190px은 여백 30 + 서명 블록 130 + 여백 30이라 꽉 차 있고,
  **C안 불가**(카드를 41 내리면 서명 자리가 41px 모자라고, 안 내리면 5px 겹친다).
  캔버스 비를 박아 둔 곳은 **없다** — `syncCardPrevCap`의 RATIO가 `plan.h`를 읽어 저절로 따라간다.
  A안은 `cardGeom().top`을 30 → 71로 올리고 글자 op 둘을 더하는 것이 전부.
  B15의 "최장 475"는 **이름 없는 값**이었고, `테오는 `을 붙이면 543px로 **49px 넘친다**(24px·494px 최대 23자).
- B17 — A안 적용(코드 수정 첫 회차). `CARD_B.descH:41` 신설 + `cardGeom().top = margin + descH` **한 줄**로
  캔버스 1125 → **1166**, 카드 y 30 → **71**, 서명 1021 → 1062(+41), footerH·카드 494×905 불변.
  `CARD_ROLE` 10개와 `cardRoleLine()`(3단계 접기, 한글 아닌 이름은 '은' 고정) 신설.
  **왼쪽 줄의 주어는 그 장에 그려진 사람**(b) — 지시문 예시와 좌우가 반대이나 명패·그림 실측을 따랐다.
  B14의 `fillCardPicDesc` 호출을 끊고 상자를 접었다(상수·함수는 남김).
  검증: 자가진단 58/58, 카드 18장 짝 어긋남 0(최장 384px ≤ 494), 폭 600·1024 넘침 0,
  시트 내용 821 → **787**로 오히려 줄었다. 12,137 → 12,185줄.
  그림: `docs/reports/img/2026-09-09-B17/`.
- B18 — 카드 위 줄의 말투·크기 교체. 「명패 윗줄 + `<역할>`」로 바꾸고(꺾쇠 앞은 `cardDirLabel()` 재사용),
  라벨 24px + 역할 52px을 한 줄에 밑선 맞춰 나란히. `descH:41` 삭제 → `descGap:10`만 두고
  `round(fs.nick×1.3)+descGap = 78` 계산 → 캔버스 1166 → **1203**, 카드 y 71 → **108**.
  넘치면 4단계 접기(역할 2px씩 → 라벨 이름만 → 역할만) — 실측 열 역할·긴 이름까지 **넘침 0**.
  B17의 `cardRoleLine`은 호출만 끊고 남겼다.
  검증: 자가진단 58/58, 카드 18장 어긋남 0(라벨이 명패와 글자 그대로 일치), 폭 600·1024 넘침 0,
  **그림 칸이 안 눌렸다**(327×409 · 화면 148×185 — 작업 전과 같음). 12,185 → 12,220줄.
  [의견] 역할 크기가 카드마다 36~52px로 갈려 한 쌍의 두 장이 달라 보일 수 있다 — 통일 여부는 미정.
  그림: `docs/reports/img/2026-09-09-B18/`.
- B19 — 카드 위 설명을 **두 줄로** 분리. 윗줄 라벨 24px + 아랫줄 `<역할>` 50px, 사이 4px, 둘 다 가운데 정렬.
  **역할 크기를 하나로 고정** — 52px에서 상관 「<판을 뒤집는 재간둥이>」가 496px(494 초과, 지시문 추정 466과 다름)이라
  `cardRolePx()`가 열 개를 다 담는 **50px**을 찾아 쓴다(숫자 안 박음). 18장 전부 50px.
  `descH = labelH31 + lead4 + roleH65 + gap10 = 110` → 캔버스 1203 → **1235**, 카드 y 108 → **140**.
  윗줄 접힘 0건, 아랫줄 넘침 0장. 자가진단이 57/58로 한 번 깨졌는데(`cardWrapNick`이 카드 밖 `<역할>` 줄까지 세었다)
  시험 조건에 `y >= geom.top`을 더해 **58/58** 복구. 카드 18장 어긋남 0, 폭 600·1024 넘침 0,
  그림 칸 148×185 유지. 12,220 → 12,232줄. [의견] 카드 창 여유가 **7px**(819/828)뿐이다.
  그림: `docs/reports/img/2026-09-09-B19/`.
- B20 — 카드 위 **윗줄**을 24 → **34px**. 규정 실측: 화면 최소 글자는 `--fs-sm` **15px**
  (index.html:104 · HANDOFF-v2.md:2911~2915의 2026-08-30 정정 — 옛 11px·13px 규정은 취소선).
  `ceil(15/0.4516)=34`, 아랫줄 50px의 68%(70% 이하)로 두 규칙 동시 충족 → 가장 작은 값 채택.
  숫자를 박지 않고 `CARD_B{minScreenPx:15, prevScale:0.4516, labelMaxRatio:0.7}` + `cardLabelPx()`로 계산.
  배율은 캔버스 폭 비(0.4516)와 그림 칸 비(0.4526/0.4523)로 교차검증.
  descH 110 → **123**, 캔버스 1235 → **1248**, 카드 y 140 → **153**. 카드 창 여유 7 → **2px**(상한 828 유지).
  **작업 B: 지운 것 0건** — `hcChipHtml`·`.hcchip`/`.hcwhy`·`wireHapChung` 셋 다 C7·B14에서 이미 삭제돼
  코드 0건, 주석만 남아 있었다(정정 기록이라 존치).
  검증: 자가진단 58/58, 카드 18장 어긋남 0·넘침 0(윗줄 34 ×36 · 아랫줄 50 ×36), 폭 600·1024·390 넘침 0,
  그림 칸 148×185 유지. 12,232 → 12,251줄.
  [의견] 폰(390)에서는 배율 0.2838이라 윗줄이 9.65px — 거기서 15px을 지키려면 53px이 필요해
  아랫줄 50px보다 커진다(설계 재검토 사항, 범위 밖으로 둠).
  그림: `docs/reports/img/2026-09-09-B20/`.
- B21 — **밀린 작업분 커밋**(이 회차만 커밋 허용). 코드를 한 줄도 안 고쳤다 —
  `index.html` md5가 커밋 전후 동일(`f7b2462dc21148f177fe1760e7a87921`, 950,946 bytes / 12,251줄).
  세 커밋: `fd05c90` 코드(index.html +185/−21) · `719fdec` 갈무리 11장(png 8.5MB) ·
  `eb29b5f` 보고서 8건(+1074). 그림을 보고서보다 먼저 넣었다(보고서가 갈무리를 참조해서).
  **index.html은 나누지 않았다** — 헝크 `@@ -2252,4 +2281,89 @@` 하나에 B14의 `CARD_PIC_DESC`와
  B17~B20의 `CARD_ROLE`·`cardRolePx()`·`cardLabelPx()`·`cardTopLine()`이 붙어 있고,
  B17이 B14의 `fillCardPicDesc()` 호출을 끊어서 B14만의 중간 상태가 지금 파일에 없다.
  뺀 파일 0건 — `my-data.js`·백업 json·`assets/card-b/web2/`가 `.gitignore`로 제외된 것을 명령으로 확인.
  검증: 자가진단 **58/58**, 브라우저 실측이 지시문 값과 전부 일치
  (labelPx 34 · rolePx 50 · descH 123 · 카드 y 153 · 494×905 · `typeof wireHapChung` = undefined),
  HTTP 200 950,946 bytes. `git fetch` 후 `origin/main` `ecbb17d`, 로컬 **10커밋 앞섬**. push 안 함.
  그림: 없음(화면 변경이 없어 갈무리할 것이 없다).

## 커밋 이력 (로컬 · push 전)

`origin/main`은 아직 `ecbb17d`에 있다(2026-09-09 B21에서 `git fetch` 후 확인).
아래 **열**은 로컬에만 있다 — `git rev-list --left-right --count origin/main...main` → `0  10`.

| 해시 | 제목 | 내용 |
|---|---|---|
| `ecbb17d` | fix(카드B): pic-10-geopjae 확정 | ← 원격이 여기까지 |
| `c1e8031` | ⑨~⑪ 명패/렌더 작업 마무리 | 카드 ⑨~⑪ |
| `3836841` | C2~C7: 관계 문구 조각 구조·b·c줄 개정, 여덟 코너 진단, 미사용 코드 정리 | C7.5 안전점 |
| `ff6f34a` | 문구 C2~C11 개정 + 카드 B12 명패 교체 | index.html (+128/−57) |
| `3f970b6` | 회차 보고서 및 진행 문서 갱신 | 보고서 7 + 갈무리 1 + CLAUDE.md |
| `bd32474` | 기타 변경분 | 이 표를 담은 `docs/현황.md` + G1 보고서 |
| `f88741a` | 문서: 커밋4 해시(bd32474)를 현황·보고서 표에 채움 | 위 표의 `bd32474` 자리를 채운 한 줄 |
| `fd05c90` | 카드 위 설명 두 줄 도입 + 그림 설명·미사용 코드 정리 (B14·B17~B20) | `index.html` (+185/−21) |
| `719fdec` | 회차 갈무리 그림 11장 추가 (B16~B20) | `docs/reports/img/**` png 11장 · 8.5MB |
| `eb29b5f` | 회차 보고서 B14~B20 추가, B13 보고서 후속 표기 보완 | 보고서 8건 (+1074) |
| `(이 커밋)` | B21 보고서 추가, 현황 문서를 커밋 이력까지 갱신 | 이 표를 담은 `docs/현황.md` + B21 보고서 |

마지막 줄의 해시는 **비워 둔다.** 커밋은 자기 해시를 담을 수 없다(적어 넣고 amend하면 그 값이
곧바로 죽은 해시가 된다 — B21에서 실제로 한 번 밟았다). 저장소가 전에 쓰던 방식대로
**다음 커밋이 채운다**(`f88741a`가 `bd32474` 자리를 채운 것과 같다).

## 미결
- ~~카드 ⑫ — 명패 3요소 지시문은 썼으나 미실행. 미정 2건(본인 안 낀 관계 라벨 / 일주 두 글자 처리)~~
  → **[2026-09-09 B⑫ 완료]** 본인 안 낀 관계는 이름 둘로, 일주 앞 글자는 쉬운 말로 처리.
  **남은 미정 1건**: 아랫줄 뒷 글자(지지 한자 `申`)를 그대로 둘지 띠 이름으로 풀지
- ~~⑪ 미해결 4건 — 글자 세로 중앙 `+3px` 임의 보정 원인 규명, 검증 스크립트 좌표계 버그,~~
  → **[2026-09-09 B⑫ 해소 2건]** +3은 잔차가 아니라 ⑪ 측정이 좌표계를 섞어 만든 헛값이었다 —
  보정 삭제 후 세로차 0~1px. 좌표계를 맞춘 경계 재측정도 차이 0.
  **남은 2건**: 자가진단 실패 1건 소멸 경위, ⑩ 액자선 원본 PNG 확인
- ~~미사용 코드 정리 3건 — `hcChipHtml` / `.hcchip`·`.hcwhy` CSS / `wireHapChung` (C7에서)~~
  → **[2026-09-09 B20·B21 해소]** 셋 다 C7·B14에서 이미 지워져 B20의 지울 것이 0건이었다.
  B21에서 브라우저로 `typeof wireHapChung` = `undefined`를 확인했다. 경위 주석만 존치.
- 신강신약을 조각으로 얹는 회차 (`REL_STRENGTH` 원문은 C2 보고서에 보존)
- 계측 — 사용자가 GoatCounter 계정·사이트 코드 입력
- 모달창 스크롤 없이 전체 보이게
- 리넨 데모(demo/linen srcset·세로 컷) 보고 미처리
- 카드 소재 웹 경량화(`assets/card-b/*.png` → webp) — B4d 이후 미처리

## 로컬 직링크
http://127.0.0.1:8934/index.html
