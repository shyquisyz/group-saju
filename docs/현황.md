# 사주은하 현황 [2026-09-20]

회차마다 인수인계 블록을 손으로 옮기지 않으려고 만든 파일이다.
**로드맵의 정본은 `HANDOFF-v2.md` §5-0 「로드맵 현황」이다** — 여기는 그것을 요약하고
회차 이력·미결을 덧붙인다. 둘이 어긋나면 `HANDOFF-v2.md`가 우선한다.

## 로드맵

**로드맵 정본은 [`docs/로드맵.md`](로드맵.md)다 (2026-09-09 R1 신설).** 이 파일에는 더 이상
직접 담지 않는다 — 여기서 로드맵을 요약하다 두 곳(여기·`HANDOFF-v2.md` §5-0)이 서로
어긋나는 사고가 반복됐다(2026-09-09 오전 "다음은 C7" 오안내가 마지막 사례).

**R1에서 찾은 것** — 이 문서가 "계측 도입은 사용자가 계정·코드를 넣어야 한다"로 들고 있던
항목이 **틀렸다.** `index.html:2089`의 `ANALYTICS={ON:true,SITE:'shyquisyz'}`가 이미 배포까지
끝났다(`docs/reports/2026-09-05-계측켜기.md`, 커밋 `7f3b6dd`·`846a248`이 `origin/main`에 있음).
자세한 경위는 `docs/로드맵.md` 부록 B 참고.

## 지금 위치

**[2026-09-20 U33] 형(刑) 넣기.** 寅巳申·丑戌未·子卯·자형 辰午酉亥, 충과 겹치면 뺌, 올해·원국·대운·근거 층에 긴장·마찰 틀로. 올해 형 문장 대상 28.7%. 자가진단 119/119. 미결: 8번(2026~2029) 원문.

**[2026-09-20 U32] 해석 레이어 보정(사용자 기준 1~7).** 대운 돈 시기는 운 기둥 강도(luckForce)로, 앞 5년/뒤 5년 삭제, 세기는 등급 말로(숫자는 근거 층 '앱 내부 균형 지표'), 천간합 50% 삭제, 반합=기울어지는 연결·대운이 채우면 삼합, 연애 새 인연·외도 문구 삭제(U13 정정), 합충=변화 압력. 금지어 8종 전원 0건, 자가진단 118/118. 미결: 8번(2026~2029) 원문 잘림 · 형(刑) 넣을지.

**[2026-09-20 U31] 계산 고치기 — 합과 세력.** 지지를 지장간 비율로, 합(삼합·방합·반합·천간합)을 세기·직업·대운에 반영. 계절 비유를 원인 설명으로, 직업에 짜임 다섯(식신생재·재생관·관인상생·재다신약·군겁쟁재), 평생 흐름에 지금·다음 10년 + 대운 합. 자가진단 117/117. 다음: 빈 성별·시각 묻기 → 지나온 길 + 보정.

**[2026-09-20 U28] 운에 따른 행성 변화.** 이번 달 좋은 달 = 이름표 ✨ + 금빛, 예민한 달 = ⚡(12달 표와 같은 판정). 자가진단 116/116. 다음: B1 로그인(Supabase 프로젝트·카카오 앱은 사용자가 만들어야 함).

**[2026-09-20 U27] 중심 바꿔 보기.** 3인 이상 그룹 패널의 칩으로 가운데 사람을 잠깐 바꿈 — 3D 궤도·크기·글이 그 사람 기준, 고리·구름은 걷음, 저장 안 함, 다른 곳으로 가면 풀림. 자가진단 115/115. 다음: U28 운에 따른 행성 변화.

**[2026-09-20 U30] 호칭 칸 + 가족 그룹 자동 제안 · 적응형 배율.** 저장 v8(`title`), 폼 호칭 칸, 이름표 아랫줄·패널 제목에 호칭, 가족 호칭이면 가족 그룹 제안. 배율은 프레임 중앙값 30ms 넘으면 2→1.5→1(데스크톱은 그대로). 자가진단 114/114. 다음: U27 중심 바꿔 보기.

**[2026-09-20 U29] 폰 속도.** 부팅 뒤 CPU 멈춤 1,389ms → 298ms, 부팅 921 → 411ms(데스크톱 실측, 사람 10명). 지도 파생 절반 크기(장당 108→29ms)·가공 줄 세우기·지도 올 행성은 일간 표면 생략(restoreSave 338→50ms)·정지 초상은 살아 있는 초상 실패 시만(패널당 105ms). 자가진단 113/113. 미결: 적응형 DPR(사용자 결정).

**[2026-09-19 U16] 내용 채우기.** 어울리는 일에 실제 직업(25칸×6)·일하는 방식 · 올해 12달(좋은 달·조심할 달) · 오늘의 한마디(일진) · 타고난 별(도화·역마·화개·천을귀인). 자가진단 107/107. 미결 모음 질문 중(B0 5건 · 호칭 칸 · 커밋 이력 이름 · 폰 속도).

**[2026-09-19 U15] 테스트 개인화.** 요즘 고민·힘들 때·사랑할 때 테스트 + 「요즘」 고민×올해 줄·힘들 때 팁 · 「궁금한 것」 고민 주제 맨 위 · 연애 팁 · 「나를 더 알려 주기 n/4」 · 저장 v7. 자가진단 105/105. **다음은 B0 결정 5건 → B1** [권장]. (`2026-09-19-U15-테스트개인화.md`)

**[2026-09-19 U14] 추정 시각을 표·풀이에.** 임의 등록자는 추정 시각으로 계산하고 「추정」 표시, 원본 시각(연동자 포함)이 언제나 이김 · 가족에게 물어보기 카드는 안 만듦 · B0 설계 v6→v7 정정. 자가진단 104/104. (`2026-09-19-U14-추정시각-반영.md`)

**[2026-09-19 U13] 새 인연 흐름 · 원고 ③ 그룹 · 시각 추정 1차.** 짝이 있어도 이별·새 인연 흐름 표기(기준 실측: 느슨한 안 100% 폐기 → 24%·15%) · 그룹 한마디 21문장·판 이름 쉬운 말 · 시각 추정 보기 정리·답할 수 없는 질문 제외. 자가진단 103/103. **결정 2건 대기**(추정 시각을 풀이에 쓸지 · 가족에게 물어보기 카드). (`2026-09-19-U13-새인연-그룹원고-시각추정.md`)

**[2026-09-19 U12] 원고 ①② 다시 쓰기 + 「궁금한 것」 + 성별 필수·관계 상태.** 어떤 사람인가·서로에게·둘이 붙으면·관심·자리·일·돈·관계 스타일을 장면으로 · 「궁금한 것」(돈·연애·자식·건강, 언제까지) · 쉬운 말(북돋는/다그치는) · 성별 필수 · love(싱글·연애 중·결혼) 저장 v6 · 추가 과제 넷 로드맵 등록. 자가진단 103/103. (`2026-09-19-U12-원고-궁금한것-성별.md`)

**[2026-09-19 U11] 운의 흐름 + 글 두 층 + 친구 말투.** 대운·세운·월운 계산(lunar-javascript 대조 400명·300순간 불일치 0) · 인물 패널 맨 위 「요즘」(올해·이번 달·대운, 친구 말투) · 그룹 「올해 우리는」 · 두 층(이야기 / 「사주로 보면」 근거, 기본 접힘) · 시각 모름 안내 6회→1회 · 관계 시점(당신 = 읽는 나만) · 이야기 층 해요체 변환(원고 원문 보존). 자가진단 101/101. **다음: 원고 자체를 장면·비교 중심으로 다시 쓰기**(말투만 바뀌었고 내용은 그대로). (`2026-09-19-U11-요즘-두층-말투.md`)

**[2026-09-19 U10] 패널 가운데 맞춤 · 누르기 판정 · 전체 글 검토.** 장신구 구름(초상+은하) · 하단 그룹 줄 마우스 좌우 이동 · 폰에서 패널이 올라오면 선택한 행성·그룹을 패널 위 영역 가운데로(투영 중심 이동) · 은하에서 행성 누르기 61/74 → 72/74 · 서로 같은 자리 블록 한 번만. 글 진단: 전문 설명과 인물 이야기가 섞임 · 시각 모름 안내 한 패널 6번 · 시점(당신) 흔들림 → **두 층 재배치 제안, 사용자 결정 2건 대기.** 자가진단 97/97. (`2026-09-19-U10-글검토.md`)

**[2026-09-19 U9] 행성 다듬기.** 핀치 확대 버그 수정 · 나 항성 과노출 수정 · 밤 쪽 용암 발광·노멀맵·반짝이는 바다 · 테두리 변위 10% · 지도를 조화 버전 50장(map5, 나 약 50% + 환경이 얽힘)으로 교체. 자가진단 97/97. (`2026-09-19-U9-행성다듬기.md`)

**[2026-09-19 U8] 행성 = 나 + 환경(4분할).** 나(일간 오행) 한 지역 + 사주에 있는 다른 오행 지역이 같은 크기로 — 지도 50장(map4/, Magnific 5,000 크레딧). 초상·은하 행성 모두 적용(은하의 일간 전용 장식 폐기, 나 항성은 지도가 스스로 빛남). 초상 조명 = 반달과 측면 사이(A2). 자가진단 97/97. (`2026-09-19-U8-4분할행성.md`)

**[2026-09-19 U7] 초상 v2 + 살아 있는 초상·장신구 앱 적용.** 초상 = 오행 분포로 고른 지도 55장(사람마다 다른 면 · 금 광택 · 바다 반사 · 계절막).
인물 패널 머리는 계속 움직이는 3D 초상 + 나와의 합충 장신구(안 D). 자가진단 97/97. 은하 작은 행성 보정. (`2026-09-19-U7-초상v2-장신구적용.md`)

**[2026-09-19 U6] 앱 반영 1단계 — 행성 크기 = 나와의 위계 · 자전 통일 · 공전 겹침 제거.** 크기: 인성·관성 100% / 비겁 90% /
식상·재성 80%(십신). 자전: ~~모두 0.12 rad/s~~ → **사람마다 규칙으로**(속도 = 신강약 + 태어난 시각, 축 = 계절 깊이 + 띠, 실데이터 10명 모두 다름). 겹침: 이웃 궤도 간격 ≥ 두 반경 + 0.8, 성계를 열면
카메라가 궤도면을 거의 위에서 봄 → 우리 가족 성계 화면 겹침 11.7% → **0%**, 항성 가림 50% → **0%**(600초 실측).
행성을 누르면 화면이 그 행성의 자전축에 맞춰 돌아 정면을 보여 준다(U6-3). 장신구(합충)·55장 지도·계절막은 아직 앱 미적용. 시각 요소 의미표: `2026-09-19-U6-시각요소-의미정리.md`.

**[2026-09-18 U5] 금 지도 교체 — 흰 금속 → 백랍·백금 원석(G3).** 금이 들어간 27장 교체, 55장 방향 재측정 전부 통과(실측).
온도(조후) 효과는 시안만 — 결정 대기 (`2026-09-18-U5-금-백랍색-교체.md`).

**[2026-09-18 U4] 오행 조합별 행성 지도 55장 완성.** `assets/planet-tex/map/` + 보이는 방향표 `facing.json`
(55장 중 54장이 "주 오행이 가장 넓고 보조도 보임" 기준 통과, 실측). **앱은 아직 안 읽는다** — 다음은 초상에 적용 [권장].
(`2026-09-18-U4-행성지도55장.md`, 경우의 수는 `2026-09-17-U3-오행조합-경우의수.md`)

**[2026-09-17 U2] 폰에서 홈 열기 + 검토 2건.** 태그바 「은하 ☰」를 은하 화면에서 누르면 홈. 오행 비율 행성 시안
(~~띠/얼룩~~ → 영역 합성 7판까지 → 7판 재검토 결과 **AI 전체 지도 방식이 더 자연스러움**, 55장 묶음 [권장])과
가독성 진단(개인 해석 56% · 공통 설명 26%가 같은 모양으로 섞임) → **사용자 결정 5건 대기**
(`2026-09-17-U2-폰홈-오행행성-가독성검토.md`).

**[2026-09-17 U1] 첫 화면 홈 · 행성 초상 · 이름표 누르기.** 왼쪽 패널의 그룹 목록을 없애고 홈(내 행성·사람 격자·
그룹 카드)으로 바꿈. 인물 패널 머리에 큰 행성 초상(은하와 같은 재질로 구운 이미지). 그룹 화면에서 이름 글자를 누르면
그 사람이 열리게(원인: 이름표가 행성 판정 22px 밖) — 18/18. 97/97(`2026-09-17-U1-홈-행성초상-이름표.md`).
**홈 디자인은 사용자 확인 대기.** push 완료(`9a5f272..de928ac`), 배포 md5 `a9082ed8…` 로컬과 일치(약 1분),
배포 주소에서 홈·초상·이름표 함수 로드 확인(읽기만).

**[2026-09-17 S1] 보안 점검 끝.** 공격용 이름·그룹명·도시명을 넣고 30개 화면을 열어 **실행 0건**
(기존 이스케이프 유효). 외부 파일 8개에 무결성 해시(SRI) 추가 — 로드·블룸·폰트 정상, 93/93.
폰 확인 모음은 `2026-09-17-S1-보안점검.md` 끝에 있다. push 완료(`1c94d65..cebc513`), 배포 md5
`321658ea…` 로컬과 일치(약 7분 — 앞 배포 작업이 취소·교체되며 늦었다), 배포 주소에서 SRI 8개·3D·블룸·폰트
로드 확인(읽기만). **다음은 결정 모음(아래 「미결」 맨 위) 대기 →
B1a.**

**[2026-09-17 B0] 계정·서버 설계안 끝(문서 전용) — 사용자 결정 3건 대기.** 권장: 화면은 GitHub Pages 그대로,
서버 Supabase(서울), 로그인 카카오+구글, 로그인 안 한 모드는 본인+2명까지. 로그인 후 서버가 정본·토큰은
서버 함수만 움직임. B1 착수 전 Supabase 프로젝트·카카오 앱을 사용자가 직접 만들어야 한다
(`2026-09-17-B0-계정서버설계.md` §8). **다음 코드 회차는 결정 뒤 B1a(서버 스키마·권한) — Opus 5 [권장].**

**[2026-09-17 A8-2] 심리·상황 테스트 구현 끝 — A8 전체 완료.** 노드 레벨 `self`(`SAVE_VER` 5) ·
「스스로 본 모습」 10문항+상황 선택+결과(사주와 나란히, `selfCompare` 순수 함수) · 시각 검증(A5
`estStart`를 `{mode:'verify'}`로 재사용, 저장 안 함). `self`를 설계 문서의 birth-레벨 제안 대신
노드 레벨·`addNode` 기본값으로 둬서 A5·A7이 두 번 겪은 "생성 경로 하나가 새 필드를 빠뜨림" 사고를
구조적으로 막았다. 자가진단 **93/93**(14건 추가), 실제 온보딩→테스트→저장→새로고침→정보 수정 후
유지→지우기, 시각 검증 같음/다름, 375px까지 브라우저로 확인(`2026-09-17-A8-2-구현.md`).
**다음 코드 회차는 B0(계정·서버 설계) — Opus 5 [권장]**(판단이 많은 설계 회차).
push 완료(`10a0517..a6cc714`), 배포 md5 `ac3d0dec…` 로컬과 일치(60초).

**[2026-09-17 A8-1] 심리·상황 테스트 설계·원고 끝(문서 전용).** 시각 검증 · 성향 10문항 · 상황 선택,
원고 88칸/119문장(검사 통과). 답변은 사주 계산을 바꾸지 않는다. 관상·손금 체크리스트는 근거 자료가
없어 뺐다 [권장](`2026-09-17-A8-1-설계원고.md`).

**[2026-09-17 A7] 자정 일주 선택 끝.** `sajuOf`가 civilDayIdx·apparentDayIdx를 항상 계산해
`dayRuleAmbiguous`로 노출, 걸리는 사람에게만 "달력 날짜 / 진태양시" 두 버튼(`birth.dayRule`,
`SAVE_VER` 4). 구현 중 온보딩·여러 명 추가가 `est`/`dayRule` 키를 아예 안 넣던 것을 실측으로
잡았다(A5와 같은 계열, `nodeBirthFieldsExplicit`으로 재발 방지). 자가진단 **79/79**, 실제
온보딩→선택→저장→새로고침→편집까지 브라우저로 확인(`2026-09-17-A7-자정일주선택.md`).
**다음 코드 회차는 A8**(심리·상황 테스트). push 완료(`e413fbb..1216334`), 배포 md5 `68c69f02…` 로컬과 일치(60초).

**[2026-09-17 A5] 시각 추정 화면 구현 끝.** 설계(A4) §8 할 일 여섯 전부 넣었다 — `EST_SLOTS`
13칸·`nextEstQuestion()`·새 시트·`birth.est`(`SAVE_VER` 3)·되돌리기. 구현 중 A6-3이 이미 항목을
아홉 개로 늘려 놓은 걸 놓쳐 우선순위가 어긋난 걸 실측으로 잡았다(고침 + 재발 방지 자가진단
`personItemAlign` 추가). 자가진단 **71/71**, 저장·새로고침·지우기·이름만 고쳐도 안 지워지는 것까지
브라우저로 확인(`2026-09-17-A5-시각추정-구현.md`). **다음 코드 회차는 A7**(자정 일주 선택). push 완료(`75bd484..628c608`), 배포 md5 `02450e6e…` 로컬과 일치(30초).

**[2026-09-17 A6-5] A6 끝.** 원고 279칸 ↔ 코드 279문자열 글자 단위 대조 차이 0, 폰 폭 375 에뮬레이션 넘침 0,
"나가"→"내가" 조사 수정, 61/61(`2026-09-17-A6-5-검증-정리.md`). 해석 원고 99 → 약 449문장.
**다음 코드 회차는 A5(시각 추정 화면)** — 결정 2건 확정됨. ~~push 대기~~ → **[2026-09-17] push 완료**(`96526f0..c0cc822`, 9커밋), 배포 md5 `074d43df…` 로컬과 일치(60초).

**[2026-09-17 A6-4] 치환 3회차 끝 — 그룹 145문장.** 구성원 역할(25)·판 성격(15)·유형×오행(35)·유형×결여(35)·
나에게(35)가 그룹 패널에 붙었다. 계산 무수정, 오행 동률이면 안 붙임. 61/61(`2026-09-17-A6-4-치환3-그룹.md`).
**다음은 A6-5(전체 검증·정리)**. 관찰: 여러 대상을 묶는 "기운이 도는 길" 문장에서 "나가"(→내가) 조사 오류 추정.

**[2026-09-17 A6-3] 치환 2회차 끝** — 자리마다 놓인 기운(년간·일지·시간 십신 30 + 일지 12운성 12),
코너 확대(관계 스타일 일간 10 · 돈 스타일 강/약 8 · 어울리는 일 세기 꼬리 10), 합충 네 기둥(년·월·시
9문장, `hapChungPos` 신설). 자가진단 **61/61**(새 검사 3개 추가). 예시 7쌍에서 일주만 볼 때 3쌍이던
합충이 년·월까지 5건 더 걸림(`2026-09-17-A6-3-치환2.md`). **다음은 A6-4(그룹 145문장)**.

**[2026-09-17 A6-2] 치환 1회차 끝** — 펼침 조정·십신×세기 40·둘이 붙으면 18·세기 사연 6을 코드에
넣었다. 인물 패널 첫 화면 본문 359→723자, 자가진단 58/58(`2026-09-17-A6-2-치환1.md`).

**[2026-09-17 A6-1] 원고 초안 288칸/350문장을 승인표로 냈다(`2026-09-17-A6-1-원고초안.md` §3).
사용자가 ○/수정/×를 주면 A6-2부터 치환한다.** 그룹 해석도 진단했다 — 사람을 일간 오행 한 글자로만
보고 그룹 유형은 마무리 한 문장에만 닿아, 개인·관계보다 더 얇다(그룹 원고 145문장 포함).
A5 결정 2건(자시 13칸 나눔 · `birth.est` 분리)은 사용자가 확정했다. 파일 정리로 `assets/card-b`가
353MB → 12MB(PNG 원본은 `../_보관/`).

**[2026-09-17 R2] 다음 코드 회차는 A6(해석 원고 확장) [권장], 그 다음 A5.** 순서 전체는
`docs/로드맵.md` §2 표. R2는 문서 회차였다(`index.html` 무수정) — 사용자가 "문구가 빈약하고
돌려쓰는 것 같다"고 한 것을 실측으로 확인했다: 해석 본문 **99문장 / 5,575자**가 전부이고, 관계
문단의 82%가 십신 1종으로 결정되며, 역방향 블록은 10가지 고정 짝이다. 폰 눈 확인 6건은 사용자가
전부 통과시켰다. 사업자등록·결제는 마지막으로, 그 전에 B(계정·서버)를 한다는 사용자 결정을 로드맵에
반영했다. 관상·손금(B 이후)·심리 테스트(A5 엔진으로 지금 가능) 검토는 `2026-09-17-R2-방향검토.md`.

~~**다음 코드 회차는 A5(시간 추정 화면 구현)다.**~~ B22·B23·A1·A2·A3에 이어 **A4(시간 추정
설계 확정)**까지 끝났다 — 자세한 내용은 아래 「회차 이력」과 각 보고서 참고.

**A4에서 시간 추정 기능의 설계가 확정됐다**(`docs/시간추정-설계.md`가 정본, `index.html` 무수정).
첫 질문(새벽/아침/낮/저녁/밤, 가점 2) + 성향 문항(가점 1)으로 **평균 3.3문항에 91.3% 적중**,
사용자가 20% 틀려도 **78.0%**. 점수제라 진짜 시각이 탈락으로 사라지는 경우가 **0건**이다.
**가장 큰 발견 — 자시(23–01)는 다른 11칸과 다르다**: 진태양시 보정으로 일주가 밀려 일간만
쓰는 항목(①·⑦)이 96.3% 갈리고, 자시 **안에서도** 23시와 0시가 100% 갈린다. **후보는 12칸이
아니라 실질 13칸**이라 A5에서 나눌지 정해야 한다.

**A3는 "태어난 시간을 모를 때 추정 질문 기능"을 만들 값어치가 있는지 숫자로 쟀다**(설계·구현
없음, `index.html` 무수정). 표본 2,000명 **전원**이 24시간 중 최소 13갈래로 결과가 갈려
"시각을 몰라도 상관없는 사람"은 **0%**다. 다만 세기(A2가 넣은 문장)처럼 안내문이 붙는 코너는
눈에 띄게 다르지만, 나머지 다수 코너는 **안내문 없이 조용히 값만 바뀐다** — 값어치는 있어
보이지만 사용자가 스스로 체감하긴 어려울 수 있다. 질문 개수는 이론상 하한 **4개**(표본
97~100%). 부수 발견 — **진태양시 보정이 자정 근처에서 일주 자체를 하루 밀 수 있다**(조자시와
별개 메커니즘, `docs/reports/2026-09-09-A3-시간추정-진단.md`).

**A2에서 관계 문단에 기운 세기 문장이 실제로 들어갔다.** 관계 패널(두 사람 나란히 보기)의
첫 문장 뒤에 그 사람의 기운 세기 한 문장이 끼워진다("…스승 같은 존재입니다. **기운이 넉넉한
편은 아니라, 무리가 쌓이면 티가 납니다.** 며칠 붙잡고…"). 인물 패널(사람 하나 눌렀을 때)에는
안 넣었다 — 거기는 이미 세기 코너가 따로 있어 중복이 생긴다(A1의 [권장]안을 그대로 따름).
`personGangyak()`이 인물 패널 렌더에 전혀 영향을 안 준다는 것을 문자 단위로 대조해 확인했다
(10,178자 == 10,178자).

**다음에 뭘 할지는 [`docs/로드맵.md`](로드맵.md)가 정본이다.** 지금 여기는 사람이 봐야 하는
일과 미커밋 상태만 적는다.

1. ~~**밀린 눈 확인 4건 + 1 + 1** — 여덟 코너 문구 · 카드 명패(`申` 처리 포함) ·
   폰에서 카드 창 아래 잘림 · 폰에서 카드 그림이 뜨는지 · 카드 위 설명 5줄 ·
   관계 패널의 기운 세기 문장~~ → **[2026-09-17 R2] 사용자가 폰에서 6건 전부 통과.**
2. ~~**push 여부** — 로컬이 원격보다 10커밋 앞서 있다, 미커밋 상태라 push 안 함~~
   → **[2026-09-09 커밋 회차] 끝났다.** B22~A4를 여섯 커밋으로 나눠 커밋·push했다.
   `origin/main`이 로컬과 완전히 같고(`70f1190`), 배포 주소 md5도 로컬과 일치 확인.
   `assets/card-b/web/` 옛 축소본 11장도 이 회차에서 삭제했다. 상세는
   `docs/reports/2026-09-09-커밋배포.md`
3. ~~**A5 착수 전 결정 2건**~~ → **[2026-09-17 확정]** ⓐ 자시 13칸으로 나눔 ⓑ 새 필드 `birth.est`로 분리.
4. ~~**순서 승인 1건**~~ → **[2026-09-17 확정] A6 먼저.**
5. ~~**[2026-09-17 A6-1] 원고 승인 3건**~~ → **[2026-09-17 확정]** 승인표는 [권장]대로 전부 진행,
   12운성 쓴다, E 첫 열은 어릴 적 사실을 빼고 성향으로 고쳐 씀(사용자가 판단 못 내려 문장 쪽을 고침).

~~**계측 계정 입력 — GoatCounter 계정·사이트 코드, Teo 몫**~~
→ **[2026-09-09 R1 정정] 이미 끝났다.** `index.html:2089`에 `ANALYTICS={ON:true,SITE:'shyquisyz'}`가
들어가 있고, `docs/reports/2026-09-05-계측켜기.md`가 로컬 6사건 200·배포 파일 해시 대조·배포
주소 실제 200 응답까지 확인했다. 그 커밋(`7f3b6dd`·`846a248`)은 이미 `origin/main`에 있다.
이 문서와 `HANDOFF-v2.md` §5-0이 오래전에 끝난 일을 계속 "남은 일"로 캐리하고 있었다 —
자세한 경위는 `docs/로드맵.md` 부록 B.

## 회차 이력 (docs/reports/ 파일명 기준)

U 시리즈 (행성 그림)
- **U33(09-20)** — 형(刑): 올해·원국·대운·근거 층, 긴장·마찰 틀
- **U32(09-20)** — 해석 레이어 보정: 대운 강도·10년 한 마디·등급 말·천간합/반합 표현·연애 과장 삭제·합충 변화 압력
- **U31(09-20)** — 계산 고치기: 세력(지장간 비율 + 합) · 계절 문장 · 직업 짜임 · 대운 합
- **U28(09-20)** — 운에 따른 행성 변화(이번 달 ✨+금빛 / ⚡)
- **U27(09-20)** — 중심 바꿔 보기(그룹 칩 → 3D 가운데·궤도·크기·글 전환, 보기 전용)
- **U30(09-20)** — 호칭 칸(저장 v8) + 가족 그룹 자동 제안 · 적응형 배율(2→1.5→1)
- **U29(09-20)** — 폰 속도: 부팅 뒤 CPU 멈춤 1,389→298ms · 지도 파생 절반 크기 · 가공 줄 세우기 · 지도 올 행성은 일간 표면 생략 · 정지 초상은 살아 있는 초상 실패 시만
- **U9(09-19)** — 핀치·과노출 수정 · 시안 조명(용암 발광·노멀맵) · 테두리 변위 · 조화 지도 50장(map5)
- **U8(09-19)** — 4분할 지도 50장(나 + 환경) · 초상 측면 조명 A2 · 설명 문구 · 은하 행성도 지도 · 자가진단 줌 검사 안정화
- **U5 추가(09-19)** — 장신구 모양 확정(무지개 고리 양끝 · 🪐 고리 · 별똥별 1~5 · 진한 먹구름) · 관계 연결 재검토(십신은 전원 해당 · 형 미구현 · 세기 값 없음 → 합충 안 D, 붙는 행성 30%)
- **U7(09-19)** — 초상 v2(55장·통과 방향 목록 실측) · 살아 있는 초상 + 합충 장신구 · 은하 위계 크기 절반
- **U6(09-19)** — 시각 요소 의미 정리(행성·막·속도 = 그 사람 / 거리·크기·장신구 = 나와의 관계 / 연결선 = 그들끼리) · 앱: 위계 크기 · 자전 통일 · 궤도 간격 + 성계 카메라 정렬

R 시리즈 (로드맵·방향)
- R1(09-09) — 로드맵 정본 `docs/로드맵.md` 신설, 어긋난 기록 2건(계측 완료·12운성) 정정
- **R2(09-17)** — 방향 검토(문서 전용). 문구 빈약 정량 진단(99문장/5,575자, 십신 1종 82%,
  역방향 고정 짝 10, 합충 없음 75%, 첫 화면 펼침 309자) · 진태양시 사용자 보정 제안(걸리는
  사람에게만 두 일주 선택, `birth.dayRule`) · 관상/손금(B 이후·사진 폐기·본인만) · 심리/상황
  테스트(A5 엔진 재사용, B 이전) · 순서 재정립(A6→A5→A7→A8→B0→B1~4→B5→C→D) ·
  폰 확인 6건 통과 반영 · A2 조합은 A6로 이관
- **A6-1(09-17)** — 원고 초안. 그룹 해석 진단(사람을 일간 오행 한 글자로만, 유형은 마무리 1문장에만) ·
  개인·관계 143 + 그룹 145 = 288칸/350문장 승인표 · 파이썬 검사 45자 초과 0/용어 노출 0 ·
  파일 정리(PNG 원본·`web2/`·`_t/` → `_보관/`, card-b 353→12MB) · A5 결정 2건 확정 반영
- **A6-2(09-17)** — 치환 1회차. 펼침 조정(관계 ② · 개인 ①②④) · `GANG_PIECE` 4→40 · `PAIR_STORY` 6종
  "둘이 붙으면" · 세기 사연 6 · 인물 패널 첫 화면 본문 359→723자 · 자가진단 58/58 · 갈무리 2장
- **A6-3(09-17)** — 치환 2회차. 자리마다 놓인 기운(30+12운성 12) · 관계 스타일 일간 10 · 돈 스타일 강/약 8 ·
  어울리는 일 세기 꼬리 10 · 합충 년·월·시 9(`hapChungPos`, 카드·케미가 쓰는 `hapChung`은 무수정) ·
  자가진단 61/61 · 갈무리 2장
- **A6-4(09-17)** — 치환 3회차(그룹). `GROLE_LINE`·`GPAN_LINE`·`GTYPE_EL`·`GTYPE_MISS`·`GTOWARD_TYPE` 145문장,
  `groupTopEl`·`groupRole` 도우미, `analysisInsightBody`·`gSections`·`gSecEl`·`groupTowardMe`에 유형 인자 ·
  61/61 · 예시 3그룹 + 합성 1그룹 실측 · 갈무리 3장
- **A6-5(09-17)** — 마무리. 원고↔코드 279/279 대조 0건 · 폰 폭 375 넘침 0 · `pairClauses` 여러 대상 조각 "나가"→"내가" ·
  헤드리스 390px 갈무리는 측정 조건 오류로 폐기 · 61/61
- **A5(09-17)** — 시각 추정 화면 구현. `EST_SLOTS` 13칸·`nextEstQuestion()` 순수 함수·새 시트
  (첫 질문→성향 질문→결과)·`birth.est`(`SAVE_VER` 3, 백업 화이트리스트 포함)·되돌리기. 구현 중
  `PERSON_ITEM_NAMES`가 A6-3의 아홉 번째 항목을 안 반영한 걸 실측으로 잡음 → 고침 + 재발 방지
  자가진단(`personItemAlign`) 추가. 71/71, 저장·새로고침·지우기·이름만 고쳐도 안 지워지는 것 확인
- **A7(09-17)** — 자정 일주 선택. `sajuOf`에 civilDayIdx·apparentDayIdx·`dayRuleAmbiguous` 추가,
  걸리는 사람에게만 "달력 날짜/진태양시" 두 버튼(`birth.dayRule`, `SAVE_VER` 4), 호출부 7곳 배선.
  구현 중 온보딩·여러 명 추가가 est/dayRule 키를 안 넣던 것 실측으로 잡음(`nodeBirthFieldsExplicit`
  재발 방지). 79/79, 실제 온보딩→선택→저장→새로고침→편집 확인. 낡은 문구 4곳 정리
- **A8-1(09-17)** — 심리·상황 테스트 설계·원고. 성향 10문항(계열 10쌍)·SELF/MATCH/GAP·상황 20·공통 8,
  88칸/119문장 검사 통과. `self` 노드 필드·SAVE_VER 5 설계. 관상·손금 체크리스트 보류 [권장]

B 시리즈 (카드)
- B1~B2d — 타로 그림 10장 생성·재생성
- B3~B3i — 카드 합성, 테두리·비율·미리보기 폭·캔버스 높이
- B4a~B4b — 그림 칸 세로형(351×528) 설계·구현
- B5~B10 — 테두리 5장·그림 10장 생성과 재생성(화풍 통일까지)
- B11(09-07) — pic-10 콩 크기 3회 시도 후 보류 / B12(09-07) — pic-10 확정
- B11(09-08) — 그림 상단 정렬 + 명패 확장 / B12(09-08) — ⑫ 명패 3요소 **지시문 초안**(미실행)

C 시리즈 (문구)
- C1 — 관계 문구 개정표 초안(승인용). 합충이 3종이 아니라 "3종 + 없음" 네 상태임을 실측
- C2 — 조각 구조 신설·본문 교체 (`REL_PIECE` 10 / `HC_PIECE` 3 / `REL_DESC` `{b,c}` / `relLead()`),
  `REL_TAKE`·`REL_STRENGTH` 삭제
- C3 — a줄 40개 목록 추출 (읽기 전용)
- C4 — 합충을 앞 문장으로 분리, 상관 조각 축약("뒷심이 짧은 재간둥이"), 관계 패널 딱지 제거
- C5 — b·c줄 20행 진단 (유지 15 / 손볼 것 5)
- C6 — 개정안 5건 치환·검증 통과 (40가지 이상 0건, 최장 38자·평균 24.3자, c줄 "대신" 10/10,
  자가진단 58/58). 상관 c는 "새로 재미난 게 보이면 하던 걸 두고 갑니다"로.
  경계선 3건(편재 "판" / 정관 "지키다" / 정인 "잔소리")은 유지 결정.
  십신 간 겹침 2쌍(겁재↔식신 b, 정관↔정인 c)은 허용.
- C7 — 여덟 코너 문구 전수 추출·진단(읽기 전용). 생일 150,481건을 합성해 실제 호출,
  문자열 7,858개 수집(이상값 0건). 문장 98행 중 **유지 42 / 손볼 것 56**, 개정안 초안 56건.
  큰 문제 셋: ①만 말투가 명사형이라 다름 / ②③이 십신·신강신약 용어를 괄호로 노출 /
  ④⑧에 45자 넘는 문장(최장 67자). 미사용 코드 정리 — `hcChipHtml`·`hcSeq`·`.hcchip`/`.hcwhy` CSS 삭제,
  `wireHapChung`은 호출 2곳이 남아 있어 되돌렸다. 자가진단 58/58, 콘솔 오류 0건.
- C7.5 — 복구 검증 후 안전점 커밋. c1e8031 대비 삭제 89줄을 전수 분류해
  "딱지 관련 37 + C2~C6 문구 교체분 52, 설명 안 되는 삭제 0"을 확인했다.
  `wireHapChung` 정의 1·호출 2 살아 있음, `hcSeq`/`hcChipHtml`/`.hcchip`·`.hcwhy` CSS 0곳.
  자가진단 58/58 · 콘솔 오류 0 · 여덟 코너 8/8. **커밋 `3836841`**(20파일, push 안 함).
  `CLAUDE.md`에 "문자열 검색은 `grep -a`" 한 줄 추가(아직 미커밋).
- C8 — 여덟 코너 문구 치환. C7 개정안 56 + ① 추가 2 = **58건 전부 반영, 보류 0**(코드 편집 47곳).
  ⑤~⑧의 "임의 수정 금지" 주석은 2026-09-09 Teo 승인으로 해제 표시했다.
  인칭을 "이 사람"으로 통일(코너 안 2곳, 코너 밖 13곳은 목록만).
  재검증 146,828표본 — 십신·신강신약·억부·지장간 잔존 0, "당신" 0, 이상값 0.
  남은 것: ⑥ 해설 층 `정재 N · 편재 N.` 23건(개정안 없어 그대로) / 45자 초과 6꼴(최장 68자, 분기표 D).
  자가진단 58/58(옛 문구를 박아 둔 시험 2건은 대조 문자열만 갱신), 콘솔 오류 0. 12,016 → 12,026줄.
- C9 — ⑥ 용어 잔존·길이 초과 정리안(읽기 전용). `정재/편재` grep 23줄 중 **화면에 나오는 건 2줄**뿐임을
  가려냈고(3436 ⑥ 해설 / 9680 사주 표 도움말), 검증 구멍의 원인이 C8 검증이 아니라
  **C7 진단이 ⑤·⑥·⑦의 해설 층을 98행 표에 안 넣은 것**임을 밝혔다.
  C8이 적은 "④ 55번 67→68자로 늘었다"는 잘못된 대조로, 같은 조건이면 **82 → 68자로 줄었다**고 정정.
- C10 — C9 승인분 치환 4곳. ⑥ 해설을 `돈과 실속을 다루는 기운(재성)은 모아 지키는 쪽 N · 크게 키우는 쪽 N입니다.`로,
  ④는 "둘까지는 이름, 셋부터는 개수" 규칙 적용(C8의 문장 쪼개기 되돌림), ⑤는 동률 5개일 때만 개수로.
  ⑤ 동률 3·4개는 실측 42자·45자라 유지(4개는 경계선). 재검증 146,828표본 —
  **십신 잔존 0**, 45자 초과 6꼴 → **2꼴**(최장 68 → 46자), 어미는 31,445문장 중 31,097 정상에
  예외 39종류가 전부 의도된 것(① 물상 10 · ⑧ 뜻풀이 28 · 청유 1). 자가진단 58/58, 콘솔 오류 0.
  12,026 → 12,046줄.
- C11 — 개수 표현 규칙 통일 + 미사용 코드 정리. ⑤ 동률 줄을 ④와 같은 규칙으로 통일
  (셋 42→35자, 넷 45→35자. 길이가 아니라 규칙 통일이 근거). ④ 결여 1개는 전용 한 문장으로
  (`반면 …기운(식상)은 여덟 글자 안에 하나도 없습니다.` 최장 32자) — "갈래가 하나입니다" 어색함 해소.
  미사용 코드 셋 중 **지운 것 0건** — `hcChipHtml`·`.hcchip`/`.hcwhy` CSS는 C7에서 이미 지워져 있었고,
  `wireHapChung`은 호출 2곳(9981·10062)이 남아 보류. 재검증 146,828표본 —
  ㄱ 규칙 일치 ✅ / ㄴ 45자 초과 2꼴(⑧ 지정 제외분뿐) / ㄷ 어미 예외 39종류로 C10과 동일, 새 예외 0 /
  ㄹ 용어 잔존 0. 자가진단 58/58, 콘솔 오류 0. 12,046 → 12,056줄.
- B ⑫ — 명패 맨 윗줄·아랫줄 교체 + 정렬 실측. 윗줄은 `isMe`로 갈라 "내가 보는 ○○"/"○○가 보는 나",
  본인이 안 끼면 이름 둘. 아랫줄 앞 글자는 `STEM_BLURB` 첫 도막(개인 패널 ①과 같은 문구)을 재사용해
  `庚申` → `무쇠·바위 申`(뒷 글자는 지시대로 한자 유지). ⑪의 임의 보정 **+3px 삭제** — 세 블록 모든 줄의
  잉크 상자로 중앙을 잡아 5색 세로차 0~1px. ⑪이 실패했던 명패 상자 경계 측정도 좌표계를 맞춰 다시 해
  **위·아래 차이 5색 전부 0**. 이물 픽셀 네 변 0, 카드 높이 905 유지, 자가진단 58/58, 콘솔 오류 0.
  캡처 `docs/reports/2026-09-05-card-b/b12-명패윗줄-5색.jpg`. 12,056 → 12,087줄.
- B13 — 카드 그림 설명문 조사(읽기 전용, 코드 무수정). 카드가 실제로 읽는 파일은
  `assets/card-b/pic-01~10-*.png`임을 `CARD_PIC`로 확정(옛 판 `01~10-*.png`·`web/`·`web2/`는 안 쓰임,
  2185행 주석의 "web/*.webp"는 낡았다). **파일 번호가 십신 순서와 거꾸로**다(비견 09 … 정인 01).
  10장을 직접 보고 관찰 기록을 남겼다 — 글자는 열 장 다 0건, 공통은 한지 바탕·먹선·2:3.
  카드에 얹힐 때 잘리지 않고 **가로로 1.19배 늘어난다**(원본 2:3 → 칸 4:5).
  자리 실측: 카드 안은 여유 16~37px(한 줄 37px 필요 — 안 들어감, 넣으면 높이 905가 깨짐),
  모달은 폭 490px·한 줄 37자인데 **지금도 4px 넘쳐 스크롤이 있다**(600·1024 값 동일, max-height 774 고정).
- G1 — 2주치 밀린 작업분 커밋(로컬만, push 없음). **미커밋 누적 해소.**
- B14 — 그림 설명 두 줄 적용. `CARD_PIC_DESC` 10개를 십신 번호로 찾아 쓰고,
  왼쪽/오른쪽 십신은 `cardText`의 `side()`에 `g`를 실어 보내 알아낸다(파일 번호는 거꾸로라 못 쓴다).
  넘침 4 → 블록 넣어 53 → **0**(`#cardsheet`만 상한 92vh·840px로, 미리보기 아래 여백 14→6).
  다른 시트 상한은 774 그대로. 짝 검증 3겹(파일명·카드 18장·창 10번) 전부 어긋남 0.
  청소 둘 — 2199행 webp 경로 주석 정정, `wireHapChung` 함수와 호출 2곳 삭제(패널·합충 표시 그대로).
  자가진단 58/58, 콘솔 오류 0, 카드 높이 905 유지. 12,087 → 12,137줄.
  **미커밋**: `index.html`(B14분) · `docs/현황.md` · B14 보고서.
- B15 — 카드 위 설명 자리 조사(코드 무수정, md5로 확인). 캔버스 1080×1125 안에서 카드는
  (30,30,494,905)·(556,30,494,905) — **위 여백 30px, 배경만 있고 글자 0**(16,200점 훑어 밝은 점 0개).
  캔버스 24px이면 설명 열 문장이 **10/10 한 줄**(카드 폭 494, 최장 475)이지만 세로가 1px 모자라,
  캔버스를 **+41px** 키우면 된다(카드 905 불변) → ㉮ 조건부 가능.
  화면 배율 0.452라 카드 한 장이 223px — 겹쳐 얹기는 자리 없음(14px), 위쪽 두 칸으로 두면 11px에서 10/10.
  **저장 카드에 이름이 이미 그려진다**(명패 윗줄 24px). B13의 "상한 774 고정" 오기도 그 문서에 정정 추가.
- B16 — 비교 그림 3장(코드 무수정, md5 동일). 아래 190px은 여백 30 + 서명 블록 130 + 여백 30이라 꽉 차 있고,
  **C안 불가**(카드를 41 내리면 서명 자리가 41px 모자라고, 안 내리면 5px 겹친다).
  캔버스 비를 박아 둔 곳은 **없다** — `syncCardPrevCap`의 RATIO가 `plan.h`를 읽어 저절로 따라간다.
  A안은 `cardGeom().top`을 30 → 71로 올리고 글자 op 둘을 더하는 것이 전부.
  B15의 "최장 475"는 **이름 없는 값**이었고, `테오는 `을 붙이면 543px로 **49px 넘친다**(24px·494px 최대 23자).
- B17 — A안 적용(코드 수정 첫 회차). `CARD_B.descH:41` 신설 + `cardGeom().top = margin + descH` **한 줄**로
  캔버스 1125 → **1166**, 카드 y 30 → **71**, 서명 1021 → 1062(+41), footerH·카드 494×905 불변.
  `CARD_ROLE` 10개와 `cardRoleLine()`(3단계 접기, 한글 아닌 이름은 '은' 고정) 신설.
  **왼쪽 줄의 주어는 그 장에 그려진 사람**(b) — 지시문 예시와 좌우가 반대이나 명패·그림 실측을 따랐다.
  B14의 `fillCardPicDesc` 호출을 끊고 상자를 접었다(상수·함수는 남김).
  검증: 자가진단 58/58, 카드 18장 짝 어긋남 0(최장 384px ≤ 494), 폭 600·1024 넘침 0,
  시트 내용 821 → **787**로 오히려 줄었다. 12,137 → 12,185줄.
  그림: `docs/reports/img/2026-09-09-B17/`.
- B18 — 카드 위 줄의 말투·크기 교체. 「명패 윗줄 + `<역할>`」로 바꾸고(꺾쇠 앞은 `cardDirLabel()` 재사용),
  라벨 24px + 역할 52px을 한 줄에 밑선 맞춰 나란히. `descH:41` 삭제 → `descGap:10`만 두고
  `round(fs.nick×1.3)+descGap = 78` 계산 → 캔버스 1166 → **1203**, 카드 y 71 → **108**.
  넘치면 4단계 접기(역할 2px씩 → 라벨 이름만 → 역할만) — 실측 열 역할·긴 이름까지 **넘침 0**.
  B17의 `cardRoleLine`은 호출만 끊고 남겼다.
  검증: 자가진단 58/58, 카드 18장 어긋남 0(라벨이 명패와 글자 그대로 일치), 폭 600·1024 넘침 0,
  **그림 칸이 안 눌렸다**(327×409 · 화면 148×185 — 작업 전과 같음). 12,185 → 12,220줄.
  [의견] 역할 크기가 카드마다 36~52px로 갈려 한 쌍의 두 장이 달라 보일 수 있다 — 통일 여부는 미정.
  그림: `docs/reports/img/2026-09-09-B18/`.
- B19 — 카드 위 설명을 **두 줄로** 분리. 윗줄 라벨 24px + 아랫줄 `<역할>` 50px, 사이 4px, 둘 다 가운데 정렬.
  **역할 크기를 하나로 고정** — 52px에서 상관 「<판을 뒤집는 재간둥이>」가 496px(494 초과, 지시문 추정 466과 다름)이라
  `cardRolePx()`가 열 개를 다 담는 **50px**을 찾아 쓴다(숫자 안 박음). 18장 전부 50px.
  `descH = labelH31 + lead4 + roleH65 + gap10 = 110` → 캔버스 1203 → **1235**, 카드 y 108 → **140**.
  윗줄 접힘 0건, 아랫줄 넘침 0장. 자가진단이 57/58로 한 번 깨졌는데(`cardWrapNick`이 카드 밖 `<역할>` 줄까지 세었다)
  시험 조건에 `y >= geom.top`을 더해 **58/58** 복구. 카드 18장 어긋남 0, 폭 600·1024 넘침 0,
  그림 칸 148×185 유지. 12,220 → 12,232줄. [의견] 카드 창 여유가 **7px**(819/828)뿐이다.
  그림: `docs/reports/img/2026-09-09-B19/`.
- B20 — 카드 위 **윗줄**을 24 → **34px**. 규정 실측: 화면 최소 글자는 `--fs-sm` **15px**
  (index.html:104 · HANDOFF-v2.md:2911~2915의 2026-08-30 정정 — 옛 11px·13px 규정은 취소선).
  `ceil(15/0.4516)=34`, 아랫줄 50px의 68%(70% 이하)로 두 규칙 동시 충족 → 가장 작은 값 채택.
  숫자를 박지 않고 `CARD_B{minScreenPx:15, prevScale:0.4516, labelMaxRatio:0.7}` + `cardLabelPx()`로 계산.
  배율은 캔버스 폭 비(0.4516)와 그림 칸 비(0.4526/0.4523)로 교차검증.
  descH 110 → **123**, 캔버스 1235 → **1248**, 카드 y 140 → **153**. 카드 창 여유 7 → **2px**(상한 828 유지).
  **작업 B: 지운 것 0건** — `hcChipHtml`·`.hcchip`/`.hcwhy`·`wireHapChung` 셋 다 C7·B14에서 이미 삭제돼
  코드 0건, 주석만 남아 있었다(정정 기록이라 존치).
  검증: 자가진단 58/58, 카드 18장 어긋남 0·넘침 0(윗줄 34 ×36 · 아랫줄 50 ×36), 폭 600·1024·390 넘침 0,
  그림 칸 148×185 유지. 12,232 → 12,251줄.
  [의견] 폰(390)에서는 배율 0.2838이라 윗줄이 9.65px — 거기서 15px을 지키려면 53px이 필요해
  아랫줄 50px보다 커진다(설계 재검토 사항, 범위 밖으로 둠).
  그림: `docs/reports/img/2026-09-09-B20/`.
- B21 — **밀린 작업분 커밋**(이 회차만 커밋 허용). 코드를 한 줄도 안 고쳤다 —
  `index.html` md5가 커밋 전후 동일(`f7b2462dc21148f177fe1760e7a87921`, 950,946 bytes / 12,251줄).
  세 커밋: `fd05c90` 코드(index.html +185/−21) · `719fdec` 갈무리 11장(png 8.5MB) ·
  `eb29b5f` 보고서 8건(+1074). 그림을 보고서보다 먼저 넣었다(보고서가 갈무리를 참조해서).
  **index.html은 나누지 않았다** — 헝크 `@@ -2252,4 +2281,89 @@` 하나에 B14의 `CARD_PIC_DESC`와
  B17~B20의 `CARD_ROLE`·`cardRolePx()`·`cardLabelPx()`·`cardTopLine()`이 붙어 있고,
  B17이 B14의 `fillCardPicDesc()` 호출을 끊어서 B14만의 중간 상태가 지금 파일에 없다.
  뺀 파일 0건 — `my-data.js`·백업 json·`assets/card-b/web2/`가 `.gitignore`로 제외된 것을 명령으로 확인.
  검증: 자가진단 **58/58**, 브라우저 실측이 지시문 값과 전부 일치
  (labelPx 34 · rolePx 50 · descH 123 · 카드 y 153 · 494×905 · `typeof wireHapChung` = undefined),
  HTTP 200 950,946 bytes. `git fetch` 후 `origin/main` `ecbb17d`, 로컬 **10커밋 앞섬**. push 안 함.
  그림: 없음(화면 변경이 없어 갈무리할 것이 없다).
- B22 — 카드 창 세로 스크롤 제거 + ⑪ 남은 확인 2건 + 이 문서 재작성.
  **[1단계]** `#cardsheet` 상한 `min(92vvh, 840px)` → **`min(94vvh, 856px)`** 한 줄.
  착수 실측에서 폭 600·1024가 **상한 828 / 자연 높이 829 → 1px 넘침**이었다(390은 여유 143px, 넘침 0).
  B19·B20의 "여유 7 → 2px"은 `clientHeight`(내용 상자)를 상한(테두리 상자)에 맞대 본 오진이다 — 테두리 1.82px 누락.
  **되먹임 발견**: 막대가 뜨면 시트 폭 −10 → 미리보기 −11.6(폭×1248/1080). `.93`(837)만 올렸더니
  막대가 사라져 카드가 567→577로 커지며 자연 높이가 839가 돼 **2px 재초과**. 막대 없는 상태(839)를 기준 삼아
  `.94`(846)로 잡았다. 940‰ 상한은 지시가 정한 한계다. px 쪽도 840→856으로 올려야 846이 실제로 나온다.
  검증: 600·1024 **넘침 0 · 여유 7**, 1440×1200에서 856 상한 걸리고 넘침 0, 화면 밖 0, 반복 측정 잡음 0.
  **[2단계 ⑪ (1)]** 자가진단 `nodeLabelZoom` 실패는 **회귀가 아니다**. 시험이 요구하는 것은
  `camera.position`이 `cam.r`을 따라간 상태이고, 그건 `camTick()` **한 번**이면 만들어진다(실측 220→75 즉시).
  현재 코드에서는 재현되지 않았다(세 조건 시도). 실패가 나오는 유일한 상태는 `camTick()`이 아예 안 돈 것이고
  그때 행성 지름이 7.9~9.4px(문턱 26)로 떨어져 "나" 이름표만 켜진다 — **시험은 살아 있다**(이빨 확인).
  마지막 실패 기록은 2026-09-04 A구현 회차이고 그 보고서가 원인을 "백그라운드 탭"으로 적으며
  개편 전 빌드도 같은 조건에서 같은 1건이 실패했음을 함께 적어 회귀가 아님을 이미 확인했다.
  **[2단계 ⑪ (2)]** 원본 그림 10장 가장자리 — **흰 테 0장 · 검은 테 0장**. 맨 바깥 줄(d0)과
  안쪽 줄(d1·d2·d4·d8·d12) 평균 밝기가 같아(예: pic-10 top 140.4 / 140.8 / 141.1 / 140.8 / 140.7 / 140.8)
  테두리 선이 아니라 그림 내용이 가장자리까지 이어진 것이다. 가장 긴 연속 검정도 변 길이의 **7.0%**(pic-10)뿐.
  **[3단계]** 이 문서의 로드맵·대기열·지금 위치·미결·커밋 이력을 실제 기록에 맞춰 다시 썼다.
  검증: 자가진단 **58/58**, 앱 콘솔 오류 **0**(크롬 확장 메시지 3건은 앱 코드 아님),
  여덟 코너 **8/8**(`.cnrfact` 4 · `.nicktake` 4 · 빈 코너 0), 카드 창 정상.
  `index.html` 950,946 → **953,069** bytes · 12,251 → **12,272**줄 ·
  md5 `f7b2462d…` → `be776879c86c363a0431efa719a4304a`. 그림: 없음.
- B23 — 카드 그림 **PNG → WebP**. 카드가 읽는 소재는 `pic-01~10` 10장 + `border-{wood,fire,earth,metal,water}`
  5장 **총 15개**로 확정(`cardAsset()`이 `CARD_B_DIR+key+확장자`를 이어 붙인다, index.html:2697).
  **지시문 전제 2건 정정** — ⓐ 테두리는 카드 한 장에 **최대 2장** 받는다(양쪽 오행이 다르면 다르다),
  ⓑ `frame.png`는 **안 읽는다**(그리기 명령 이름이 `op:'frame'`이라 헷갈리지만 키는 `border-*`다).
  변환: Pillow 12.3.0 · `method=6` · 무손실→q92→q88 사다리. **무손실은 15장 전부 70% 초과**
  (70.8~77.7%, `border-water` 121.9%·`pic-08-sanggwan` 136.9%는 PNG보다 커짐) → **전부 q92**.
  **84.24MB → 11.13MB (86.8% 감소)**, 카드 한 번 열 때 19.05 → 2.08MB (89.1% 감소),
  적재 34~101ms → 1~23ms. 가로세로 불변.
  화질 — 낱장에서는 그림 10장 결 보존 87~111%로 멀쩡하지만 **테두리 5장이 43~57%**로 걸린다.
  그래서 **최종 저장 카드(1080×1248)를 PNG 소재/WebP 소재로 각각 그려 픽셀로 맞댔다** — 18쌍,
  소재 15개 전부 태움: **최대차 16 · 평균차 0.39~0.51 · 차>16 픽셀 0% · 빠진 소재 0**.
  소재가 카드에서 20~32%로 줄면서 q92 차이가 평균되어 사라진다 → **분기표 A**(분기표 B·C·D·E 미발동).
  되돌림 — `<img>` `onerror`에서 같은 이름의 PNG를 다시 받는다. 없는 확장자로 강제 실패시켜
  **PNG 4장 재요청·빠진 소재 0·카드 정상**을 확인했다. `CARD_ASSET_EXT` 선언은 TDZ 때문에
  `cardAsset()` **위**에 뒀다(처음에 아래 뒀다가 옮김).
  검증: 자가진단 **58/58**, 콘솔 오류 **0**, 카드 높이 5색 전부 905, 그림 칸 327×409 등 **전과 같음**.
  `index.html` 953,069 → **954,738** bytes · 12,272 → **12,291**줄 ·
  md5 `be776879…` → `ae4049571692071270b30af6ac27f877`. 새 파일 `assets/card-b/*.webp` **15장 11.13MB**.
  **안 읽는 옛 파일 86.58MB는 목록만 만들고 안 지웠다**(옛 그림 10장 59.14 · `frame*` 4장 26.33 ·
  `web/` 11장 1.10). `web2/` 29장 171.24MB는 `.gitignore`라 커밋에 없다.
- R1 — 로드맵 정본 `docs/로드맵.md` 신설(문서 전용, `index.html` 무수정). 로드맵이 흩어진
  세 곳(HANDOFF §5-0·§6·§8) 확인, §5-0·§8에 낡음 표시(§6은 계산 단계표라 다른 개념 — 제외).
  **분기표 C 두 번 발동** — ① 계측(GoatCounter)이 2026-09-05에 이미 배포·검증까지 끝났는데
  옛 문서 세 곳이 "Teo가 계정·코드 넣어야 함"으로 계속 캐리하고 있었다(커밋 `7f3b6dd`·`846a248`이
  이미 `origin/main`에 있음). ② 12운성이 "논의된 적 없음"이라는 지시문 전제와 달리 실제로는
  계산·표시는 있고 해석 문장에서만 뺀 상태(사용자 판단 이미 내려짐, index.html:3288).
  `docs/현황.md`의 로드맵 절을 포인터로 교체, `HANDOFF-v2.md` §5-0·§8에 낡음 표시 부착(삭제 없음),
  `CLAUDE.md` 상비 규칙에 "회차 시작 시 docs/로드맵.md 확인" 1줄 추가.
- A1 — 기운 세기(신강신약) 관계 문단 얹기 **진단**(읽기 전용, `index.html` 무수정).
  `personGangyak()`이 이미 있고 호출처 0곳임을 확인(연결부 완성, 새로 안 만들어도 됨).
  분포 실측 73,409표본(1900~2100, 하루 간격) — 극약 24.84%·신약 43.39%·신강 26.37%·극왕 5.41%
  (시각 모름·삼주는 극왕 2.21%로 더 줄어듦). 양 끝 합 30.25%로 **분기표 A**(조각 4개, B 기각).
  1차 초안("혼자/곁을 빌려 쓰는" 축)이 b줄과 정면 충돌해 **축을 "기운의 양"으로 바꿔** 재작성.
  160조합(십신10×합충4×세기4) 전수 자수 검사 45자 초과 0건. 승인용 문구·조립 예시 표를
  보고서 맨 앞에 제시. 남은 어긋난 조합 1건(상관+아주 센)을 목록으로 보고.
- A2 — A1 승인 문구를 관계 문단에 **적용**(코드 편집). `pairBlock`/`pairReading`이 인물 패널
  (showNode)·관계 패널(showEdge) **양쪽에서 호출됨을 확인** → **분기표 B 발동**. 선택 인자
  `withStrength`를 추가해 관계 패널 호출부(`pairBlock(e.a,e.b,undefined,true)`)에서만 켰다.
  인물 패널 호출부는 한 글자도 안 바꿈 — 회차 전후 렌더 HTML을 문자 단위로 대조해
  **완전 일치(10,178자==10,178자)** 확인. `GANG_PIECE` 상수를 `HC_PIECE` 아래에 신설,
  `personGangyak()`을 그대로 재사용(새 함수 안 만듦). 분기 D(생년월일 없음)도 실측 확인 —
  조각이 공백·마침표 겹침 없이 깨끗이 생략됨. 관계 패널에서 위/아래 문단이 각자 다른 사람의
  세기로 서로 다르게 나오는 것을 실측(테오=극약/아내=신약 표본, 폭 600·1024). 자가진단 58/58
  (nodeLabelZoom 포함 1회 통과), 콘솔 오류 0. `index.html` 954,738 → **956,931** bytes ·
  12,291 → **12,311**줄 · md5 `ae404957…` → `8d78426cdbcf718d5e1844fc8e5f3ec8`.
  극왕 조각 자수 실측 21(지시문 기대 20, 콤마·마침표 포함 셈 차이로 추정, 45자 상한에는 무영향).
- A3 — 태어난 시간 추정 기능의 **값어치를 숫자로 진단**(설계·구현 없음, `index.html` 무수정).
  표본 2,000일 × 24시간(경도 서울) — 갈래 수 평균 **13.03**(13~15, 12지지 시진 + 자정 전후
  예외), **갈래 1개(=시각 무관) 비율 0%**. ㉠ 세기만 보면 평균 1.80갈래, 시각 따라 단계가
  바뀌는 비율 **70.2%**, 최대 3단계 벌어짐. 삼주(시각모름) 매칭 — 전체 결과 기준 **0% 일치**
  (구조적: 오행 글자 수 6 대 8이라 원천적으로 못 맞음, 지시문 5번 방법을 세기 단독 비교로
  바꿈), 세기만 보면 대략 80% 시간대와 일치하되 **0시만 50.6%로 예외**. 질문 개수 산수 —
  표본 97~100%가 이론상 **4개**(⌈log2(13~15)⌉)로 수렴. **부수 발견**: 진태양시 보정이
  자정 근처에서 조자시(시주 천간 전용)와 별개로 **일주 자체를 하루 밀 수 있음**을 실측으로
  확인(1960-01-01 0시 입력 → 진태양시 23시로 환산되어 전날 일주가 나옴).
  분기표 B(오래 연 페이지 정지) 2회 발동 — 매번 새로고침으로 즉시 해소, 표본 축소 없이 완주.
  `index.html` 착수·종료 완전 동일(956,931 bytes / 12,311줄 / md5 `8d78426c…`), 콘솔 오류 0.
- A4 — 시간 추정 **갈림 실측 + 설계 확정**(읽기 전용, `index.html` 무수정). 표본 300명 ×
  12칸 × 8항목(고정 시드 20260909, 재현 확인). **질문 재료 우선순위 확정** — ④ 넘치는 것
  8.48갈래 > ⑤ 어울리는 일 7.91 > ⑥ 돈 스타일 4.03 ≈ ⑧ 힘이 되는 기운 3.89 > ② 3.28 > ③ 1.97.
  **첫 질문(다섯 구간) 가점 2 : 성향 1**이 최적(W=1~4 비교, 3 이상은 개선 없음).
  모의실행 — 첫 질문 있음 **평균 2.32문항(첫 질문 포함 3.32) · 유일 1위 적중 91.3%**,
  **20% 오답에도 78.0%**(첫 질문 없으면 16.7%로 무너짐). 점수제라 **진짜 시각이 탈락한 경우 0건**.
  **★ 가장 큰 발견 — 자시(23–01)는 다른 11칸과 다르다**: 일간만 쓰는 ①·⑦이 96.3% 갈리는데
  그 289명 **전원이 "자시만 다른" 패턴**(자시 아닌 곳에서 갈린 사람 0명), 자시(0시)와 축시(2시)의
  일주가 다른 사람 50명 중 48명, 자시 **안에서도** 23시 vs 0시가 100명 전원 갈림(8항목 중 평균 6.47개).
  → **후보는 12칸이 아니라 실질 13칸**. 13칸으로 늘렸을 때의 문항 증가는 **재지 않았다(추정)**.
  성별은 계산에 영향 0(20명 대조). 코드 주석 어긋남 1건 발견 — 3049행 "정오를 가정하지 않는다"와
  실제 동작(3058행이 절기 판정에 정오를 씀)이 표면상 상충(3057행 주석이 그 뜻을 설명한다).
  옛 **"대략" 모드 폐지 이력**(2026-08-29, "정확한 척" 우려)을 찾아 설계 문서에서 정면으로 다뤘다.
  산출물: `docs/시간추정-설계.md`(설계 정본) · `tools/a4-time-estimate-sim.js`(재현 스크립트).
  `index.html` 착수·종료 완전 동일(956,931 bytes / 12,311줄 / md5 `8d78426c…`).

## 커밋 이력

~~로컬 · push 전~~ → **[2026-09-09 커밋 회차] push 완료.** `origin/main`이 `70f1190`으로
로컬과 완전히 같다(`git rev-list --left-right --count origin/main...main` → `0  0`).
배포 주소(https://shyquisyz.github.io/group-saju/)도 반영 확인 — md5가 로컬 `index.html`과
바이트 단위로 일치한다(`8d78426cdbcf718d5e1844fc8e5f3ec8`). 아래 열여섯은 이제 원격에도 있다.

| 해시 | 제목 | 내용 |
|---|---|---|
| `ecbb17d` | fix(카드B): pic-10-geopjae 확정 | ← 원격이 여기까지 |
| `c1e8031` | ⑨~⑪ 명패/렌더 작업 마무리 | 카드 ⑨~⑪ |
| `3836841` | C2~C7: 관계 문구 조각 구조·b·c줄 개정, 여덟 코너 진단, 미사용 코드 정리 | C7.5 안전점 |
| `ff6f34a` | 문구 C2~C11 개정 + 카드 B12 명패 교체 | index.html (+128/−57) |
| `3f970b6` | 회차 보고서 및 진행 문서 갱신 | 보고서 7 + 갈무리 1 + CLAUDE.md |
| `bd32474` | 기타 변경분 | 이 표를 담은 `docs/현황.md` + G1 보고서 |
| `f88741a` | 문서: 커밋4 해시(bd32474)를 현황·보고서 표에 채움 | 위 표의 `bd32474` 자리를 채운 한 줄 |
| `fd05c90` | 카드 위 설명 두 줄 도입 + 그림 설명·미사용 코드 정리 (B14·B17~B20) | `index.html` (+185/−21) |
| `719fdec` | 회차 갈무리 그림 11장 추가 (B16~B20) | `docs/reports/img/**` png 11장 · 8.5MB |
| `eb29b5f` | 회차 보고서 B14~B20 추가, B13 보고서 후속 표기 보완 | 보고서 8건 (+1074) |
| `18ffd3b` | B21 보고서 추가, 현황 문서를 커밋 이력까지 갱신 | `docs/현황.md` + B21 보고서 |
| `01fcf32` | 카드 창 세로 스크롤 제거 + 그림 PNG→WebP 경량화 (B22·B23) | `index.html`(카드 헝크) + webp 15장 + 보고서 2건 |
| `385985c` | 로드맵 정본 docs/로드맵.md 신설 (R1) | `docs/로드맵.md`(신규) + `HANDOFF-v2.md` + R1 보고서 |
| `ecada04` | 상비 규칙에 로드맵 정본 확인 지시 추가 | `CLAUDE.md` (+3) |
| `00b6b47` | 관계 패널에 기운 세기(신강신약) 문장 얹기 (A1 진단·A2 적용) | `index.html`(A2 헝크) + A1·A2 보고서 |
| `43fc63e` | 태어난 시각 추정 기능: 값어치 진단 + 설계 확정 (A3·A4) | 설계서 + A3·A4 보고서 + `tools/a4-time-estimate-sim.js` |
| `70f1190` | 안 쓰는 옛 소재 web/ 11장 삭제 + 현황 문서 커밋 이력 갱신 | `assets/card-b/web/*` 삭제 + `docs/현황.md` |

**마지막 커밋 `70f1190`이 `origin/main`과 완전히 같다** — push 완료(아래 참고).
표의 해시 칸은 **그 커밋을 만든 다음 회차가 채운다** — 커밋은 자기 해시를 담을 수 없다
(적어 넣고 amend하면 곧바로 죽은 해시가 된다. B21에서 한 번 밟았다). 이번 표의 마지막 두 줄
(`43fc63e`·`70f1190`)은 다음 회차가 아니라 **같은 회차 안에서** 다음 커밋이 채웠다 —
push 전에 미리 적어 넣지 않고, 커밋을 만든 뒤 그 해시를 실측해 채워 넣는 순서를 지켰다.

**index.html 한 파일을 두 커밋(01fcf32·00b6b47)으로 쪼갰다** — `git diff`의 헝크가
카드(B22·B23) 구역과 관계 패널(A2) 구역으로 물리적으로 안 겹쳐, `git apply --cached`로
헝크를 나눠 스테이징했다(패치를 워킹트리가 아니라 **인덱스 기준**으로 적용해야 헝크가
분리된다 — 자세한 경위는 `docs/reports/2026-09-09-커밋배포.md` 2단계).

**미커밋 0건. push 완료(`0 0`). 배포 주소 md5도 로컬과 일치.**

## 미결

**해결된 항목은 이 칸에서 지운다** — 경위는 위 「회차 이력」에 남는다.
[2026-09-09 B22에서 지운 것] 카드 ⑫ 미실행 · ⑪ 미해결 4건 · 미사용 코드 정리 3건 ·
미커밋 누적 · 모달창 스크롤. 다섯 다 끝났고 근거는 회차 이력의 B⑫ · B20 · B21 · B22 항목에 있다.

### Teo가 결정할 것

~~**[2026-09-17 B0 뒤 — 사용자: "나중에 한꺼번에 정하자"] 모아 둔 결정 5건**~~ → **[2026-09-19 U16] 전부 권장안으로 확정**: Supabase(서울) · 카카오+구글 · 로그인 없이 본인+2명 · 남의 정보는 기기에만 · 명패 아랫줄 동물 이름(적용 완료). 같은 자리에서 추가 확정: 호칭 칸 + 가족 그룹 자동 제안 만든다 · 커밋 이력의 호칭·그룹명은 **앞으로만** 안 남긴다(이력은 그대로) · 폰 속도는 다음 회차에 고친다. **다음 사용자 할 일: Supabase 프로젝트(서울)·카카오 앱 만들기** (B0 §8)
1. 서버 서비스 — Supabase [권장] / Firebase / 자체 서버 (`2026-09-17-B0-계정서버설계.md` §2)
2. 로그인 수단 — 카카오 + 구글 [권장] (§3)
3. 로그인 없이 쓰는 범위 — 본인 + 2명까지 [권장] (§8-3)
4. 다른 사람 이름·생년월일을 서버에 둘지 — 두지 않는다(기기에만) [권장] (§8-4)
5. 명패 아랫줄 `申` 그대로 / 띠 이름 — 아래 항목(2026-09-09부터 미결, A6에서 안 다뤘다)
+ 직접 하실 일: Supabase 프로젝트(서울)·카카오 앱 만들기 — 1·2가 정해진 뒤 (§8)

- **명패 아랫줄 뒷 글자** — 지지 한자 `申`을 그대로 둘지, 띠 이름(`원숭이`)으로 풀지.
  지금은 `무쇠·바위 申`처럼 쉬운 말과 한자가 한 줄에 섞인다 (근거: `2026-09-09-B12-명패윗줄.md` 「보류·실패」)
- ~~**밀린 눈 확인** — 상세 목록은 `docs/로드맵.md` §5 참고~~ → **[2026-09-17 R2] 6건 전부 통과.**
- ~~**A2 「말이 안 되는 조합」 1건** — 상관 + 극왕 조합이 읽는 방향이 살짝 엇갈린다~~
  → **[2026-09-17 R2] 보류, A6로 이관.** 사용자가 개별 판단을 못 내리겠다고 했고, 원인이
  `GANG_PIECE` 4문장이 십신 10종에 똑같이 붙는 구조라 십신×세기 복원 때 함께 푼다(R2 §2-3).
- ~~**순서 승인** — A6 → A5 순서로 갈지~~ → **[2026-09-17 확정] A6 먼저.**
- ~~**12운성을 해석 문장에 쓸지**~~ → **[2026-09-17 정정] 끝났다** — A6-3이 "자리마다 놓인 기운"에
  일지 12운성을 넣었다(`2026-09-17-A6-3-치환2.md`).
- ~~**원고 승인표 288칸**~~ → **[2026-09-17 정정] 끝났다** — 사용자가 권장대로 진행을 승인, A6-2~5 치환 완료.
- ~~**"태어난 해 = 출발점" 서술**~~ → **[2026-09-17 정정] 끝났다** — 사용자가 판단을 맡겼고 단정 없이
  성향으로 고쳐 썼다(커밋 `bec471c`).
- ~~**계측** — GoatCounter 계정·사이트 코드 입력. 코드는 심어져 있다~~
  → **[2026-09-09 R1 정정] 이미 끝났다.** 배포까지 확인됨(`docs/reports/2026-09-05-계측켜기.md`,
  근거는 `docs/로드맵.md` 부록 B). **미결에서 뺀다.**
- ~~**push 여부** — 로컬이 `origin/main`보다 10커밋 앞서 있다~~ → **[2026-09-09 커밋 회차]
  끝났다.** 여섯 커밋으로 나눠 커밋·push, 배포 주소 반영까지 확인. 상세는
  `docs/reports/2026-09-09-커밋배포.md`
- ~~**A3 — 시간 추정 기능을 만들지, 만든다면 질문 몇 개로 할지.**~~
  → **[2026-09-09 A4] 만들기로 하고 설계까지 확정했다**(`docs/시간추정-설계.md`).
  **대신 A5 착수 전 결정 2건이 새로 생겼다:** → **[2026-09-17 정정] 둘 다 확정·구현 끝**(ⓐ 나눈다,
  ⓑ `birth.est` 분리 — `2026-09-17-A5-시각추정-구현.md`). 아래 두 항목은 경위로만 남긴다.
- ~~**A5-ⓐ 자시를 13칸으로 나눌지.**~~ 자시(23–01)는 계산상 두 갈래다(23시대 / 0시대).
  안 나누면 자시로 추정된 사람의 결과가 **절반 확률로 어긋난다**. 나누면 "밤 11시쯤인가,
  자정 넘어서인가"를 한 번 더 물어야 한다(사실 질문이라 정확도는 높다).
  문항이 얼마나 느는지는 **아직 안 쟀다** (근거: `2026-09-09-A4-갈림실측.md` 2-6절)
- ~~**A5-ⓑ 추정값을 어디에 저장할지.**~~ 옛 "대략" 데이터가 `birth.sijin`에 남아 있고 화면이
  그것을 "예전에 넣으신 시간대"로 보여 준다(index.html:9940). 같은 자리에 넣으면 그 안내와
  섞인다 (근거: `docs/시간추정-설계.md` §7)

### 남은 코드 일감
- ~~**A6 — 해석 원고 확장**~~ → **[2026-09-17 A6-5 완료.]** 99 → 약 449문장. 코드가 정본. ~~A6-1 완료, 승인 대기~~ → 승인됨. 처방 8항목은
  `2026-09-17-R2-방향검토.md` §2-4, 원고·치환 순서는 `2026-09-17-A6-1-원고초안.md` §3·§5.
  치환은 A6-2(펼침·세기·둘의 관계·사연) → A6-3(자리·코너·합충) → A6-4(그룹) → A6-5(검증·갈무리)
- ~~**A5 — 시간 추정 화면 구현**~~ → **[2026-09-17 완료.]** `2026-09-17-A5-시각추정-구현.md`. 71/71
- ~~**A7 — 자정 일주 선택**~~ → **[2026-09-17 완료.]** `2026-09-17-A7-자정일주선택.md`. 79/79
  ~~**진태양시 보정으로 날짜가 하루 밀리는 문제** — 별건 … 따로 정해야 한다~~ → A7이 그 답
- ~~**A8 — 심리·상황 테스트**~~ → **[2026-09-17 A8-2 완료.]** `2026-09-17-A8-2-구현.md`. 93/93.
  (옛 기록) ← **[A8-1 설계·원고 완료] 다음은 A8-2 구현.** [2026-09-17 R2] ~~**심리테스트 메뉴에 태어난 시각 검증 넣기** …
  **B(계정·서버) 이후**~~ → **B 이전으로 당김.** A5의 `nextQuestion()` 재사용, 성향 프로파일·상황
  맞춤·시각 검증·관상/손금 체크리스트판. 답변은 계산을 바꾸지 않는다. R2 §5
- **B5 — 관상·손금 사진 분석** — [2026-09-17 R2 신설] B 이후. 본인만·사진 즉시 폐기·별도 동의. R2 §4
- **D 뿌리기 설계** (공유 문구·뿌릴 곳) — 로드맵상 A 묶음 뒤
- ~~신강신약을 조각으로 얹는 회차 (`REL_STRENGTH` 원문은 C2 보고서에 보존)~~
  → **[2026-09-09 A2 완료]** 관계 패널에 `GANG_PIECE` 조각으로 얹었다(A1 진단 → A2 적용).
  인물 패널은 의도적으로 제외(중복 방지, 위 「Teo가 결정할 것」의 밀린 눈 확인에 포함).
- 회차 갈무리 png 8.5MB(`docs/reports/img/`)를 webp로 줄일지 — B23에서 카드 소재만 했고 이쪽은 안 건드렸다
- **안 읽히는 옛 소재 86.58MB 정리** — 옛 그림 10장(59.14) · `frame*` 4장(26.33) · `web/` 11장(1.10).
  전부 배포에 실리는데 코드가 한 번도 안 읽는다. B23 보고서에 목록이 있다.
  [권장] `web/` 11장만 지운다 — 800×993 옛 축소본이라 원본 대체물이 못 되고 B23 webp가 그 자리를 대신한다.
  `frame*`과 옛 그림 10장은 **원본 성격이라 남긴다**
- 리넨 데모(demo/linen srcset·세로 컷) 보고 미처리

### 남겨 두기로 한 것 (일감 아님)
- **⑧ 오행 뜻풀이 45자 초과 2꼴** — 그룹 패널과 공용이라 일부러 안 건드린다 (C10·C11 결정)
- **그림이 카드에서 가로 1.19배 늘어남** — 원본 2:3 → 칸 4:5. 그대로 두기로 결정 (B13)
- **폰(390)에서 카드 위 윗줄이 화면 9.65px** — 미리보기 배율 0.2838 때문이라 글자로는 못 푼다.
  미리보기를 키우거나 "카드는 예외"로 규정에 적어야 한다. 뿌리기 전에 결정 (B20)
- **이름 21자쯤이면 카드 창 제목이 두 줄로 접혀 21px 스크롤** — 원래 동작

### 근거 없음 (확인 필요)
- `nodeLabelZoom`이 **왜 백그라운드 탭에서 실패했는가**의 정확한 기전.
  2026-09-04 보고서가 원인을 그렇게 적었지만, B22에서 지금 코드로는 재현하지 못했다
  (`camTick()` 한 번이면 카메라가 따라붙는다). 회귀가 아니라는 것은 확실하고,
  기전은 미확인으로 남긴다

## 로컬 직링크
http://127.0.0.1:8934/index.html
