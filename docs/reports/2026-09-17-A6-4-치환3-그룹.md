# A6-4 · 원고 치환 3회차 — 그룹 145문장 [2026-09-17]

**실행 모델**: Fable 5.1. **적정성**: 과했다(절차 회차). 다음 A6-5(검증·갈무리·정리)는 **Sonnet 5 [권장]**.
**비용 수준**: 중 — `index.html` 12곳 수정(표 5개·도우미 2개 신설, 함수 6개 인자 추가), 로컬 실측·자가진단·갈무리 3장.

---

## 세 줄 요약

1. **그룹 패널이 처음으로 유형·오행·역할을 문장으로 말한다.** 구성원마다 "이 판에서 무슨 자리인지" 한 줄(G1 25),
   가장 많은 오행 × 역학으로 판의 성격(G2 15), 그룹 유형 × 가장 많은 오행(G3 35), 유형 × 없는 오행(G4 35),
   나 기준 가장 많은 계열 × 유형(G5 35) — 145문장 전부 코드에 들어갔고 키 누락 0.
2. **계산은 한 줄도 안 바뀌었다.** `analyzeGroup`이 이미 세던 `saengGive`·`geukGive`·`geukRecv`와 `gets`로
   역할을 고르고, 오행 동률이면 G2·G3·G5를 안 붙인다(어느 쪽이라 말할 수 없으므로 종전 문장 유지).
3. 자가진단 **61/61**, `node --check` 통과. 예시 그룹 셋 + 합성 그룹 하나(토 쏠림·순환형)로 다섯 표 전부 화면에서 확인.

---

## 0. 착수 실측
```
git rev-list --left-right --count origin/main...main → 0 7
```

## 1. 바뀐 곳

| 원고 | 어디에 붙나 | 무엇 |
|---|---|---|
| G1 역할 | `gSecFlow` — "흐름" 문장 아래 구성원 수만큼 | `GROLE_LINE[오행][역할]`, 역할은 `groupRole()`(누르는 > 눌리는 > 대는 > 모이는 > 가운데). 이름·조사는 `fillPair` |
| G2 판 성격 | `analysisInsightBody` — ~~"쏠림 성계" 한 줄~~ 자리 | `groupTopEl(A)`가 하나로 정해질 때 "n인 중 m명이 X 기운입니다." + `GPAN_LINE[오행][역학]`. 동률이면 종전 "고르게 섞인" |
| G3 유형×오행 | `showGroup` INSIGHT 상자, `GTYPE_CLOSER` 앞 | `GTYPE_EL[유형][오행]`, 동률이면 생략 |
| G4 유형×결여 | `gSecEl(A,gtype)` 해석 층 | 첫 결여 오행만 `GTYPE_MISS[유형][오행]`, 둘째부터 "X 기운도 없습니다." 유형 없으면(자가진단·관계 보기) 종전 문장 |
| G5 나에게 | `groupTowardMe(ms,here,gtype)` 끝 | 나 기준 최다 계열이 하나일 때 `GTOWARD_TYPE[계열][유형]` |
| 배선 | `analysisInsightBody(A,mode,gtype)` · `gSections(A,gtype)` · `showGroup`이 `g.type`을 넘김 | 관계 보기(`relview`)·가상 성계·미분류는 유형이 없어 **종전 그대로** |

## 2. 실측 (예시 인물 8명, `my-data.js` 뺀 사본)

### 2-1. 자가진단 · 문법
```
node --check → OK · sajuSelfTest() → 61/61 (?v=a64-1, ?v=a64-2 두 번)
표 키 완전성(콘솔): G1 25 · G2 15 · G3 35 · G4 35 · G5 35, 누락 키 0
```
자가진단이 `gSecFlow({ms})`처럼 집계 표 없이 부르는 경우가 있어 `groupRole`이 `geukGive` 등이 없으면 0으로 본다 —
그 가드가 없을 때 `gFlowRing`류 검사가 던졌을 것이다(넣고 나서 61/61).

### 2-2. 예시 그룹 셋 — 실제로 나온 문장

**가족(4인, 목1·화1·토1·금1, 혼합형)** — 오행 동률이라 G2·G3·G5는 안 붙고(설계대로), G4·G1이 붙었다.
> 사주은하의 해석 — 집안에 스며드는 기운이 없어, 속 얘기가 안 나옵니다.
> 나는 이 판의 쇠를 달구는 자리라, 누르는 쪽입니다. / 어머니는 이 판의 곁가지를 쳐내는 자리라, 누르는 쪽입니다. /
> 아버지는 파고드는 힘을 받는 자리라, 버텨 주는 동안 판이 섭니다. / 동생은 이 판의 흙을 파고드는 자리라, 누르는 쪽입니다.

**친구들(3인, 목1·화1·수1, 혼합형)**
> 친구들 사이에 받치는 기운이 없어, 힘들 때 받아 줄 자리가 얇습니다. 금 기운도 없습니다.
> 나는 물에 식는 자리라, 기세가 과할 때만 이 눌림이 약이 됩니다. / 친구는 뻗어 나가는 기운으로 이 판에 불씨를 대 줍니다. /
> 선배는 이 판의 열을 식히는 자리라, 누르는 쪽입니다.

**회사(2인, 화1·토1, 순환형)** — G5가 붙는 첫 사례.
> 나에게 이 그룹은 만들어 내고 표현하는 기운(식상)을 지닌 한 사람입니다. **이 팀에서 당신은 내주는 쪽입니다. 생각과 손이
> 당신에게서 나옵니다.** / 팀에 뻗어 나가는 기운이 없어, 치고 나가는 사람이 없습니다. 금·수 기운도 없습니다. /
> 나는 태우고 남긴 것을 판에 대 주는 자리입니다. / 동료에게 결과물이 쌓입니다. 받아서 다지는 자리입니다.

### 2-3. 합성 그룹(나·아버지·동료 = 화1·토2, 동료, 순환형) — G2·G3가 붙는 경우
> 3인 중 2명이 토 기운입니다. **받치는 기운이 많고 부딪힘이 없어, 편하지만 몇 년째 같은 모양인 판입니다.** (G2)
> 동료 · 순환형 INSIGHT — **자리를 지키는 동료라, 안정적이지만 먼저 나서는 사람이 없습니다.** 생의 사슬이 업무 흐름과 겹치면… (G3 + 종전 마무리)
> 나에게 이 그룹은 만들어 내고 표현하는 기운(식상) 둘이 모인 곳입니다. 이 팀에서 당신은 내주는 쪽입니다. … (G5)

### 2-4. 관계 보기(3인 즉석 조회)는 그대로
`showRelationView([나,어머니,아버지])` — 유형이 없으므로 G3·G4·G5 없이 종전 문장. G2 자리는 동률이라 종전 "고르게 섞인 조합". 오류 0.

### 2-5. 갈무리
- `img/2026-09-17-A6-4/그룹패널-가족.png` · `그룹패널-친구들.png` · `그룹패널-합성팀-토쏠림.png` (380px 패널 열)

## 3. 관찰 — 이번 범위 밖, 다음에 볼 것

- **"나가 아버지·동료를(화→토) 살립니다"** — 합성 그룹의 "기운이 도는 길" 문장에서 1인칭 주격이 "나가"로 나왔다.
  `clauseHTML`/`pairClauses`가 여러 대상을 묶을 때 `subjKo`(내가) 대신 `josa`를 쓰는 것으로 보인다(**추정**, 코드 미확인).
  예시 가족 그룹(대상 하나)에서는 "내가 어머니를"로 맞다. A6-5에서 확인해 고친다.

## 4. 남은 것 — A6-5
전체 검증(폰 폭 600 iframe 실측, 접힘 판 44px, 자가진단), 원고 파일과 코드 문장 대조(글자 단위), 현황·로드맵 정리, `2026-09-17-A6-1-원고초안.md` §3 승인 칸을 "반영됨"으로.

## 실기기 재확인 목록
- 폰에서 그룹 패널 "흐름" 아래 역할 줄이 구성원 수만큼 늘어나(가족 4줄) 스크롤이 자연스러운지.
- 폰에서 INSIGHT 상자가 두 문장(G3 + 마무리)이 되어 잘리지 않는지.

## 사용자 결정 필요 — 0건

## 그냥 진행함
- **오행 동률이면 G2·G3·G5를 안 붙였다.** "가장 많은 기운"이 없는데 있는 것처럼 말하면 틀린 문장이 된다. 예시 그룹 셋이 전부
  동률이라 화면에서는 G2·G3가 안 보인다 — 합성 그룹으로 확인했다.
- **관계 보기·가상 성계·미분류에는 안 붙였다.** 유형이 없다(관계 보기는 §5.4가 "유형을 고른 적이 없어 억지로 붙일 이유가 없다"고 결정).
- **G1 문장 둘을 고쳤다** — "곁가지를 쳐이는" → "곁가지가 쳐지는", "파고들리는 자리" → "파고드는 힘을 받는 자리"(우리말이 안 됐다).
  원고 파일도 같이 고쳤다.
- **역할 우선순위는 A6-1 §7에 적은 대로**(누르는 > 눌리는 > 대는 > 모이는 > 가운데).

## 로컬 서버
8934 **내렸다**.

## 배포 직링크
https://shyquisyz.github.io/group-saju/ — 아직 push 안 함(로컬 8커밋 앞섬 예정).
