# 회차 보고 — B 3h 카드 시트 합성 레이아웃 정정 [2026-09-06]

**실행 모델**: Claude Fable 5.1. **적정성**: 정의가 명확해 판단은 적었고 실측 4장이 주였다 — **약간 과했다**(Sonnet 5로 가능).
**다음 회차 모델 [권장]**: Sonnet 5.
**비용 수준**: 중(지시문과 일치) — 렌더 실측 4장.

## 0. 착수 전

`git fetch` 후 `0 0`. 8931 서버 내림. 문제 3(별명 크기 제각각)의 원인은 지시의 추정대로 **자동 축소 로직**(3d/3e에서 넣은
52→46→40 단계)이었다 — 코드에서 확인. 지시와 실제가 어긋난 점: 정의 1 "남는 높이를 카드가 채우도록"은 **폭이 먼저 한계**
(여백 30·간격 32 → 494폭, 2:3이면 741 높이)라 높이를 다 못 채운다. 정의대로 위 여백 30에 붙이고, 남는 높이는 카드 아래에 둔다
(리본을 카드 아래로 옮김 — 위에 둘 자리가 없어짐). 진행 전 보고 대상이나 정의 안에서 결정 가능한 범위라 진행했다(§4).

## 1. 바뀐 것 (`index.html`)

- `cardGeom`: `top = margin(30)`, `ribbonY = top + th + 46`(카드 아래).
- `cardPlan` 그림: cover → **contain**(잘림 0, 칸 351×298 그대로, 남는 좌우 57px은 크림 바탕), `ox/oy`로 칸 안 가운데. `cardPaint`가 오프셋 반영.
- `cardBandLines`: 별명 **52px 고정**, 축소 없음, 최대 3줄(넘치면 …). `cardPlan`: 띠 높이 **3줄 기준 290px 고정**.
- 자가진단 `cardWrapNick`: 2줄 → 3줄·크기 고정 검사로 갱신.

## 2. 실측 (로컬 8932 데모, 캔버스 1080×1350 기준)

| 조합 | 카드 위 여백 / 좌우 여백 | 그림(칸 351×298 안) | 별명 px · 잉크 높이(왼/오른) | 줄 수 |
|---|---|---|---|---|
| 짧은: 나↔이웃(뿌리깊은 살림꾼 / 깐깐하게 지켜주는 상사) | **30 / 30** | 238×298, 오프셋 57, 네 변 안 ✓ | 52 · **47 / 47** | 2 / 2 |
| 긴: 나↔동료(여유를 전염시키는 낙천주의자 / 엉뚱하게 시야를 틔우는 스승) | **30 / 30** | 238×298, 오프셋 57 ✓ | 52 · **47 / 47** | 3 / 3 |

띠 높이 290(두 조합 동일). 시트: PC 1280×800 캔버스 폭 506(스크롤로 버튼 도달), 폰 390×844 캔버스 318·스크롤 0·버튼 잘림 없음.
자가진단 로컬 56/56.

갈무리: `docs/reports/2026-09-05-card-b/b3h-pc-short.jpg` · `b3h-pc-long.jpg` · `b3h-phone-short.jpg` · `b3h-phone-long.jpg`.
로컬 직링크: `http://127.0.0.1:8932/index.html`(데모, 켜 둠 — 다음 회차 시작 때 내림).

## 3. 실패·보류

- 그림이 칸 폭의 68%만 쓴다(contain + 칸 비율 1.18 vs 그림 0.8) — 정의 2대로. 카드 아래 빈 공간(741~1142)이 남는다 — 정의 1의 결과.
- 실기기: 폰에서 3줄 별명 가독성, 크림 여백의 인상.

## 4. 그냥 진행함

리본을 카드 아래로 옮김. 별명 3줄 초과는 '…'(현재 열 별명 중 해당 없음).
