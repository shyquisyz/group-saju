# U14 — 추정한 시각을 사주 표·풀이에 쓴다 [2026-09-19]

- 실행 모델: Opus 5 — 계산 코어(sajuOf) 변경과 경계(자시·진태양시) 판단이라 적정. 다음 회차 Opus 5 [권장].
- [비용 수준: 중 — 계산 코어 한 곳 + 호출부 13곳, 13칸 경계 실측]

## 사용자 결정(이 회차)
- "행성은 두 종류 — 내가 임의로 등록한 자 / 로그인해서 연동한 자. 연동되면 추정이 아니라 연동된 원본 시각으로 판단"
  → 지금(전원 임의 등록자)은 **추정을 계산에 쓰고 「추정」 표시**, 원본 시각이 있으면 언제나 원본
- "가족에게 물어보기 카드는 안 만든다 — 스스로 물어보면 5초"

## 결과
- `2707bf6` 추정 시각을 계산에 — `sajuOf`: 시각 모름 + `birth.est` → `timeMode:'est'`, 추정 칸 가운데를 진태양시 그대로
  (자시 23:30 · 0:30. 벽시계로 넣으면 서울 −32분 보정에 23시가 亥시로 넘어간다)
- 호출부 13곳이 `est`를 넘김 · 사주 표 「시주 · 추정」 + 안내(다시 추정·지우기) · 날짜 줄 "추정 ○시로 봄" 한 번
- 결과 화면: "저장하면 사주 표와 풀이가 이 시각으로 바뀌고 「추정」 표시가 붙습니다" · 저장·지우기 때 행성 지도도 다시
- 실측: 아내 묘시 추정 → 시주 己卯(庚일 卯시), 글자 6 → 8, 지우면 원래대로 · 추정이 저장돼 있던 기존 인물 0명(풀이가 바뀐 사람 없음)
  · 13칸 전부 맞음(辛일 戊子~己亥, 밤 11시대 조자시 庚子, 일주 불변) · 정확한 시각이 이김
- B0 설계 문서 정정: B1c 저장 버전 6 → 7(6은 love가 먼저 씀) + "행성 두 종류" 원칙과 출처 필드(`source`) [권장]
- 자가검사 104/104 (estInCalc)

## 사용자 결정 필요 — 0건

## 그냥 진행함
- 추정 칸의 "가운데"를 대표 시각으로 — 두 시간 칸 어디든 그 칸의 시주는 같다(시주는 칸 단위). 칸 경계 근처의 월주 변화(절입)는 안 본다
- 원본 시각이 없는 연동자를 위한 `noEst` 스위치를 계산에 미리 뒀다(쓰는 곳은 아직 없음)

## 실기기 재확인 목록
- 시각 추정 → 「이 시각으로 보기」 → 사주 표 시주 칸 「추정」·날짜 줄 표시가 보이는지
- 「추정 지우기」로 되돌아가는지

## 로컬 서버
8900 내림.
