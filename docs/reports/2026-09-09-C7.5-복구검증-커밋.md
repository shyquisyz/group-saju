# C7.5 — 삭제분 복구 검증 후 안전점 커밋 [2026-09-09]

목적: C7에서 `wireHapChung`을 잘못 지웠다가 되돌린 것이 **제대로 복구됐는지 증명**하고,
그 상태를 커밋해 되돌릴 지점을 만든다. 로컬 커밋 1회, **push 없음.**

문자열 검색은 전부 `grep -a`로 했다.

---

## 1단계 — 마지막 커밋(c1e8031)과 비교

| 항목 | 값 |
|---|---|
| 바뀐 파일 | `index.html` 1개 |
| 추가 줄 | 78 |
| 삭제 줄 | **89** |
| 줄 수 | c1e8031 **12,025** → 커밋 3836841 **12,016** |

### 삭제 89줄의 내역 — 설명 안 되는 삭제 0줄

| 구간 | 줄 수 | 무엇 | 판정 |
|---|---|---|---|
| 1~28 | 28 | 합·충 딱지 CSS 주석 + `.hcchip` 5규칙 + `.hcwhy` 2규칙 | 지시한 셋 중 하나 |
| 29~37 | 9 | `hcSeq` · `hcChipHtml()` + 그 주석 둘 | 지시한 셋 중 하나 |
| 38~47 | 10 | 옛 `REL_DESC` 10줄 (C2에서 `{b,c}` 구조로 교체) | C2~C6 작업분 |
| 48~65 | 18 | `REL_STRENGTH` 주석 + 상수 (C2에서 지시대로 삭제) | C2~C6 작업분 |
| 66~76 | 11 | `REL_TAKE` 10줄 (C2에서 지시대로 삭제) | C2~C6 작업분 |
| 77~89 | 13 | 관계 패널 옛 `blk()` 렌더 코드 (C4에서 딱지 제거) | C2~C6 작업분 |

= **딱지 관련 37줄 + C2~C6 문구 교체분 52줄.** 셋 밖의 삭제는 없다 → **분기표 A 해당 없음.**

※ 지시문 1-3은 "삭제된 줄이 셋에만 해당하는지"를 묻지만, 같은 지시문 1-2가
"문구 교체로 바뀐 줄(C2~C6 작업분)은 정상"이라고 못 박아 두었으므로 그 52줄은 분기표 A의
"셋 외에 사라진 줄"로 세지 않았다. 그 52줄은 전부 C2·C4 지시문이 명시적으로 삭제를 지시한
것들이라 되살리면 오히려 승인된 작업이 뒤집히고 현재 코드가 깨진다(`blk()`가 `REL_TAKE`를
더 이상 참조하지 않는다).

---

## 2단계 — 개별 실측 (전부 `grep -a`)

| # | 대상 | 결과 | 행 번호 | 분기 |
|---|---|---|---|---|
| 1 | `wireHapChung` 정의 | **있음** | 2762 | B 해당 없음 |
| 1 | `wireHapChung` 호출 | **2곳 다 있음** | 9941 (인물 패널 `showNode`) · 10022 (관계 패널) | B 해당 없음 |
| 2 | `hcSeq` | 코드 참조 **0곳** (239·2754는 주석) | — | C 해당 없음 |
| 3 | `hcChipHtml` | 정의 **0** · 호출 **0** (239·2754·10042~10044는 주석) | — | — |
| 3 | `.hcchip` CSS 규칙 | **0** | — | — |
| 3 | `.hcwhy` CSS 규칙 | **0** | — | — |

`.hcchip` 문자열은 2763행에 **하나 남아 있다** — 남겨 둔 `wireHapChung` 안의
`querySelectorAll('.hcchip')` 셀렉터다. 함수를 살려 뒀으니 당연히 함께 남는다.
CSS 규칙이 아니므로 "규칙 0곳"과 어긋나지 않는다.

### 화면·콘솔 (`?v=c75b`로 캐시를 깨고 새로 엶)

| 항목 | 값 |
|---|---|
| 콘솔 오류 | **0건** (남은 로그는 `[nebula] 384x192 생성 118ms` 하나) |
| 자가진단 | **58/58 통과**, 실패 0 |
| 여덟 코너 | **8/8 표시**, 못 뜬 코너 0 |
| 해설/해석 층 | `.cnrfact` 4 · `.nicktake` 4 |
| 관계 패널 | `.nickblk` 2 · `.nickdesc` 2 · `.hcchip` 요소 **0** |
| 전역 | `wireHapChung` function · `hapChungOf` function · `hcChipHtml` undefined · `hcSeq` undefined |

**분기표 D 해당 없음** → 3단계 진행.

---

## 3단계 — 안전점 커밋

| 항목 | 값 |
|---|---|
| 커밋 해시 | **`3836841`** |
| 브랜치 | `main` (그대로) |
| 파일 | 20개 — `index.html` + 보고서 10 + 카드 갈무리 9 |
| 변화 | 1,526 insertions / 89 deletions |
| push | **안 함.** `git fetch` 후 `origin/main...main` = `0 2` (origin/main = `ecbb17d`, 로컬이 2 앞섬) |

앞선 커밋 `c1e8031`도 아직 안 올라가 있어 로컬이 2개 앞선 상태다.

**커밋에 넣지 않은 것** — `CLAUDE.md`. 4단계에서 고치는 파일이고, 커밋 메시지가 가리키는
C2~C7 작업분이 아니라 프로젝트 규칙 변경이라 뺐다. 지금도 미커밋 상태다.

---

## 4단계 — 재발 방지 한 줄

`CLAUDE.md` 「상비 규칙 (0) 착수 전 점검」 끝에 추가했다(기존 줄은 안 건드림):

> **`index.html`은 큰 단일 파일이라 grep이 이진으로 오판한다. 문자열 검색은 항상 `grep -a`를 쓸 것.**
> [2026-09-08 C7] `grep -n`이 `Binary file index.html matches`를 내며 목록을 도중에 끊는 바람에
> `wireHapChung` 호출을 "0곳"으로 잘못 세고 함수를 지웠다. 실제로는 호출이 두 곳이었고
> 패널이 `ReferenceError`로 안 그려졌다.

---

## 덤으로 고친 것 · 앞 보고서 정정

1. **낡은 주석 정정** — `index.html` 10042행 부근 C4 주석이 "hcChipHtml()은 그대로 남긴다"라고
   적혀 있었는데 C7에서 그 함수를 지웠다. 취소선 + `[2026-09-08 C7 정정]` 줄을 달았다
   (코드 변경 아님, 주석만).
2. **C7 보고서의 줄 수가 틀렸다.** "12,038 → 12,010"이라고 적었는데, `wireHapChung`을
   되돌린 뒤 다시 재지 않고 쓴 값이다. 실측하면 —
   c1e8031 **12,025** / C7 착수 시 작업본 **12,038**(C2~C6 미커밋분 포함) /
   딱지 삭제 직후 **12,001** / 복구·주석 정정 후 커밋 시점 **12,016**.
   C7 보고서 파일은 이미 커밋됐으므로 고치지 않고 여기에 정정만 남긴다.

---

## 사용자 결정 필요

1. **`CLAUDE.md`를 언제 커밋할 것인가.** 지금 미커밋이다(C6.5의 「회차 마무리」 절 +
   이번 `grep -a` 한 줄). [권장] 다음 회차 첫 커밋에 함께 넣는다.
2. **push 시점.** 로컬이 `origin/main`보다 2개 앞서 있다. [권장] C8(문구 치환)까지 끝내고
   한 번에 올린다.

## 그냥 진행함

- 삭제 89줄 중 C2~C6 작업분 52줄은 분기표 A를 적용하지 않았다. 근거는 위 1단계 각주.
- 10042행 낡은 주석 정정은 지시에 없지만 넣었다. CLAUDE.md 「문서 정합」이 요구하는 것이고,
  주석만 바꾸므로 커밋 전 검증 결과에 영향이 없다.
- `git fetch`를 한 번 돌렸다(읽기 전용). 안 하면 "push 안 했다"를 낡은 참조로 말하게 된다.

## 실기기 재확인 목록

없음 — 화면에 보이는 것은 바뀌지 않았다(주석·규칙 문서·커밋뿐).

## 로컬 직링크
http://127.0.0.1:8934/index.html
