# B19 — 카드 위 설명 두 줄로 나누기 [2026-09-09]

커밋 없음. 카드 그림·명패·서명·테두리의 **내용**은 안 건드렸다(자리만 아래로 밀렸다).

| 시점 | 바이트 | 줄 | md5 |
|---|---|---|---|
| 회차 시작 | 948,120 | 12,220 | `5943eecbce033443876917c5fed4e0ba` |
| **회차 끝** | **949,194** | **12,232** | **`732d06d609a0cd6eb6a24f58155a6ce4`** |

## 착수 전 확인 — 지시문과 실제가 **전부 일치**

948,120 / 12,220 / `5943eecb…` ✅ · 저장 캔버스 1080×1203 · 카드 494×905가 y=108 · `descGap` 10 ✅

---

## 지시문 숫자와 달랐던 것 하나 — **52px에서 한 역할이 넘친다**

지시문은 "가장 긴 것이 466px일 것으로 보나"라고 했다. 실측은 다르다.

| 십신 | 역할 | `<역할>` 52px 폭 |
|---|---|---|
| 비견 | 나란히 걷는 동무 | 407 |
| 겁재 | 같은 걸 노리는 맞수 | 463 |
| 식신 | 느긋한 낙천주의자 | 439 |
| **상관** | **판을 뒤집는 재간둥이** | **496 ← 494를 2px 넘침** |
| 편재 | 판을 벌이는 사람 | 407 |
| 정재 | 살림을 챙기는 사람 | 451 |
| 편관 | 담금질하는 어른 | 395 |
| 정관 | 선을 지키는 어른 | 407 |
| 편인 | 엉뚱한 스승 | 305 |
| 정인 | 품어 주는 어른 | 362 |

분기표대로 **열 개가 전부 들어가는 가장 큰 크기**를 찾아 그 값으로 통일했다 — **50px**.
(50px에서 최장이 477px로 494 안에 든다.)

숫자를 박지 않았다 — `cardRolePx()`가 `fs.nick`(52)에서 2px씩 내리며 열 개가 다 들어가는 값을 **한 번 찾아 캐시**한다.
역할 낱말을 바꾸면 알아서 다시 찾는다.

---

## 할 일 1 — 두 줄로

`cardTopLine()`을 두 줄 판으로 바꿨다.

| | 내용 | 크기 | 정렬 |
|---|---|---|---|
| 윗줄 | `cardDirLabel()`이 만드는 **명패 윗줄과 같은 문자열** (`내가 보는 이웃`) | 24px (라벨 크기) | 카드 폭 494 가운데 |
| 아랫줄 | `<역할>` (`<엉뚱한 스승>`) | **50px** (모든 카드 공통) | 〃 |

- 꺾쇠 `<` `>`는 아랫줄에 그대로.
- 두 줄 사이 **4px** (`CARD_B.descLead`).
- 줄 자리는 명패와 같은 방식 — 줄 위쪽에서 `round(px×0.95)` 내려온 곳이 기준선.
  실측 기준선: 윗줄 **y=53**, 아랫줄 **y=113**.

## 할 일 2 — 크기 고정

**18장 전부 50px 하나**로 나왔다(분포 `{50: 36}` — 두 장씩 18쌍). B18의 36~52px 들쭉날쭉이 없어졌다.

## 할 일 3 — 위 여백

`descH`를 손으로 안 박고 계산한다 — `labelH + descLead + roleH + descGap`.

| 조각 | 값 |
|---|---|
| `labelH` = round(24 × 1.3) | 31 |
| `descLead` | 4 |
| `roleH` = round(50 × 1.3) | 65 |
| `descGap` | 10 |
| **`descH`** | **110** |

| 항목 | 전(B18) | 후 |
|---|---|---|
| 카드 시작 y | 108 | **140** |
| 저장 캔버스 | 1080×1203 | **1080×1235** |
| `footTop` | 1043 | **1075** |
| `footerH` | 130 | **130** (그대로) |
| 카드 | 494×905 | **494×905** (그대로) |
| 그림 칸 | 327×409 | **327×409** (그대로, 4:5) |

서명 블록 1075~1205, +아래 여백 30 = **1235** — 캔버스 안에 온전하다.

## 할 일 4 — 넘칠 때

- **윗줄**: 그대로 → 이름만 → 윗줄 삭제. 실제 카드 18장에서 **1단계(그대로)로 전부 들어갔다** — 접힘 0건.
- **아랫줄**: `cardRolePx()`가 열 개를 다 담는 50px을 고르므로 **넘치는 카드가 없다.**
  따라서 "그 카드만 2px씩 줄이되 같은 쌍의 다른 장도 함께" 규칙이 **발동한 카드 0장**이다.

---

## 자가진단이 한 번 깨졌다 — 고쳤다

치환 직후 **57/58**, 실패는 `cardWrapNick: 별명 최대 3줄·크기 고정`.

원인: 그 시험이 `weight===700 && align==='center'`인 글자 op을 전부 세는데,
새로 생긴 카드 **위** `<역할>` 줄도 같은 조건이라 함께 잡혔다(그리고 50px ≠ `fs.nick` 52라 크기 검사도 틀어졌다).
시험이 보려는 것은 **명패 안 별명**이므로 조건에 `o.y >= lp.geom.top`(카드 윗변 아래)를 더했다.
고친 뒤 **58/58**, 별명 줄 수 6으로 정상.

---

## 마무리 검증

| 항목 | 값 |
|---|---|
| 자가진단 | **58/58 통과**, 실패 0 ✅ |
| 카드 18장 대조 | 윗줄이 명패 윗줄과 **글자 그대로 같음**, 아랫줄 역할이 그 장의 십신과 일치 — **어긋남 0** ✅ |
| 18장 역할 크기 | **전부 50px** (분포 `{50: 36}`) ✅ |
| 저장 캔버스 | **1080×1235**, 카드 494×905가 **y=140**에서 시작 ✅ |
| 서명 블록 | 1075 ~ 1205, 캔버스 1235 안에 온전(잘림 없음) ✅ |
| 폭 600 / 창 900 | 시트 821 / 내용 819 / **넘침 0** (상한 828 — **여유 7px**) ✅ |
| 폭 1024 / 창 900 | 같은 값, **넘침 0** ✅ |
| **그림 칸 (작업 전 → 후)** | 화면 **148×185 → 148×185** — 안 눌렸다 ✅ (캔버스 327×409 그대로) |
| 로컬 서버 | **HTTP 200** / 949,194 bytes ✅ |

미리보기 캔버스는 488×543 → 488×558로 세로만 길어졌다.

## 결과 그림

- `D:\work\web service\사주은하\sajugalaxy.com\docs\reports\img\2026-09-09-B19\적용후-저장캔버스.png` (1080×1235)
- `D:\work\web service\사주은하\sajugalaxy.com\docs\reports\img\2026-09-09-B19\적용후-화면600.png` (523×768 — 뒤 배경에 실제 이름이 있어 카드 창만 잘랐다)

## 남은 것 **[의견]**

- **카드 창에 여유가 7px밖에 없다**(내용 819 / 상한 828). 여기서 캔버스가 더 길어지면 스크롤이 생긴다 —
  다음에 무엇을 더 얹을 때는 상한(92vh·840px)부터 봐야 한다.
- 저장 그림이 또 길어졌다(1203 → 1235, B17 전 1125에서 **9.8%**). 카톡 미리보기 확인은 **실기기 몫**.
- 안 쓰는 채로 남은 것 셋 — `cardRoleLine`(B17) · `CARD_PIC_DESC`·`fillCardPicDesc`(B14).

## 로컬 직링크
http://127.0.0.1:8934/index.html
