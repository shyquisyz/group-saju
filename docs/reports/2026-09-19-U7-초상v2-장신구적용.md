# 2026-09-19 U7 — 초상 v2(55장 지도) + 살아 있는 초상·장신구 앱 적용

- 실행 모델: Opus 5 — 렌더 파이프라인 차이 진단·앱 통합·자가진단 원인 추적이 있어 적정. 다음 회차 Opus 5 [권장].
- [비용 수준: 상 — 렌더링 방식 추가(초상 2종), 브라우저 실측 여러 번, 두 번 배포]
- 사용자: "문제점 해결하고 … 크리티컬한거 있거나 모델 변경해야하는 경우 아니면 멈추지 않게 진행해"

## 1. 문제 해결 — 은하 조망의 작은 행성
U6 실기기 확인 항목("작은 행성 4px대"). 은하 조망에서만 위계 크기 차이를 절반으로(`PLANET_RANK_GALAXY_KEEP 0.5`, 0.8 → 0.9배).
성계·포커스 장면에서는 위계 그대로.

## 2. 초상 v2 — 오행 조합 지도 55장 (커밋 d1d1617, 배포)
- 초상(홈·인물 패널)을 일간 모양 20장 → **여덟 글자 오행 분포로 고른 지도 55장**으로. U4·U5 확정 규칙:
  주 = 최다(동점: 일간 → 월지 → 목화토금수), 주 5글자 이상이면 압도 지도, 아니면 주 + 보조 둘.
- **사람마다 다른 면**: `facing.json`에 통과 방향 목록(`pass`)을 새로 넣었다. `tools/planet-maps/passlist.html`로 실측
  (96px, 좌우 36 × 기울기 5, 기준 = 주 오행이 가장 넓고 보조 모두 3% 이상). 지도별 1~180개, 중간값 79.
  적은 것: 토+금·수 1 · 토+화·수 2 · 토+화·금 3 · 토+목·금 6 · 수+토·금 7. 생년월일·시각 해시로 하나 고른다.
- 금 영역만 은은한 금속 광택(0.55, 밝은 회색 스튜디오 반사) · 바다만 매끈한 반사 · 수 압도 극지방 얼음 절반 ·
  계절막(양력 12·1·2월 겨울 / 7·8월 여름, 20%, 10시·5시) · 아주 약한 개인 색감(±3% 따뜻함, ±4% 밝기).
- 비동기: 처음엔 구 초상을 보이고 다 그려지면 교체. 실데이터 10장 1.7초, 10장 모두 다른 조합·방향.
- **실측으로 잡은 것**: 앱 렌더러는 출력 색 변환을 안 써서 지도를 sRGB로 표시하면 어둡고 붉게 가라앉았다 → 그대로 사용.
  금 영역이 어두운 우주를 비춰 검게 보였다 → 스튜디오 환경맵.

## 3. 살아 있는 초상 + 나와의 관계 장신구
- 인물 패널을 열면 정지 초상 자리에 3D 캔버스를 겹쳐 **계속 움직인다**(구름 흐름·번개·태풍 회전·오로라 일렁임·고리 반짝임·별똥별 공전).
- 장신구 = 나와의 합충(U5 안 D, 확정). 실데이터 9명 중 1명(토성 고리) — 안 D의 약 30%와 같은 수준.
- 모양은 U5 확정 시안 코드를 그대로 옮겼다(시안 파일에서 함수를 잘라 붙임, 색 파이프라인도 시안과 같게 sRGB 출력).
- 캔버스 = 정지 초상의 1.8배(`HERO_SPILL`), 행성 크기는 정지 초상과 같다. 고리 등이 동그란 테두리 밖으로 나온다.
  넓은 장신구(고리·무지개·별똥별·오로라)가 있으면 패널 왼쪽 끝에 안 잘리게 초상을 그만큼 민다(실측 33px, 캔버스 왼쪽 = 패널 왼쪽 0px).
- 렌더러는 이 초상 전용 1개(WebGL 문맥 +1). 패널이 닫히거나 다른 사람을 열면 멈추고 정리한다. 탭이 숨으면 그리지 않는다.
- 확인: 장신구 10종(먹구름·번개구름·태풍·흰 구름·오로라·무지개 고리·토성 고리·별똥별 3·무지개+별똥별 2·없음)을 검사용 인자로 띄워 모두 그려짐.
  폰 크기(375×812)에서 고리 안 잘림.

## 4. 자가진단
- `await sajuSelfTest()` 97/97 (새로 연 상태 두 번).
- **nodeLabelZoom은 공전 위치(시간)에 따라 흔들린다** — 변경 전 사본도 25초 간격 8번 중 1번 실패(현재 빌드 8번 중 2번).
  기존 검사의 약점이라 별도 작업으로 분리했다.

## 사용자 결정 필요 — 0건

## 그냥 진행함
- 홈 목록의 작은 초상은 정지 그림(장신구 없음), 인물 패널 머리만 살아 있는 초상 — 목록까지 움직이면 WebGL 문맥·연산이 사람 수만큼 는다.
- '나'도 55장 지도 초상(U5 ⑥ 홈 화면 시안이 "내 행성"을 행성으로 그렸다). 은하 속 '나'는 그대로 항성.
- 살아 있는 초상의 행성은 고정(보이는 면 유지), 장신구만 움직인다 — 자전시키면 사람마다 고른 "주 오행이 보이는 면"이 돌아가 버린다.
- 넓은 장신구가 있을 때 초상을 33px 오른쪽으로 민다(글자 칸도 같이 밀림).

## 실기기 재확인 목록
- 폰에서 인물 패널을 열었을 때 살아 있는 초상이 버벅이지 않는지(특히 구름 18겹·태풍)
- 처음 여는 구름 인물에서 멈칫함(구름 높이 계산 1회)이 느껴지는지
- 초상 색감이 시안과 같게 느껴지는지(정지 초상 = 앱 파이프라인, 살아 있는 초상 = 시안 파이프라인)

## 로컬 서버
8900·8936 모두 내렸다.
