# 회차 보고 — B 4a 카드 시트 그림 칸 세로형 2:3 [2026-09-06] — **보류(착수 전 점검에서 멈춤)**

**실행 모델**: Claude Fable 5.1. **적정성**: 실측·판단만 하고 코드는 안 고쳤다 — 이 회차엔 **과했다**. **다음 [권장]**: 결정 후 구현 회차는 Sonnet 5.
**비용 수준**: 하(실측만. 지시문의 "중"보다 적게 씀).

## 0. 착수 전 점검 (지시 1단계)

`git fetch` 후 `0 0`. 8933 서버 내림(PID 42228). 값이 정해지는 곳은 전부 `index.html`의 `cardGeom`/`cardPlan`(2318~2392행)이다.

| 항목 | 지시가 적은 현재값 | 코드 실측 | 일치 |
|---|---|---|---|
| 캔버스 | 1080×961 / 1053, 여백 30 | `plan.h` = 30+741+30[+62+30]+130+30 | ○ |
| 그림 칸 | 351×298 | `inner.w`=351, `picH`=`inner.h`(588)−`bandH`(290)=298 | ○ |
| 띠 | 290 | `band3`=24+31+6+63×3+6+34=290(글 3줄 기준 고정) | ○ |
| 별명 | 52px 고정 | `fs.nick`=52 | ○ |

**어긋난 점 2건 — 진행하지 않았다.**

1. **"폭 352 유지"인데 현재 폭은 351이다.** 그림 칸 폭은 테두리 소재의 빈 영역 비율(1091/1536)×카드 폭 494 = 350.87 → 351로 **계산되는 값**이지 상수가 아니다. 352로 하려면 테두리 빈 영역 비율을 손대거나 1px을 따로 더해야 한다(어느 쪽이든 정의 밖).
2. **띠와 그림 칸은 "관계"가 아니라 뺄셈이다.** 띠 290은 글자 크기에서 정해지고(별명 52px×3줄), 그림 칸은 **테두리 빈 영역 588에서 띠를 뺀 나머지**다. 그래서 그림 칸을 528로 키우면 띠는 그대로 290이고 **카드 한 장 높이가 741 → 971**이 된다(위 테두리 79 + 528 + 290 + 아래 테두리 74). 이건
   - 3e에서 확정한 **카드 비율 2:3(`CARD_RATIO`)이 깨진다**(494×971 ≈ 1:1.97), 그리고
   - 테두리 소재(9:16)를 폭 494에 맞추면 자연 높이가 **885**라 카드보다 **86px 짧다.** 지금 코드는 소재가 카드보다 **길 때 가운데를 잘라내는** 길(`cutSrc`)만 있고 **늘리는 길은 없다** — 그대로 두면 `drawImage(im,x,y,w,h)`로 소재가 세로로 9.7% 늘어난 채 그려진다.

## 1. 바뀐 파일

- `docs/reports/2026-09-06-B4a-그림칸세로.md`(이 파일)뿐. **`index.html` 무변경.**

## 2. 결과

- 그림 칸 실측 351×298(현재 그대로) / 띠 290 / 캔버스 1080×961(A↔B, 딱지 없음)·1080×1053(딱지 있음) — 코드에서 읽은 값, 렌더는 안 함.
- 정의대로 갔을 때의 예상(계산값, 렌더 아님): 그림 칸 351(또는 352)×528, 띠 290, 카드 494×971, 캔버스 1080×**1191** / **1283**.

## 3. 실패·보류

- **보류.** 위 2건 결정 전엔 구현하지 않는다. 로컬 서버는 보여 줄 렌더가 없어 **안 띄웠다**(직링크 없음).

## 사용자 결정 필요 (2건)

1. **카드 비율** — (a) `[권장]` 카드 2:3을 풀고 그림 칸 2:3을 우선한다 → 카드 494×971, 테두리는 가운데 띠를 **늘려** 붙이는 코드를 추가(자르는 `cutSrc`의 거울상. 빈 영역 테두리는 얇은 띠라 세로로 늘려도 눈에 안 띌 것으로 봄 — 근거 없음, 렌더로 확인 필요). (b) 카드 2:3을 지키고 그림 칸을 세로형으로 하려면 띠를 줄여야 하는데(588−528=60), 별명 52px 3줄(290)과 양립 불가 → 정의 충돌이라 권장하지 않음.
2. **그림 칸 폭** — (a) `[권장]` 계산값 351 그대로 두고 높이만 528(2:3 오차 0.3%). (b) 352로 맞추려면 테두리 빈 영역 비율에 1px 보정을 넣는다.

## 그냥 진행함

- 8933 서버 내림(사전 정리 지시). 그 밖에 코드는 손대지 않았다.

## 실기기 재확인 목록

없음(바꾼 것이 없다).
