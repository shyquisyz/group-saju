# A6-5 · A6 마무리 — 원고↔코드 대조 · 폰 폭 실측 · 조사 오류 1건 수정 [2026-09-17]

**실행 모델**: Fable 5.1. **적정성**: 과했다(검증·정리 회차). **다음 회차 A5(시각 추정 화면) [권장]**: Sonnet 5 —
설계(`docs/시간추정-설계.md`)가 확정돼 있고 결정 2건도 끝났다.
**비용 수준**: 하 — `index.html` 1곳(조사), 파이썬 대조 1회, 브라우저 폰 폭 실측 1회.

---

## 세 줄 요약

1. **A6(해석 원고 확장)이 끝났다.** 원고 초안 문장 칸 **279개**와 코드 문자열 **279개를 글자 단위로 대조 — 차이 0건.**
   해석 원고는 99문장/5,575자 → **약 449문장/13,270자**(2.4배).
2. **폰 폭(375×812 에뮬레이션)에서 인물·그룹 패널 모두 가로 넘침 0블록, 접힘 판 44px 미만 0개.** 헤드리스 크롬으로
   390px 창을 찍은 갈무리는 뷰포트 에뮬레이션이 안 돼 오른쪽이 잘려 보였다 — **측정 조건 오류**라 버렸다
   (CLAUDE.md "측정값이 예상과 어긋나면 측정 조건부터").
3. A6-4 관찰 1건("나가 아버지·동료를")을 고쳤다 — `pairClauses`의 여러 대상 조각이 `josa` 대신 `subjKo`를 쓰도록.
   자가진단 **61/61**.

---

## 0. 착수 실측
```
git rev-list --left-right --count origin/main...main → 0 8
```

## 1. 바뀐 곳
| 위치 | 무엇 |
|---|---|
| `pairClauses` `one` 조각(7783 부근) | ~~`josa(s.giver.name,'이가')`~~ → `subjKo(s.giver.name)`. 대상이 하나인 조각은 `pairPhrase`가 이미 `subjKo`를 써서 맞았고, 여러 대상 조각만 빠져 있었다 |
| `docs/reports/2026-09-17-A6-1-원고초안.md` §3 머리 | "반영 완료 · 코드가 정본" 안내 추가 |

## 2. 실측

### 2-1. 원고 ↔ 코드 글자 단위 대조 (파이썬)
```
원고 문장 칸(§3 표, A표 제외, {P}/{Q}→{A}/{B} 치환) 279
코드 문자열(GANG_PIECE·PAIR_STORY·HC_POS_LINE·POS_YEAR/DAY/HOUR·UNSEONG_LINE·TIE_STEM 음간·
  MONEY_LINE_STRONG·JOB_TAIL·STORY·GROLE_LINE·GPAN_LINE·GTYPE_EL·GTYPE_MISS·GTOWARD_TYPE) 279
원고에만 있음 0 · 코드에만 있음 0
```
288칸 중 279가 문장 칸이다 — A표 2칸(코드 변경)과 F-가·F-나에서 기존 문장을 그대로 쓴 칸은 원고 표에 없다.

### 2-2. 조사 수정 확인 (합성 그룹 나·아버지·동료)
```
치환 전: 기운이 도는 길 — 나가 아버지·동료를(화→토) 살립니다.
치환 후: 기운이 도는 길 — 내가 아버지·동료를(화→토) 살립니다.   (본문 전체에 "나가 " 0건)
```

### 2-3. 폰 폭 실측 (브라우저 창 375×812 에뮬레이션, 예시 데이터)
| 패널 | innerWidth | 패널 폭 | 본문 scrollWidth / clientWidth | 가로 넘치는 블록 |
|---|---|---|---|---|
| 인물(어머니) | 375 | 375 | 375 / 375 | **0** |
| 그룹(가족) | 375 | — | 375 / 375 | **0** |
접힘 제목 버튼(`.sjhelp-b`) 높이 44px 미만: **0개**. 화면 갈무리(0.6배)로 그룹 패널 첫 화면이 정상 접힘·정상 줄바꿈인 것을 확인.

### 2-4. 인물 패널 크기 (데스크톱, 어머니) — A6 전후
| | A6 전 | A6 후 |
|---|---|---|
| 첫 화면 펼친 본문(공백 제외) | 359자 | **780자** |
| 패널 전체 글자 | 1,587 | 1,992 |
| 패널 높이 | 4,409px | 5,058px |

### 2-5. 자가진단
```
sajuSelfTest() → 61/61 (?v=a65-1, 데스크톱) · node --check OK
```

## 3. A6 전체 요약 (A6-1 ~ A6-5)
| 회차 | 넣은 것 | 문장 |
|---|---|---|
| A6-2 | 첫 화면 펼침 · 십신×세기 · 둘이 붙으면 · 세기 사연 | 64 |
| A6-3 | 자리마다 놓인 기운(+12운성) · 코너 3개 분기 · 합충 년·월·시 | 79 |
| A6-4 | 그룹 역할·판 성격·유형×오행·유형×결여·나에게 | 145 |
| 합계 | | **288칸 / 350문장** (기존 99문장 유지) |

계산이 바뀐 곳은 **없다**(`hapChungPos`는 기존 `hapChungOf`를 다른 기둥에 돌린 것). 옛 문장·옛 결정은 전부 취소선 주석으로 남겼다.

## 실기기 재확인 목록
- 폰에서 인물 패널 첫 화면(780자)이 길게 느껴지지 않는지 — 에뮬레이션은 통과, 체감은 사람이 봐야 한다.
- 폰에서 그룹 패널 "흐름" 아래 역할 줄(구성원 수만큼)과 INSIGHT 두 문장.

## 사용자 결정 필요 — 1건
- **push.** 로컬이 `origin/main`보다 9커밋 앞선다(R2·파일 정리·A6-1·BM·A6-1 정정·A6-2·A6-3·A6-4·A6-5). 배포 주소가
  바뀌는 것은 `index.html`이 든 A6-2~A6-5 넉 개다. 뿌리기 전이라 `main` 직접 배포가 허용된다(CLAUDE.md 13).
  [권장] 지금 push — 실기기 확인은 배포 주소에서 해야 한다.

## 그냥 진행함
- **헤드리스 390px 갈무리 2장을 버렸다.** 뷰포트 에뮬레이션 없는 창 크기 지정이라 텍스트가 오른쪽으로 잘려 보였는데,
  브라우저 에뮬레이션 실측(2-3)에서는 넘침 0이었다. 틀린 측정을 증거로 남기지 않는다.
- **원고 파일에 "코드가 정본" 안내를 달았다.** 앞으로 문장을 고칠 때 두 곳을 같이 고치는 부담을 없애기 위해서다.

## 로컬 서버
8934 **내렸다**.

## 배포 직링크
https://shyquisyz.github.io/group-saju/
