# G1 — 밀린 작업분 커밋 (2주치 저장) [2026-09-09]

이 회차만 "커밋 금지"를 해제했다. **로컬 커밋만 했고 push는 하지 않았다.**

## 먼저 — 지시와 실제가 다른 것 하나

지시문은 마지막 커밋이 `c1e8031`인지 확인하라고 했으나, **실제 HEAD는 `3836841`**이었다.
`c1e8031`은 그 바로 앞 커밋이다(C7.5 회차에서 안전점 커밋 `3836841`을 이미 만들어 뒀다).
실제 값을 기준으로 진행했다.

---

## 1단계 — 착수 전 실측

| 항목 | 값 |
|---|---|
| 브랜치 | `main` |
| 마지막 커밋 | **`3836841`** — "C2~C7: 관계 문구 조각 구조·b·c줄 개정, 여덟 코너 진단, 미사용 코드 정리" |
| 원격 | `origin` = `https://github.com/shyquisyz/group-saju.git` (등록돼 있음 → 분기표대로 **무시, push 안 함**) |

### 변경된 파일 (추적 중) — 4개 · +203 / −67

| 파일 | 추가 | 삭제 |
|---|---|---|
| `index.html` | 128 | 57 |
| `docs/현황.md` | 72 | 1 |
| `CLAUDE.md` | 10 | 0 |
| `docs/reports/2026-09-08-C7-여덟코너-진단.md` | 1 | 1 |

### 추적되지 않은 파일 — 8개

| 파일 | 크기 |
|---|---|
| `docs/reports/2026-09-09-B12-명패윗줄.md` | 10,259 B |
| `docs/reports/2026-09-09-B13-그림설명-조사.md` | 17,783 B |
| `docs/reports/2026-09-09-C7.5-복구검증-커밋.md` | 6,308 B |
| `docs/reports/2026-09-09-C8-여덟코너-치환.md` | 10,911 B |
| `docs/reports/2026-09-09-C9-돈스타일-용어정리안.md` | 13,203 B |
| `docs/reports/2026-09-09-C10-치환.md` | 10,052 B |
| `docs/reports/2026-09-09-C11-정리.md` | 10,158 B |
| `docs/reports/2026-09-05-card-b/b12-명패윗줄-5색.jpg` | 56,418 B |

### `.gitignore` 요지

개인정보 차단(`my-data.js` · `*.local.js` · `*-backup-*.json`), 작업용 이미지 전면 차단
(`*.png` `*.jpg` `*.jpeg` `*.psd` …)에 배포 자산만 예외로 여는 구조
(`!assets/` · `!assets/card-b/*.png` · `!docs/reports/**/*.jpg` 등), OS·편집기 찌꺼기,
`*.log` · `*.tmp` · `.cache/` · `node_modules/`.
**이번에 한 줄도 고치지 않았다** — 뺄 파일이 하나도 없었기 때문이다(2단계).

---

## 2단계 — 커밋하면 안 되는 것 걸러내기

| 검사 | 결과 |
|---|---|
| 개별 20MB 초과 파일 | **0건** (`find -size +20M`, 저장소 전체) |
| `node_modules` · `.env` · `*.log` · `*.tmp` · `*.bak` · `~$*` | **0건** |
| `api_key` `apikey` `secret` `password` `token` `private_key` (커밋 대상 전 파일) | **0건** — `grep -ain` 결과가 아예 없어 사람이 판단할 줄 자체가 없었다 |
| 개인정보 파일 | `my-data.js`·`group-saju-backup-*.json`은 `.gitignore`가 막고 있어 스테이징에 안 올라옴(`git status`로 눈으로 확인) |
| 갈무리 이미지의 개인정보 | `b12-명패윗줄-5색.jpg`는 **합성 인물**로 뽑아 실제 이름·생년월일·일주가 없다(B⑫ 보고서 참고) |

**뺀 파일 0건.** 커밋 대상 전부를 그대로 담았다.

---

## 3단계 — 커밋 나누기

| 지시상 커밋 | 대상 | 결과 |
|---|---|---|
| 커밋1 `index.html` | 1개 | **`ff6f34a`** |
| 커밋2 `assets/` 이미지·소재 | **0개** | **건너뜀** — `assets/` 아래 변경이 없다. B13에서 확인한 `web/`·`web2/`는 삭제하지 않았고(그 회차는 조사 전용), `web2/`는 `.gitignore`가 막아 애초에 추적 대상이 아니다 |
| 커밋3 `docs/` 보고서 + `CLAUDE.md` | 10개 | **`3f970b6`** |
| 커밋4 나머지 | 2개 | **아래 참고** |

전체 대상이 12개라 "3개 이하면 합친다" 분기는 해당 없음. 삭제·이름 변경 파일도 없었다.

### 커밋4를 마지막에 둔 이유 (판단해서 정한 것)

5단계가 **`docs/현황.md`에 이번 커밋 해시를 적으라**고 한다. 해시는 커밋을 해야 나오므로
`docs/현황.md`를 커밋3에 넣으면 자기 해시를 적을 수 없다. 그래서 분기표의
"어느 커밋에 넣을지 애매하다 → 커밋4" 를 따라 `docs/현황.md`와 이 보고서를 마지막 커밋으로 뺐다.

---

## 4단계 — 커밋 후 검증

| 항목 | 값 |
|---|---|
| 남은 변경 목록 | 커밋4 직전 `docs/현황.md` 하나(의도된 것) → 커밋4 뒤 **비어 있음** |
| `index.html` 크기 (작업본) | **936,933 bytes / 12,087 줄** |
| `index.html` 크기 (커밋 `ff6f34a` 안) | **936,933 bytes / 12,087 줄** — **같다** ✅ |
| 로컬 서버 | `http://127.0.0.1:8934/index.html` → **HTTP 200**, 936,933 bytes 응답 ✅ |
| push | **하지 않았다.** `origin/main` = `ecbb17d` 그대로, 로컬이 5개 앞선다 |

### 새로 생긴 커밋 (순서대로)

| 순서 | 해시 | 제목 | 파일 | 줄 |
|---|---|---|---|---|
| 1 | `ff6f34a` | 문구 C2~C11 개정 + 카드 B12 명패 교체 | 1 | +128 / −57 |
| 2 | `3f970b6` | 회차 보고서 및 진행 문서 갱신 | 10 | +1,351 / −1 |
| 3 | `bd32474` | 기타 변경분 (`docs/현황.md` + 이 보고서) | 2 | +196 / −9 |

커밋4를 만든 뒤 `git status`가 **비었고**, `origin/main...main` = `0 5`(로컬이 5개 앞섬, push 없음)였다.

**덧붙인 다섯 번째 커밋** — 커밋4 자신의 해시(`bd32474`)를 `docs/현황.md` 표와 이 표에 채워 넣은
한 줄짜리 후속 커밋이다. 커밋 안에 그 커밋 자신의 해시를 미리 적을 수는 없어서 한 번 더 나눴다.
그 커밋의 해시는 어디에도 안 적는다(같은 문제가 되풀이되므로).

---

## 5단계 — 문서 갱신

`docs/현황.md`의 "**미커밋**: …" 줄을 지우고, 그 자리에 「커밋 이력 (로컬 · push 전)」 표를 넣었다.
표에는 `ecbb17d`(원격 위치)부터 이번 커밋까지 다섯 줄을 적었다.

---

## 보류 · 실패

**없음.** 뺀 파일 0건, 검증 4항목 모두 통과.
다만 **push는 안 했다** — 지시대로다. 로컬이 원격보다 5개 앞선 상태로 남는다.

## 로컬 직링크
http://127.0.0.1:8934/index.html
